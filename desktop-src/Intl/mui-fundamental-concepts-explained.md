---
description: Concetti fondamentali di MUI descritti
ms.assetid: 9ab19a56-4d31-471d-949e-a539751b62e3
title: Concetti fondamentali di MUI descritti
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 82dbeedb246944bef6c23c739eafddff86423b35
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/07/2021
ms.locfileid: "103966932"
---
# <a name="mui-fundamental-concepts-explained"></a><span data-ttu-id="856f6-103">Concetti fondamentali di MUI descritti</span><span class="sxs-lookup"><span data-stu-id="856f6-103">MUI Fundamental Concepts Explained</span></span>

-   [<span data-ttu-id="856f6-104">Prerequisito per MUI</span><span class="sxs-lookup"><span data-stu-id="856f6-104">Prerequisite for MUI</span></span>](#prerequisite-for-mui)
-   [<span data-ttu-id="856f6-105">Separazione del codice sorgente da risorse specifiche del linguaggio</span><span class="sxs-lookup"><span data-stu-id="856f6-105">Separation of source code from language specific resources</span></span>](#separation-of-source-code-from-language-specific-resources)
    -   [<span data-ttu-id="856f6-106">Primi giorni: codice e risorse risiedono insieme</span><span class="sxs-lookup"><span data-stu-id="856f6-106">The early days: code and resources live together</span></span>](#the-early-days-code-and-resources-live-together)
    -   [<span data-ttu-id="856f6-107">Separazione logica del codice e delle risorse localizzabili</span><span class="sxs-lookup"><span data-stu-id="856f6-107">Logically separating code and localizable resources</span></span>](#logically-separating-code-and-localizable-resources)
    -   [<span data-ttu-id="856f6-108">Separazione fisica di codice e risorse</span><span class="sxs-lookup"><span data-stu-id="856f6-108">Physically separating code and resources</span></span>](#physically-separating-code-and-resources)
-   [<span data-ttu-id="856f6-109">Caricamento dinamico di risorse specifiche della lingua</span><span class="sxs-lookup"><span data-stu-id="856f6-109">Dynamically loading language-specific resources</span></span>](#dynamically-loading-language-specific-resources)
-   [<span data-ttu-id="856f6-110">Compilazione di applicazioni MUI</span><span class="sxs-lookup"><span data-stu-id="856f6-110">Building MUI applications</span></span>](#building-mui-applications)

## <a name="prerequisite-for-mui"></a><span data-ttu-id="856f6-111">Prerequisito per MUI</span><span class="sxs-lookup"><span data-stu-id="856f6-111">Prerequisite for MUI</span></span>

<span data-ttu-id="856f6-112">Il prerequisito di base per la creazione di un'applicazione compatibile con MUI per Windows Vista e oltre consiste nel progettare l'applicazione in base alle [linee guida per la globalizzazione](https://msdn.microsoft.com/goglobal/bb688110.aspx)di Windows.</span><span class="sxs-lookup"><span data-stu-id="856f6-112">The basic prerequisite for building a MUI-compliant application for Windows Vista and beyond is to design the application in accordance with Windows [globalization guidelines](https://msdn.microsoft.com/goglobal/bb688110.aspx).</span></span>

## <a name="separation-of-source-code-from-language-specific-resources"></a><span data-ttu-id="856f6-113">Separazione del codice sorgente da risorse specifiche del linguaggio</span><span class="sxs-lookup"><span data-stu-id="856f6-113">Separation of source code from language specific resources</span></span>

<span data-ttu-id="856f6-114">Uno degli elementi fondamentali della tecnologia MUI è la separazione del codice sorgente dell'applicazione da risorse specifiche della lingua, per consentire più efficacemente scenari multilingue nelle applicazioni.</span><span class="sxs-lookup"><span data-stu-id="856f6-114">One of the fundamental premises of MUI technology is the separation of application source code from language-specific resources, to enable multi-lingual scenarios in applications more efficiently.</span></span> <span data-ttu-id="856f6-115">La separazione del codice e delle risorse è stata realizzata tramite meccanismi diversi e a gradi diversi nel tempo, come descritto nelle sezioni seguenti.</span><span class="sxs-lookup"><span data-stu-id="856f6-115">The separation of code and resources has been achieved via different mechanisms and to different degrees over time, as outlined in the following sections.</span></span> <span data-ttu-id="856f6-116">Ogni metodo ha fornito diversi gradi di flessibilità per la compilazione, la distribuzione e la manutenzione dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-116">Each method provided varying degrees of flexibility in building, deploying, and servicing the application.</span></span>

<span data-ttu-id="856f6-117">La soluzione consigliata per le applicazioni compatibili con MUI è l'ultimo metodo illustrato in questo articolo, ovvero la separazione fisica del codice sorgente e delle risorse dell'applicazione, con le risorse stesse ulteriormente suddivise in un file per ogni lingua supportata per la massima flessibilità di compilazione, distribuzione e manutenzione.</span><span class="sxs-lookup"><span data-stu-id="856f6-117">The recommended solution for MUI-compliant applications is the last method outlined here, namely the physical separation of application source code and resources, with the resources themselves further broken down into one file per supported language for maximum flexibility in building, deploying, and servicing.</span></span>

### <a name="the-early-days-code-and-resources-live-together"></a><span data-ttu-id="856f6-118">Primi giorni: codice e risorse risiedono insieme</span><span class="sxs-lookup"><span data-stu-id="856f6-118">The early days: code and resources live together</span></span>

<span data-ttu-id="856f6-119">Inizialmente, le applicazioni localizzate sono state create modificando il codice sorgente e modificando le risorse (in genere stringhe) nel codice stesso e ricompilando le applicazioni.</span><span class="sxs-lookup"><span data-stu-id="856f6-119">Initially, localized applications were created by editing the source code and changing the resources (usually strings) in the code itself and recompiling the applications.</span></span> <span data-ttu-id="856f6-120">Ciò significava che per produrre una versione localizzata, era necessario copiare il codice sorgente originale, tradurre gli elementi di testo (risorse) all'interno del codice sorgente e ricompilare il codice.</span><span class="sxs-lookup"><span data-stu-id="856f6-120">This meant that to produce a localized version, one had to copy the original source code, translate the text (resources) elements within the source code, and recompile the code.</span></span> <span data-ttu-id="856f6-121">Nell'immagine seguente viene illustrato il codice dell'applicazione con testo che deve essere localizzato.</span><span class="sxs-lookup"><span data-stu-id="856f6-121">The following image shows application code with text that needs to be localized.</span></span>

![diagramma concettuale che mostra un'applicazione contenente unità di risorse di testo incorporate](images/mui-fundamental-concepts-explained-fig3.gif)

<span data-ttu-id="856f6-123">Sebbene questo approccio consenta la creazione di applicazioni localizzate, presenta anche svantaggi significativi:</span><span class="sxs-lookup"><span data-stu-id="856f6-123">While this approach enables the creation of localized applications, it also has significant drawbacks:</span></span>

-   <span data-ttu-id="856f6-124">Richiede più versioni del codice sorgente, almeno una per la lingua di origine e una per ogni lingua di destinazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-124">It requires multiple versions of the source code, at least one for the source language and one for each of the target languages.</span></span> <span data-ttu-id="856f6-125">Questa operazione consente di creare problemi significativi durante la sincronizzazione delle diverse versioni di lingua dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-125">This creates significant issues synchronizing the different language releases of the application.</span></span> <span data-ttu-id="856f6-126">In particolare, quando è necessario correggere un difetto nel codice, è necessario correggere il difetto in ogni copia del codice sorgente (uno per lingua).</span><span class="sxs-lookup"><span data-stu-id="856f6-126">In particular, when a defect needs to be fixed in the code, the defect needs to be fixed in every copy of the source code (one per language).</span></span>
-   <span data-ttu-id="856f6-127">È inoltre estremamente soggetto a errori poiché richiede localizzatori, che non sono sviluppatori, per apportare modifiche nel codice sorgente, creando così un notevole rischio in termini di integrità del codice.</span><span class="sxs-lookup"><span data-stu-id="856f6-127">It is also extremely error-prone as it required localizers—who are not developers—to make modifications in source code, thus creating a considerable risk in terms of code integrity.</span></span>

<span data-ttu-id="856f6-128">La combinazione di questi svantaggi rende questa proposta estremamente inefficiente ed è necessario un modello migliore.</span><span class="sxs-lookup"><span data-stu-id="856f6-128">The combination of these drawbacks makes this an extremely inefficient proposition, and a better model was needed.</span></span>

### <a name="logically-separating-code-and-localizable-resources"></a><span data-ttu-id="856f6-129">Separazione logica del codice e delle risorse localizzabili</span><span class="sxs-lookup"><span data-stu-id="856f6-129">Logically separating code and localizable resources</span></span>

<span data-ttu-id="856f6-130">Un miglioramento significativo rispetto al modello precedente è la separazione logica del codice e delle risorse localizzabili per un'applicazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-130">A significant improvement over the preceding model is the logical separation of code and localizable resources for an application.</span></span> <span data-ttu-id="856f6-131">In questo modo si isola il codice dalle risorse e si garantisce che il codice rimanga invariato quando le risorse vengono modificate dalla localizzazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-131">This isolates the code from the resources and ensures that code remains untouched when resources are being changed by localization.</span></span> <span data-ttu-id="856f6-132">Dal punto di vista dell'implementazione, le stringhe e altri elementi dell'interfaccia utente vengono archiviati nei file di risorse, che sono relativamente facili da tradurre e sono separati logicamente dalle sezioni di codice.</span><span class="sxs-lookup"><span data-stu-id="856f6-132">From an implementation standpoint, strings and other user interface elements are stored in resource files, which are relatively easy to translate and are logically separated from the code sections.</span></span>

<span data-ttu-id="856f6-133">Idealmente, l'aggiunta del supporto per qualsiasi lingua può essere semplice quanto la conversione delle risorse localizzabili in questo nuovo linguaggio e l'utilizzo di queste risorse tradotte per creare una nuova versione localizzata dell'applicazione, senza richiedere alcuna modifica del codice.</span><span class="sxs-lookup"><span data-stu-id="856f6-133">Ideally, adding support for any given language can be as simple as translating the localizable resources into this new language and using these translated resources to create a new localized version of the application—without requiring any code modification.</span></span> <span data-ttu-id="856f6-134">Nell'immagine seguente viene illustrato il modo in cui il codice e le risorse localizzabili devono essere separate logicamente all'interno di un'applicazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-134">The following image illustrates how code and localizable resources should be logically separated within an application.</span></span>

![diagramma concettuale che mostra un'applicazione che contiene risorse localizzabili separate dal codice](images/mui-fundamental-concepts-explained-fig4.gif)

<span data-ttu-id="856f6-136">Questo modello consente una creazione più semplice di versioni localizzate di un'applicazione e rappresenta un miglioramento significativo rispetto al modello precedente.</span><span class="sxs-lookup"><span data-stu-id="856f6-136">This model enables an easier creation of localized versions of an application and is a significant improvement over the previous model.</span></span> <span data-ttu-id="856f6-137">Questo modello, implementato tramite l'utilizzo degli strumenti di gestione delle risorse, ha avuto un successo nel corso degli anni e viene ancora comunemente utilizzato da molte applicazioni.</span><span class="sxs-lookup"><span data-stu-id="856f6-137">This model, implemented through the use of resource management tools, has been very successful over the years and is still commonly used by many applications today.</span></span> <span data-ttu-id="856f6-138">Tuttavia, presenta svantaggi significativi:</span><span class="sxs-lookup"><span data-stu-id="856f6-138">However, it does have significant drawbacks:</span></span>

-   <span data-ttu-id="856f6-139">Sebbene siano separate logicamente, il codice e le risorse localizzate vengono ancora Uniti fisicamente in un file eseguibile.</span><span class="sxs-lookup"><span data-stu-id="856f6-139">While logically separated, code and localized resources are still physically joined in one executable file.</span></span> <span data-ttu-id="856f6-140">In particolare, la manutenzione è ancora problematica, in quanto lo stesso difetto del codice (identico in tutte le versioni del linguaggio) richiede una patch per ogni lingua.</span><span class="sxs-lookup"><span data-stu-id="856f6-140">In particular, serviceability is still problematic as the same code defect (identical in all language versions) requires a patch per language.</span></span> <span data-ttu-id="856f6-141">Se, pertanto, un'applicazione viene distribuita in 20 lingue, è necessario emettere una patch del servizio 20 volte (una per ogni lingua), anche se il codice viene corretto una sola volta.</span><span class="sxs-lookup"><span data-stu-id="856f6-141">Therefore, if one is shipping the application in 20 languages, a service patch needs to be issued 20 times (one for each language), even though the code is only fixed once.</span></span>
-   <span data-ttu-id="856f6-142">La distribuzione e l'uso di applicazioni multilingue non sono supportate in modo adeguato da questo modello.</span><span class="sxs-lookup"><span data-stu-id="856f6-142">Distribution and use of multi-lingual applications are not adequately supported by this model.</span></span> <span data-ttu-id="856f6-143">Questo può costituire un problema significativo negli scenari OEM e aziendali.</span><span class="sxs-lookup"><span data-stu-id="856f6-143">This can be a significant problem in OEM and enterprise scenarios.</span></span> <span data-ttu-id="856f6-144">Se, ad esempio, un computer deve essere condiviso tra due utenti che utilizzano linguaggi diversi, sarà necessario installare le applicazioni due volte con questo modello, quindi è necessario abilitare un meccanismo per alternare le due installazioni.</span><span class="sxs-lookup"><span data-stu-id="856f6-144">For instance, if a computer is to be shared between two users using different languages, applications will need to be installed twice with this model, and then some mechanism will need to be enabled to alternate between the two installations.</span></span> <span data-ttu-id="856f6-145">Si presuppone che non siano presenti dipendenze aggiuntive che impediscono l'implementazione di questo scenario.</span><span class="sxs-lookup"><span data-stu-id="856f6-145">This assumes that there are no additional dependencies that prevent even this scenario from being implemented.</span></span> <span data-ttu-id="856f6-146">Nell'immagine seguente viene illustrato un esempio di un singolo difetto del codice che richiede due patch.</span><span class="sxs-lookup"><span data-stu-id="856f6-146">The following image shows an example of a single code defect that requires two patches.</span></span>

![diagramma concettuale che mostra due applicazioni localizzate con lo stesso difetto del codice](images/mui-fundamental-concepts-explained-fig5.gif)

<span data-ttu-id="856f6-148">È chiaro che anche se questo modello funziona bene in alcuni scenari, le limitazioni per le applicazioni multilingue e le relative distribuzioni possono essere molto problematiche.</span><span class="sxs-lookup"><span data-stu-id="856f6-148">It is clear that while this model works well in some scenarios, its limitations for multi-lingual applications and their deployments can be very problematic.</span></span>

<span data-ttu-id="856f6-149">Una variante di questo modello che attenua alcuni problemi di applicazioni multilingue è il modello in cui le risorse localizzabili contengono un set di risorse di lingua diverse.</span><span class="sxs-lookup"><span data-stu-id="856f6-149">A variation of this model that alleviates some of the multi-lingual applications issues is the model where the localizable resources contain a set of different language resources.</span></span> <span data-ttu-id="856f6-150">Questo modello dispone di una codebase comune e di diverse risorse per lingue diverse nella stessa applicazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-150">This model has a common code base and several resources for different languages in the same application.</span></span> <span data-ttu-id="856f6-151">Ad esempio, un'applicazione può essere fornita con inglese, tedesco, francese, spagnolo, olandese e italiano nello stesso pacchetto.</span><span class="sxs-lookup"><span data-stu-id="856f6-151">For example, an application can ship with English, German, French, Spanish, Dutch, and Italian in the same package.</span></span> <span data-ttu-id="856f6-152">Nell'immagine seguente viene illustrata un'applicazione che contiene più risorse di lingua.</span><span class="sxs-lookup"><span data-stu-id="856f6-152">The following image shows an application that contains multiple language resources.</span></span>

![diagramma concettuale che mostra un'applicazione con codice separato da due risorse specifiche delle impostazioni locali](images/mui-fundamental-concepts-explained-fig6.gif)

<span data-ttu-id="856f6-154">Questo modello rende più semplice il servizio dell'applicazione quando è necessario correggere un difetto del codice, ovvero un miglioramento, ma non migliora rispetto ai modelli precedenti quando si tratta di supportare lingue aggiuntive.</span><span class="sxs-lookup"><span data-stu-id="856f6-154">This model makes it easier to service the application when a code defect needs to be fixed—which is an improvement—but does not improve over previous models when it comes to supporting additional languages.</span></span> <span data-ttu-id="856f6-155">In questo caso, è ancora necessario rilasciare una nuova versione dell'applicazione (e tale versione è potenzialmente complessa dalla necessità di garantire che tutte le risorse della lingua siano sincronizzate all'interno della stessa distribuzione).</span><span class="sxs-lookup"><span data-stu-id="856f6-155">In this case, one still needs to release a new version of the application (and that release is potentially complicated by the need to ensure that all language resources are synchronized within the same distribution).</span></span>

### <a name="physically-separating-code-and-resources"></a><span data-ttu-id="856f6-156">Separazione fisica di codice e risorse</span><span class="sxs-lookup"><span data-stu-id="856f6-156">Physically separating code and resources</span></span>

<span data-ttu-id="856f6-157">Il successivo passaggio evoluzionario consiste nel separare fisicamente il codice e le risorse.</span><span class="sxs-lookup"><span data-stu-id="856f6-157">The next evolutionary step is to physically separate code and resources.</span></span> <span data-ttu-id="856f6-158">In questo modello, le risorse vengono astratte dal codice e separate fisicamente in diversi file di implementazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-158">In this model, the resources are abstracted from the code and physically separated in different implementation files.</span></span> <span data-ttu-id="856f6-159">In particolare, ciò significa che il codice può diventare realmente indipendente dal linguaggio; lo stesso codice fisico viene effettivamente spedito per tutte le versioni localizzate di un'applicazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-159">In particular, this means that the code can become truly language independent; the same physical code is actually shipped for all localized versions of an application.</span></span> <span data-ttu-id="856f6-160">Nell'immagine seguente viene illustrato che il codice e le risorse localizzabili devono essere separate fisicamente.</span><span class="sxs-lookup"><span data-stu-id="856f6-160">The following image illustrates that code and localizable resources must be physically separated.</span></span>

![diagramma concettuale che mostra un'applicazione separata dalle risorse localizzabili](images/mui-fundamental-concepts-explained-fig7.gif)

<span data-ttu-id="856f6-162">Questo approccio presenta diversi vantaggi rispetto agli approcci precedenti:</span><span class="sxs-lookup"><span data-stu-id="856f6-162">This approach has several advantages over the previous approaches:</span></span>

-   <span data-ttu-id="856f6-163">La distribuzione di soluzioni multilingue è molto semplificata.</span><span class="sxs-lookup"><span data-stu-id="856f6-163">Deployment of multi-lingual solutions is greatly simplified.</span></span> <span data-ttu-id="856f6-164">L'aggiunta del supporto per qualsiasi lingua specifica richiede solo la distribuzione e l'installazione di un nuovo set di risorse della lingua per questa lingua.</span><span class="sxs-lookup"><span data-stu-id="856f6-164">Adding support for any given language requires only shipping and installing a new set of language resources for this particular language.</span></span> <span data-ttu-id="856f6-165">Questa operazione è particolarmente importante quando le risorse di lingua non sono tutte disponibili simultaneamente.</span><span class="sxs-lookup"><span data-stu-id="856f6-165">This is particularly important when language resources are not all simultaneously available.</span></span> <span data-ttu-id="856f6-166">Con questo modello, è possibile distribuire un'applicazione in un set di linguaggi di base e aumentare il numero di lingue supportate nel tempo senza dover modificare o ridistribuire il codice effettivo.</span><span class="sxs-lookup"><span data-stu-id="856f6-166">With this model, one can deploy an application in a set of core languages and increase the number of supported languages over time without having to modify or redeploy the actual code.</span></span> <span data-ttu-id="856f6-167">Questo vantaggio è ulteriormente esteso da un meccanismo di fallback che consente la localizzazione parziale delle applicazioni e dei componenti di sistema in un determinato linguaggio garantendo che le risorse che non sono presenti in questo linguaggio preferenziale "rientrino" in un'altra lingua che gli utenti comprendono.</span><span class="sxs-lookup"><span data-stu-id="856f6-167">This advantage is further extended by a fallback mechanism that allows for partial localization of applications and system components in a given language by ensuring that resources that are not found in this preferred language "fall back" to another language that the users also understand.</span></span> <span data-ttu-id="856f6-168">In generale, questo modello contribuisce a ridurre il carico considerevole che le aziende globali devono affrontare per la distribuzione di applicazioni in più lingue, perché consente la distribuzione di singole immagini in modo molto più efficace.</span><span class="sxs-lookup"><span data-stu-id="856f6-168">Overall, this model helps alleviate the considerable burden that global companies face in deploying applications in multiple languages as it enables single image deployment in a much more effective way.</span></span>
-   <span data-ttu-id="856f6-169">È stato migliorato il servizio.</span><span class="sxs-lookup"><span data-stu-id="856f6-169">Serviceability is improved.</span></span> <span data-ttu-id="856f6-170">Un difetto del codice deve essere risolto e distribuito solo una volta, poiché il codice è completamente indipendente dalla localizzazione.</span><span class="sxs-lookup"><span data-stu-id="856f6-170">A code defect only needs to be fixed and deployed once, as the code is completely localization-independent.</span></span> <span data-ttu-id="856f6-171">Con questo modello, l'emissione di una correzione per un difetto del codice, purché la modifica non sia correlata all'interfaccia utente, è molto più semplice ed economicamente conveniente rispetto ai modelli precedenti.</span><span class="sxs-lookup"><span data-stu-id="856f6-171">With this model, issuing a correction for a code defect, provided the change is not UI-related, is much simpler and cost effective than in any of the previous models.</span></span>

## <a name="dynamically-loading-language-specific-resources"></a><span data-ttu-id="856f6-172">Caricamento dinamico di risorse specifiche della lingua</span><span class="sxs-lookup"><span data-stu-id="856f6-172">Dynamically loading language-specific resources</span></span>

<span data-ttu-id="856f6-173">I concetti fondamentali di MUI relativi alla [separazione fisica del codice sorgente da risorse specifiche della lingua](#physically-separating-code-and-resources)e alla creazione di un file binario di base indipendente dalla lingua per un'applicazione, configurano essenzialmente un'architettura che contribuisce all'implementazione del caricamento dinamico di risorse specifiche della lingua in base alle impostazioni della lingua dell'utente e del sistema.</span><span class="sxs-lookup"><span data-stu-id="856f6-173">The MUI fundamental concepts of [physically separating source code from language-specific resources](#physically-separating-code-and-resources), and building a language-neutral core binary for an application, essentially set up an architecture that is conducive to implementing the dynamic loading of language-specific resources based on user and system language settings.</span></span>

<span data-ttu-id="856f6-174">Il codice sorgente dell'applicazione incluso nel file binario Core indipendente dalla lingua può utilizzare le API MUI nella piattaforma Windows per astrarre la selezione della lingua dell'interfaccia utente di visualizzazione appropriata per un determinato contesto.</span><span class="sxs-lookup"><span data-stu-id="856f6-174">Application source code bundled into the language-neutral core binary can utilize MUI APIs in the Windows platform to abstract the selection of the appropriate display user interface language for a given context.</span></span> <span data-ttu-id="856f6-175">MUI supporta:</span><span class="sxs-lookup"><span data-stu-id="856f6-175">MUI supports this by:</span></span>

-   <span data-ttu-id="856f6-176">Creazione di un elenco con priorità di lingue dell'interfaccia utente per la visualizzazione in base alle impostazioni a livello di sistema, utente e a livello di applicazione, a livello di utente e di sistema.</span><span class="sxs-lookup"><span data-stu-id="856f6-176">Constructing a prioritized list of display user interface languages based on system, user, and application-level, user-level, and system-level settings.</span></span>
-   <span data-ttu-id="856f6-177">Implementazione di un meccanismo di fallback che sceglie un candidato appropriato da questo elenco di lingue con priorità, in base alla disponibilità delle risorse localizzate.</span><span class="sxs-lookup"><span data-stu-id="856f6-177">Implementing a fallback mechanism that chooses an appropriate candidate from this prioritized list of languages, based on the availability of localized resources.</span></span>

<span data-ttu-id="856f6-178">I vantaggi del caricamento dinamico delle risorse dell'interfaccia utente con fallback con priorità sono:</span><span class="sxs-lookup"><span data-stu-id="856f6-178">The benefits of dynamically loading user-interface resources with prioritized fallback are:</span></span>

-   <span data-ttu-id="856f6-179">Consente la localizzazione parziale delle applicazioni e dei componenti di sistema in una determinata lingua, perché le risorse che non sono presenti in questa lingua preferita verranno automaticamente riportate alla lingua successiva nell'elenco con priorità.</span><span class="sxs-lookup"><span data-stu-id="856f6-179">It allows for partial localization of applications and system components in a given language, as resources that are not found in this preferred language will automatically fall back to the next language on the prioritized list.</span></span>
-   <span data-ttu-id="856f6-180">Consente scenari come il cambio dinamico da un linguaggio a un altro.</span><span class="sxs-lookup"><span data-stu-id="856f6-180">It enables scenarios such as switching dynamically from one language to another.</span></span>
-   <span data-ttu-id="856f6-181">Consente la creazione di immagini di distribuzione singola a livello di area o di tutto il mondo che coprono un set di lingue per OEM e aziende.</span><span class="sxs-lookup"><span data-stu-id="856f6-181">It allows creating regional or worldwide single deployment images that cover a set of languages for OEMs and enterprises.</span></span>

## <a name="building-mui-applications"></a><span data-ttu-id="856f6-182">Compilazione di applicazioni MUI</span><span class="sxs-lookup"><span data-stu-id="856f6-182">Building MUI applications</span></span>

<span data-ttu-id="856f6-183">Nelle sezioni precedenti sono state descritte le opzioni per la separazione del codice sorgente da risorse specifiche della lingua e il vantaggio risultante nella possibilità di utilizzare le API della piattaforma Windows Core per caricare dinamicamente le risorse localizzate.</span><span class="sxs-lookup"><span data-stu-id="856f6-183">The previous sections outlined the options for separating source code from language-specific resources, and the resultant benefit in being able to utilize core Windows platform APIs to dynamically load localized resources.</span></span> <span data-ttu-id="856f6-184">Anche se si tratta di linee guida, è opportuno sottolineare che non esiste un modo prescrittivo specifico per sviluppare un'applicazione MUI per la piattaforma Windows.</span><span class="sxs-lookup"><span data-stu-id="856f6-184">While these are guidelines, it should also be pointed out that there is no specific prescriptive way to develop a MUI application for the Windows platform.</span></span>

<span data-ttu-id="856f6-185">Gli sviluppatori di applicazioni hanno la possibilità di scegliere la modalità di gestione delle diverse impostazioni della lingua dell'interfaccia utente, le opzioni di creazione delle risorse e i metodi di caricamento delle risorse.</span><span class="sxs-lookup"><span data-stu-id="856f6-185">Application developers have full choice in how they handle various user-interface language settings, resource creation options, and resource loading methods.</span></span> <span data-ttu-id="856f6-186">Gli sviluppatori possono valutare le linee guida fornite in questo documento e scegliere una combinazione che soddisfi i requisiti e l'ambiente di sviluppo.</span><span class="sxs-lookup"><span data-stu-id="856f6-186">Developers can evaluate the guidelines provided in this document and choose a combination that fits their requirements and development environment.</span></span>

<span data-ttu-id="856f6-187">Nella tabella seguente sono riepilogate le diverse opzioni di progettazione disponibili per gli sviluppatori di applicazioni che vogliono creare un'applicazione MUI per la piattaforma Windows.</span><span class="sxs-lookup"><span data-stu-id="856f6-187">The following table summarizes the different design options available to application developers who are looking to create a MUI application for the Windows platform.</span></span>



<table>
<thead>
<tr class="header">
<th><span data-ttu-id="856f6-188">Impostazioni della lingua dell'interfaccia utente</span><span class="sxs-lookup"><span data-stu-id="856f6-188">User interface language settings</span></span></th>
<th><span data-ttu-id="856f6-189">Creazione di risorse</span><span class="sxs-lookup"><span data-stu-id="856f6-189">Resource creation</span></span></th>
<th><span data-ttu-id="856f6-190">Caricamento delle risorse</span><span class="sxs-lookup"><span data-stu-id="856f6-190">Resource loading</span></span></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td rowspan="2"><span data-ttu-id="856f6-191">Seguire le impostazioni della lingua dell'interfaccia utente nel sistema operativo $ {REMOVE} $</span><span class="sxs-lookup"><span data-stu-id="856f6-191">Follow UI language settings in the operating system${REMOVE}$</span></span><br />
</td>
<td><span data-ttu-id="856f6-192">Linguaggio singolo in un file binario della risorsa divisa (tecnologia delle risorse MUI)</span><span class="sxs-lookup"><span data-stu-id="856f6-192">Single language in a split resource binary (MUI Resource Technology)</span></span><br/> <span data-ttu-id="856f6-193">-oppure-</span><span class="sxs-lookup"><span data-stu-id="856f6-193">-or-</span></span><br/> <span data-ttu-id="856f6-194">Più lingue in un file binario di risorse non suddivise</span><span class="sxs-lookup"><span data-stu-id="856f6-194">Multiple languages in a non-split resource binary</span></span><br/></td>
<td><span data-ttu-id="856f6-195">L'applicazione chiama funzioni di caricamento delle risorse standard.</span><span class="sxs-lookup"><span data-stu-id="856f6-195">Application calls standard resource loading functions.</span></span> <span data-ttu-id="856f6-196">Le risorse vengono restituite nelle lingue corrispondenti alle impostazioni del sistema operativo.</span><span class="sxs-lookup"><span data-stu-id="856f6-196">Resources are returned in the languages matching the operating system settings.</span></span><br/></td>
</tr>
<tr class="even">
<td><span data-ttu-id="856f6-197">Meccanismo delle risorse specifico dell'applicazione</span><span class="sxs-lookup"><span data-stu-id="856f6-197">Application-specific resource mechanism</span></span><br/></td>
<td><span data-ttu-id="856f6-198">L'applicazione chiama l'API MUI per recuperare le lingue dell'interfaccia utente preferite dal thread o i linguaggi dell'interfaccia utente preferiti dal sistema operativo e usare queste impostazioni per caricare le proprie risorse.</span><span class="sxs-lookup"><span data-stu-id="856f6-198">Application calls MUI API to retrieve the thread-preferred UI languages or process-preferred UI languages from the operating system and use these settings to load its own resources.</span></span><br/></td>

</tr>
<tr class="odd">
<td rowspan="2"><span data-ttu-id="856f6-199">Impostazioni della lingua dell'interfaccia utente specifiche dell'applicazione $ {REMOVE} $</span><span class="sxs-lookup"><span data-stu-id="856f6-199">Application-specific UI language settings${REMOVE}$</span></span><br />
</td>
<td><span data-ttu-id="856f6-200">Linguaggio singolo in un file binario di una risorsa divisa</span><span class="sxs-lookup"><span data-stu-id="856f6-200">Single language in a split resource binary</span></span><br/> <span data-ttu-id="856f6-201">-oppure-</span><span class="sxs-lookup"><span data-stu-id="856f6-201">-or-</span></span><br/> <span data-ttu-id="856f6-202">Più lingue in un file binario di risorse non suddivise</span><span class="sxs-lookup"><span data-stu-id="856f6-202">Multiple languages in a non-split resource binary</span></span><br/></td>
<td><span data-ttu-id="856f6-203">L'applicazione chiama l'API MUI per impostare le lingue dell'interfaccia utente specifiche dell'applicazione o le lingue dell'interfaccia utente preferite del processo e quindi chiama le funzioni standard di caricamento delle risorse.</span><span class="sxs-lookup"><span data-stu-id="856f6-203">Application calls MUI API to set application-specific UI languages or process-preferred UI languages and then calls standard resource loading functions.</span></span> <span data-ttu-id="856f6-204">Le risorse vengono restituite nelle lingue impostate dalle lingue dell'applicazione o del sistema.</span><span class="sxs-lookup"><span data-stu-id="856f6-204">Resources are returned in the languages set by the application or system languages.</span></span><br/> <span data-ttu-id="856f6-205">-oppure-</span><span class="sxs-lookup"><span data-stu-id="856f6-205">-or-</span></span><br/> <span data-ttu-id="856f6-206">L'applicazione esegue il probe delle risorse in una lingua specifica e gestisce la propria estrazione delle risorse dai dati binari recuperati.</span><span class="sxs-lookup"><span data-stu-id="856f6-206">Application probes resources in a specific language and handles its own resource extraction from the retrieved binary data.</span></span><br/></td>
</tr>
<tr class="even">
<td><span data-ttu-id="856f6-207">Meccanismo delle risorse specifico dell'applicazione</span><span class="sxs-lookup"><span data-stu-id="856f6-207">Application-specific resource mechanism</span></span><br/></td>
<td><span data-ttu-id="856f6-208">L'applicazione gestisce il caricamento delle risorse.</span><span class="sxs-lookup"><span data-stu-id="856f6-208">Application manages its own resource loading.</span></span><br/></td>

</tr>
</tbody>
</table>



 

 

 




