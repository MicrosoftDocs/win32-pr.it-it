---
description: Nel tempo è possibile che vengano generate versioni diverse di TAPI, applicazioni e provider di servizi.
ms.assetid: 994fad0e-5958-4d93-8952-9db2bbe01f44
title: Controllo delle versioni di TSPI
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: dfe405a629ddc64f76535b33554509b1a6fca81d
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/07/2021
ms.locfileid: "104526814"
---
# <a name="tspi-versioning"></a><span data-ttu-id="f92df-103">Controllo delle versioni di TSPI</span><span class="sxs-lookup"><span data-stu-id="f92df-103">TSPI Versioning</span></span>

<span data-ttu-id="f92df-104">Nel tempo è possibile che vengano generate versioni diverse di TAPI, applicazioni e provider di servizi.</span><span class="sxs-lookup"><span data-stu-id="f92df-104">Over time, different versions of TAPI, applications, and service providers may be produced.</span></span> <span data-ttu-id="f92df-105">Queste nuove versioni possono creare nuove definizioni, ad esempio per nuove funzionalità, nuovi membri nelle strutture di dati e nuovi campi di bit.</span><span class="sxs-lookup"><span data-stu-id="f92df-105">These new versions can make new definitions, such as for new features, new members in data structures, and new bit fields.</span></span> <span data-ttu-id="f92df-106">I numeri di versione sono pertanto necessari per indicare come interpretare diverse strutture di dati.</span><span class="sxs-lookup"><span data-stu-id="f92df-106">Version numbers are therefore necessary to indicate how to interpret various data structures.</span></span>

<span data-ttu-id="f92df-107">Per consentire l'interoperabilità ottimale di versioni diverse di applicazioni, versioni di TAPI e versioni di provider di servizi da fornitori diversi, la telefonia Microsoft fornisce un meccanismo di negoziazione della versione semplice per le applicazioni.</span><span class="sxs-lookup"><span data-stu-id="f92df-107">To allow optimal interoperability of different versions of applications, versions of TAPI itself, and versions of service providers by different vendors, Microsoft Telephony provides a simple version negotiation mechanism for applications.</span></span> <span data-ttu-id="f92df-108">Ci sono due versioni diverse che devono essere concordate da TAPI e dal provider di servizi di telefonia per ogni dispositivo line.</span><span class="sxs-lookup"><span data-stu-id="f92df-108">There are two different versions that need to be agreed on by TAPI and the telephony service provider for each line device.</span></span> <span data-ttu-id="f92df-109">Il primo è il numero di versione per la telefonia SPI di base e supplementare, denominata versione dell' *interfaccia TSPI*.</span><span class="sxs-lookup"><span data-stu-id="f92df-109">The first is the version number for the Basic and Supplementary Telephony SPI, referred to as the *TSPI interface version*.</span></span> <span data-ttu-id="f92df-110">L'altro è per le estensioni specifiche del provider, se presente, e viene indicato come *versione dell'estensione*.</span><span class="sxs-lookup"><span data-stu-id="f92df-110">The other is for provider-specific extensions, if any, and is referred to as the *extension version*.</span></span> <span data-ttu-id="f92df-111">Il formato delle strutture di dati e dei tipi di dati usati dalle funzionalità di base e supplementare di TSPI è definito dalla versione TSPI, mentre la versione dell'estensione determina il formato delle strutture di dati definite dalle estensioni specifiche del fornitore.</span><span class="sxs-lookup"><span data-stu-id="f92df-111">The format of the data structures and datatypes used by TSPI's Basic and Supplementary features is defined by the TSPI version, while the extension version determines the format of data structures defined by the vendor-specific extensions.</span></span>

<span data-ttu-id="f92df-112">Questi due tipi di negoziazione della versione vengono gestiti da due procedure diverse: [**TSPI \_ lineNegotiateTSPIVersion**](/windows/win32/api/tspi/nf-tspi-tspi_linenegotiatetspiversion) viene usato per negoziare la versione dell'interfaccia TSPI e [**TSPI \_ lineNegotiateExtVersion**](/windows/win32/api/tspi/nf-tspi-tspi_linenegotiateextversion) viene usato per negoziare la versione dell'estensione.</span><span class="sxs-lookup"><span data-stu-id="f92df-112">These two types of version negotiation are handled by two different procedures: [**TSPI\_lineNegotiateTSPIVersion**](/windows/win32/api/tspi/nf-tspi-tspi_linenegotiatetspiversion) is used to negotiate the TSPI interface version, and [**TSPI\_lineNegotiateExtVersion**](/windows/win32/api/tspi/nf-tspi-tspi_linenegotiateextversion) is used to negotiate the extension version.</span></span> <span data-ttu-id="f92df-113">La negoziazione della versione dell'estensione può essere ignorata se non si desiderano le estensioni.</span><span class="sxs-lookup"><span data-stu-id="f92df-113">Extension version negotiation can be skipped if extensions are not wanted.</span></span> <span data-ttu-id="f92df-114">Se questi intervalli di input durante la negoziazione si sovrappongono, il provider di servizi deve restituire un valore all'interno della parte sovrapposta dell'intervallo come risultato della negoziazione.</span><span class="sxs-lookup"><span data-stu-id="f92df-114">If these ranges input during negotiation overlap, the service provider must return a value within the overlapping portion of the range as the result of the negotiation.</span></span> <span data-ttu-id="f92df-115">In genere, deve essere il valore massimo possibile.</span><span class="sxs-lookup"><span data-stu-id="f92df-115">Usually, this should be the highest possible value.</span></span> <span data-ttu-id="f92df-116">Se gli intervalli non si sovrappongono, le due entità sono incompatibili e la funzione restituisce un errore.</span><span class="sxs-lookup"><span data-stu-id="f92df-116">If the ranges do not overlap, the two parties are incompatible and the function returns an error.</span></span>

<span data-ttu-id="f92df-117">I risultati di una negoziazione indicano semplicemente che il provider di servizi è disposto a funzionare con un determinato numero di versione, ma non esegue il commit del provider di servizi.</span><span class="sxs-lookup"><span data-stu-id="f92df-117">Results of a negotiation simply indicate that the service provider is willing to operate at a particular version number, but do not commit the service provider to doing so.</span></span> <span data-ttu-id="f92df-118">Ad esempio, è possibile rinegoziare TAPI per determinare una versione ideale dopo aver negoziato una versione possibile.</span><span class="sxs-lookup"><span data-stu-id="f92df-118">For example, TAPI can renegotiate to determine an ideal version after having negotiated a possible version.</span></span> <span data-ttu-id="f92df-119">Viene eseguito il commit della versione dell'interfaccia TSPI solo quando viene aperta una riga con [**TSPI \_ lineOpen**](/windows/win32/api/tspi/nf-tspi-tspi_lineopen) e viene mantenuta fino alla chiusura del dispositivo.</span><span class="sxs-lookup"><span data-stu-id="f92df-119">The TSPI interface version is only committed when a line is opened using [**TSPI\_lineOpen**](/windows/win32/api/tspi/nf-tspi-tspi_lineopen) and survives until the device is closed.</span></span> <span data-ttu-id="f92df-120">Viene eseguito il commit della versione dell'estensione quando viene chiamata la funzione [**TSPI \_ lineSelectExtVersion**](/windows/win32/api/tspi/nf-tspi-tspi_lineselectextversion) , che rimane in attesa fino a quando la selezione non viene annullata selezionando versione dell'estensione zero.</span><span class="sxs-lookup"><span data-stu-id="f92df-120">The extension version is committed when the [**TSPI\_lineSelectExtVersion**](/windows/win32/api/tspi/nf-tspi-tspi_lineselectextversion) function is called, and survives until the selection is canceled by selecting extension version zero.</span></span>

<span data-ttu-id="f92df-121">La selezione della versione dell'estensione può essere eseguita più volte, incluso mentre è attiva una versione di estensione.</span><span class="sxs-lookup"><span data-stu-id="f92df-121">Extension version selection can happen many times, including while an extension version is in effect.</span></span> <span data-ttu-id="f92df-122">Poiché viene eseguito il commit del provider di servizi nella versione dell'estensione, il relativo intervallo di versioni supportate viene ridotto esattamente a tale versione dell'estensione.</span><span class="sxs-lookup"><span data-stu-id="f92df-122">Because the service provider is committed to the extension version, its range of supported versions narrows to exactly that extension version.</span></span> <span data-ttu-id="f92df-123">Si consideri, ad esempio, un provider di servizi normalmente compatibile con le versioni di estensione da 1,0 a 5,5.</span><span class="sxs-lookup"><span data-stu-id="f92df-123">For example, consider a service provider that is normally compatible with extension versions 1.0 through 5.5.</span></span> <span data-ttu-id="f92df-124">Se è attiva la versione 3,0 mentre un chiamante tenta di negoziare una versione nell'intervallo compreso tra 1,0 e 5,5, la negoziazione restituisce 3,0.</span><span class="sxs-lookup"><span data-stu-id="f92df-124">If version 3.0 is in effect while a caller attempts to negotiate a version within the range 1.0 to 5.5, the negotiation returns 3.0.</span></span>

<span data-ttu-id="f92df-125">Poiché TAPI negozia la versione, è possibile aggiornare un provider di servizi a nuove versioni dell'interfaccia senza che sia necessario aggiornare anche TAPI.</span><span class="sxs-lookup"><span data-stu-id="f92df-125">Because TAPI negotiates the version, you can upgrade a service provider to new versions of the interface without requiring that TAPI also be upgraded.</span></span> <span data-ttu-id="f92df-126">Analogamente, è possibile aggiornare TAPI, ma continuare a usare il provider di servizi meno recente.</span><span class="sxs-lookup"><span data-stu-id="f92df-126">Similarly, TAPI can be upgraded, yet still use your older service provider.</span></span>

 

 
