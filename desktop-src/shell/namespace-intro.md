---
description: Lo spazio dei nomi della shell organizza il file system e altri oggetti gestiti dalla shell in una singola gerarchia strutturata ad albero. Concettualmente, si tratta di una versione più ampia e più completa del file system.
ms.assetid: 539c4455-e1c7-45a0-b3c3-781f2b7a1617
title: Introduzione allo spazio dei nomi della shell
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 28b9ccb87cc67c025f09233128780545d610f90d
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/07/2021
ms.locfileid: "104344151"
---
# <a name="introduction-to-the-shell-namespace"></a><span data-ttu-id="75a07-104">Introduzione allo spazio dei nomi della shell</span><span class="sxs-lookup"><span data-stu-id="75a07-104">Introduction to the Shell Namespace</span></span>

<span data-ttu-id="75a07-105">Lo *spazio dei nomi* della shell organizza il file System e altri oggetti gestiti dalla shell in una singola gerarchia strutturata ad albero.</span><span class="sxs-lookup"><span data-stu-id="75a07-105">The Shell *namespace* organizes the file system and other objects managed by the Shell into a single tree-structured hierarchy.</span></span> <span data-ttu-id="75a07-106">Concettualmente, si tratta di una versione più ampia e più completa del file system.</span><span class="sxs-lookup"><span data-stu-id="75a07-106">Conceptually, it is a larger and more inclusive version of the file system.</span></span>

-   [<span data-ttu-id="75a07-107">Introduzione</span><span class="sxs-lookup"><span data-stu-id="75a07-107">Introduction</span></span>](#introduction)
-   [<span data-ttu-id="75a07-108">Identificazione di oggetti spazio dei nomi</span><span class="sxs-lookup"><span data-stu-id="75a07-108">Identifying Namespace Objects</span></span>](#identifying-namespace-objects)
    -   [<span data-ttu-id="75a07-109">ID elemento</span><span class="sxs-lookup"><span data-stu-id="75a07-109">Item IDs</span></span>](#item-ids)
    -   [<span data-ttu-id="75a07-110">Elenchi di ID elemento</span><span class="sxs-lookup"><span data-stu-id="75a07-110">Item ID Lists</span></span>](#item-id-lists)
    -   [<span data-ttu-id="75a07-111">PIDL</span><span class="sxs-lookup"><span data-stu-id="75a07-111">PIDLs</span></span>](#pidls)
    -   [<span data-ttu-id="75a07-112">Allocazione di PIDL</span><span class="sxs-lookup"><span data-stu-id="75a07-112">Allocating PIDLs</span></span>](#allocating-pidls)

## <a name="introduction"></a><span data-ttu-id="75a07-113">Introduzione</span><span class="sxs-lookup"><span data-stu-id="75a07-113">Introduction</span></span>

<span data-ttu-id="75a07-114">Una delle responsabilità principali della shell è la gestione e l'accesso all'ampia gamma di oggetti che costituiscono il sistema.</span><span class="sxs-lookup"><span data-stu-id="75a07-114">One of the primary responsibilities of the Shell is managing and providing access to the wide variety of objects that make up the system.</span></span> <span data-ttu-id="75a07-115">I più numerosi e familiari di questi oggetti sono le cartelle e i file che risiedono in unità disco del computer.</span><span class="sxs-lookup"><span data-stu-id="75a07-115">The most numerous and familiar of these objects are the folders and files that reside on computer disk drives.</span></span> <span data-ttu-id="75a07-116">Tuttavia, la shell gestisce anche un certo numero di oggetti non file System o *virtuali* .</span><span class="sxs-lookup"><span data-stu-id="75a07-116">However, the Shell manages a number of nonfile system, or *virtual* objects, as well.</span></span> <span data-ttu-id="75a07-117">Di seguito sono riportati alcuni esempi:</span><span class="sxs-lookup"><span data-stu-id="75a07-117">Some examples include:</span></span>

-   <span data-ttu-id="75a07-118">Stampanti di rete</span><span class="sxs-lookup"><span data-stu-id="75a07-118">Network printers</span></span>
-   <span data-ttu-id="75a07-119">Altri computer in rete</span><span class="sxs-lookup"><span data-stu-id="75a07-119">Other networked computers</span></span>
-   <span data-ttu-id="75a07-120">Applicazioni del pannello di controllo</span><span class="sxs-lookup"><span data-stu-id="75a07-120">Control Panel applications</span></span>
-   <span data-ttu-id="75a07-121">Cestino</span><span class="sxs-lookup"><span data-stu-id="75a07-121">The Recycle Bin</span></span>

<span data-ttu-id="75a07-122">Alcuni oggetti virtuali non implicano alcuna archiviazione fisica.</span><span class="sxs-lookup"><span data-stu-id="75a07-122">Some virtual objects do not involve physical storage at all.</span></span> <span data-ttu-id="75a07-123">L'oggetto Printer, ad esempio, contiene una raccolta di collegamenti alle stampanti di rete.</span><span class="sxs-lookup"><span data-stu-id="75a07-123">The printer object, for instance, contains a collection of links to networked printers.</span></span> <span data-ttu-id="75a07-124">Altri oggetti virtuali, ad esempio il Cestino, possono contenere dati archiviati in un'unità disco, ma devono essere gestiti in modo diverso rispetto ai file normali.</span><span class="sxs-lookup"><span data-stu-id="75a07-124">Other virtual objects, such as the Recycle Bin, may contain data that is stored on a disk drive, but needs to be handled differently than normal files.</span></span> <span data-ttu-id="75a07-125">È ad esempio possibile utilizzare un oggetto virtuale per rappresentare i dati archiviati in un database.</span><span class="sxs-lookup"><span data-stu-id="75a07-125">For example, a virtual object can be used to represent data stored in a database.</span></span> <span data-ttu-id="75a07-126">In termini di spazio dei nomi, i vari elementi nel database possono essere visualizzati in Esplora risorse come oggetti separati, anche se sono tutti archiviati in un singolo file su disco.</span><span class="sxs-lookup"><span data-stu-id="75a07-126">In terms of the namespace, the various items in the database could appear in the Windows Explorer as separate objects, even though they are all stored in a single disk file.</span></span>

<span data-ttu-id="75a07-127">È possibile che gli oggetti virtuali si trovino anche sui computer remoti.</span><span class="sxs-lookup"><span data-stu-id="75a07-127">Virtual objects may even be located on remote computers.</span></span> <span data-ttu-id="75a07-128">Ad esempio, per facilitare il roaming, i file di documento di un utente possono essere archiviati in un server.</span><span class="sxs-lookup"><span data-stu-id="75a07-128">For instance, to facilitate roaming, a user's document files might be stored on a server.</span></span> <span data-ttu-id="75a07-129">Per concedere agli utenti l'accesso ai file da più PC desktop, la cartella documenti sul PC desktop attualmente in uso punterà al server, non al disco rigido del PC desktop.</span><span class="sxs-lookup"><span data-stu-id="75a07-129">To give users access to their files from multiple desktop PCs, the My Documents folder on the desktop PC they are currently using will point to the server, not the hard disk of the desktop PC.</span></span> <span data-ttu-id="75a07-130">Il percorso includerà un'unità di rete mappata o un nome di percorso UNC.</span><span class="sxs-lookup"><span data-stu-id="75a07-130">Its path will include either a mapped network drive or a UNC path name.</span></span>

<span data-ttu-id="75a07-131">Analogamente al file system, lo spazio dei nomi include due tipi di base di oggetto: cartelle e file.</span><span class="sxs-lookup"><span data-stu-id="75a07-131">Like the file system, the namespace includes two basic types of object: folders and files.</span></span> <span data-ttu-id="75a07-132">Gli oggetti folder sono i *nodi* dell'albero. si tratta di contenitori per oggetti file e altre cartelle.</span><span class="sxs-lookup"><span data-stu-id="75a07-132">Folder objects are the *nodes* of the tree; they are containers for file objects and other folders.</span></span> <span data-ttu-id="75a07-133">Gli oggetti file sono le *foglie* dell'albero. si tratta di normali file su disco o oggetti virtuali, ad esempio collegamenti alla stampante.</span><span class="sxs-lookup"><span data-stu-id="75a07-133">File objects are the *leaves* of the tree; they are either normal disk files or virtual objects, such as printer links.</span></span> <span data-ttu-id="75a07-134">Le cartelle che non fanno parte del file system sono talvolta denominate *cartelle virtuali*.</span><span class="sxs-lookup"><span data-stu-id="75a07-134">Folders that are not part of the file system are sometimes referred to as *virtual folders*.</span></span>

<span data-ttu-id="75a07-135">Come file system cartelle, la raccolta di cartelle virtuali varia in genere da sistema a sistema.</span><span class="sxs-lookup"><span data-stu-id="75a07-135">Like file system folders, the collection of virtual folders generally varies from system to system.</span></span> <span data-ttu-id="75a07-136">Sono disponibili tre classi di cartelle virtuali:</span><span class="sxs-lookup"><span data-stu-id="75a07-136">There are three classes of virtual folders:</span></span>

-   <span data-ttu-id="75a07-137">Cartelle virtuali standard, ad esempio il Cestino, presenti in tutti i sistemi.</span><span class="sxs-lookup"><span data-stu-id="75a07-137">Standard virtual folders, such as the Recycle Bin, that are found on all systems.</span></span>
-   <span data-ttu-id="75a07-138">Cartelle virtuali facoltative con nomi e funzionalità standard, ma che potrebbero non essere presenti in tutti i sistemi.</span><span class="sxs-lookup"><span data-stu-id="75a07-138">Optional virtual folders that have standard names and functionality, but may not be present on all systems.</span></span>
-   <span data-ttu-id="75a07-139">Cartelle non standard installate dall'utente.</span><span class="sxs-lookup"><span data-stu-id="75a07-139">Non-standard folders that are installed by the user.</span></span>

<span data-ttu-id="75a07-140">A differenza di file system cartelle, gli utenti non possono creare nuove cartelle virtuali.</span><span class="sxs-lookup"><span data-stu-id="75a07-140">Unlike file system folders, users cannot create new virtual folders themselves.</span></span> <span data-ttu-id="75a07-141">Possono installare solo quelli creati da sviluppatori non Microsoft.</span><span class="sxs-lookup"><span data-stu-id="75a07-141">They can only install ones created by non-Microsoft developers.</span></span> <span data-ttu-id="75a07-142">Il numero di cartelle virtuali è quindi in genere molto inferiore al numero di cartelle file system.</span><span class="sxs-lookup"><span data-stu-id="75a07-142">The number of virtual folders is thus normally much fewer than the number of file system folders.</span></span> <span data-ttu-id="75a07-143">Per informazioni su come implementare le cartelle virtuali, vedere [estensioni dello spazio dei nomi](nse-works.md).</span><span class="sxs-lookup"><span data-stu-id="75a07-143">For a discussion of how to implement virtual folders see [Namespace Extensions](nse-works.md).</span></span>

<span data-ttu-id="75a07-144">È possibile visualizzare una rappresentazione visiva del modo in cui lo spazio dei nomi è strutturato nella barra di Explorer di Esplora risorse.</span><span class="sxs-lookup"><span data-stu-id="75a07-144">You can see a visual representation of how the namespace is structured in the Explorer Bar of the Windows Explorer.</span></span> <span data-ttu-id="75a07-145">Ad esempio, lo screenshot seguente di Esplora risorse Mostra uno spazio dei nomi relativamente semplice.</span><span class="sxs-lookup"><span data-stu-id="75a07-145">For example, the following screen shot of Windows Explorer shows a relatively simple namespace.</span></span>

![visualizzazione dello spazio dei nomi della shell](images/prog1.png)

<span data-ttu-id="75a07-147">La radice finale della gerarchia dello spazio dei nomi è il desktop.</span><span class="sxs-lookup"><span data-stu-id="75a07-147">The ultimate root of the namespace hierarchy is the desktop.</span></span> <span data-ttu-id="75a07-148">Immediatamente sotto la radice sono presenti diverse cartelle virtuali, ad esempio Computer locale e il Cestino.</span><span class="sxs-lookup"><span data-stu-id="75a07-148">Immediately below the root are several virtual folders such as My Computer and the Recycle Bin.</span></span>

<span data-ttu-id="75a07-149">I file System delle varie unità disco possono essere considerati come subset della gerarchia dello spazio dei nomi più grande.</span><span class="sxs-lookup"><span data-stu-id="75a07-149">The file systems of the various disk drives can be seen to be subsets of the larger namespace hierarchy.</span></span> <span data-ttu-id="75a07-150">Le radici di questi file System sono le sottocartelle della cartella Computer locale.</span><span class="sxs-lookup"><span data-stu-id="75a07-150">The roots of these file systems are subfolders of the My Computer folder.</span></span> <span data-ttu-id="75a07-151">Computer locale include anche le radici delle unità di rete mappate.</span><span class="sxs-lookup"><span data-stu-id="75a07-151">My Computer also includes the roots of any mapped network drives.</span></span> <span data-ttu-id="75a07-152">Gli altri nodi dell'albero, ad esempio documenti, sono cartelle virtuali.</span><span class="sxs-lookup"><span data-stu-id="75a07-152">Other nodes in the tree, such as My Documents, are virtual folders.</span></span>

## <a name="identifying-namespace-objects"></a><span data-ttu-id="75a07-153">Identificazione di oggetti spazio dei nomi</span><span class="sxs-lookup"><span data-stu-id="75a07-153">Identifying Namespace Objects</span></span>

<span data-ttu-id="75a07-154">Prima di poter utilizzare un oggetto spazio dei nomi, è necessario innanzitutto disporre di un modo per identificarlo.</span><span class="sxs-lookup"><span data-stu-id="75a07-154">Before you can use a namespace object, you must first have a way of identifying it.</span></span> <span data-ttu-id="75a07-155">Un oggetto nel file system potrebbe avere un nome, ad esempio MyFile.htm.</span><span class="sxs-lookup"><span data-stu-id="75a07-155">An object in the file system could have a name such as MyFile.htm.</span></span> <span data-ttu-id="75a07-156">Poiché potrebbero essere presenti altri file con lo stesso nome in un'altra posizione del sistema, l'identificazione univoca di un file o di una cartella richiede un percorso completo, ad esempio "C: \\ docs \\MyFile.htm".</span><span class="sxs-lookup"><span data-stu-id="75a07-156">Because there might be other files with that name elsewhere in the system, uniquely identifying a file or folder requires a fully qualified path such as "C:\\MyDocs\\MyFile.htm".</span></span> <span data-ttu-id="75a07-157">Questo percorso è fondamentalmente un elenco ordinato di tutte le cartelle in un percorso dalla radice file system, C: \\ , che termina con il file.</span><span class="sxs-lookup"><span data-stu-id="75a07-157">This path is basically an ordered list of all folders in a path from the file system root, C:\\, ending with the file.</span></span>

<span data-ttu-id="75a07-158">Nel contesto dello spazio dei nomi, i percorsi sono comunque molto utili per identificare gli oggetti che si trovano nella parte file system dello spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="75a07-158">In the context of the namespace, paths are still quite useful for identifying objects located in the file system part of the namespace.</span></span> <span data-ttu-id="75a07-159">Tuttavia, non possono essere utilizzati per gli oggetti virtuali.</span><span class="sxs-lookup"><span data-stu-id="75a07-159">However, they cannot be used for virtual objects.</span></span> <span data-ttu-id="75a07-160">La shell fornisce invece un mezzo di identificazione alternativo che può essere usato con qualsiasi oggetto spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="75a07-160">Instead, the Shell provides an alternative means of identification that can be used with any namespace object.</span></span>

### <a name="item-ids"></a><span data-ttu-id="75a07-161">ID elemento</span><span class="sxs-lookup"><span data-stu-id="75a07-161">Item IDs</span></span>

<span data-ttu-id="75a07-162">All'interno di una cartella, ogni oggetto dispone di un *ID elemento*, che è l'equivalente funzionale del nome di un file o di una cartella.</span><span class="sxs-lookup"><span data-stu-id="75a07-162">Within a folder, each object has an *item ID*, which is the functional equivalent of a file or folder name.</span></span> <span data-ttu-id="75a07-163">L'ID dell'elemento è in realtà una struttura [**SHITEMID**](/windows/desktop/api/Shtypes/ns-shtypes-shitemid) :</span><span class="sxs-lookup"><span data-stu-id="75a07-163">The item ID is actually a [**SHITEMID**](/windows/desktop/api/Shtypes/ns-shtypes-shitemid) structure:</span></span>


```
typedef struct _SHITEMID { 
    USHORT cb; 
    BYTE   abID[1]; 
} SHITEMID, * LPSHITEMID; 
```



<span data-ttu-id="75a07-164">Il membro **permanente** è l'identificatore dell'oggetto.</span><span class="sxs-lookup"><span data-stu-id="75a07-164">The **abID** member is the object's identifier.</span></span> <span data-ttu-id="75a07-165">La lunghezza di **subatter** non è definita e il relativo valore è determinato dalla cartella che contiene l'oggetto.</span><span class="sxs-lookup"><span data-stu-id="75a07-165">The length of **abID** is not defined, and its value is determined by the folder that contains the object.</span></span> <span data-ttu-id="75a07-166">Poiché non esiste una definizione standard per il **modo in** cui i valori di tipo subentrano assegnati dalle cartelle, sono significativi solo per l'oggetto cartella associato.</span><span class="sxs-lookup"><span data-stu-id="75a07-166">Because there is no standard definition for how **abID** values are assigned by folders, they are only meaningful to the associated folder object.</span></span> <span data-ttu-id="75a07-167">Le applicazioni devono semplicemente gestirle come token che identifica un oggetto in una cartella specifica.</span><span class="sxs-lookup"><span data-stu-id="75a07-167">Applications should simply treat them as a token that identifies an object in a particular folder.</span></span> <span data-ttu-id="75a07-168">Poiché la lunghezza di **attention** varia, il membro **CB** include le dimensioni della struttura [**SHITEMID**](/windows/desktop/api/Shtypes/ns-shtypes-shitemid) in byte.</span><span class="sxs-lookup"><span data-stu-id="75a07-168">Because the length of **abID** varies, the **cb** member holds the size of the [**SHITEMID**](/windows/desktop/api/Shtypes/ns-shtypes-shitemid) structure, in bytes.</span></span>

<span data-ttu-id="75a07-169">Poiché gli ID elemento non sono utili a scopo di visualizzazione, la cartella che contiene l'oggetto normalmente assegna un nome visualizzato.</span><span class="sxs-lookup"><span data-stu-id="75a07-169">Because item IDs aren't useful for display purposes, the folder that contains the object normally assigns it a display name.</span></span> <span data-ttu-id="75a07-170">Si tratta del nome utilizzato da Esplora risorse quando viene visualizzato il contenuto di una cartella.</span><span class="sxs-lookup"><span data-stu-id="75a07-170">This is the name that is used by Windows Explorer when it displays the contents of a folder.</span></span> <span data-ttu-id="75a07-171">Per ulteriori informazioni su come vengono gestiti i nomi visualizzati, vedere [recupero di informazioni da una cartella](folder-info.md).</span><span class="sxs-lookup"><span data-stu-id="75a07-171">For more information on how display names are handled, see [Getting Information From a Folder](folder-info.md).</span></span>

### <a name="item-id-lists"></a><span data-ttu-id="75a07-172">Elenchi di ID elemento</span><span class="sxs-lookup"><span data-stu-id="75a07-172">Item ID Lists</span></span>

<span data-ttu-id="75a07-173">L'ID dell'elemento viene raramente usato da solo.</span><span class="sxs-lookup"><span data-stu-id="75a07-173">The item ID is rarely used by itself.</span></span> <span data-ttu-id="75a07-174">In genere, fa parte di un elenco di ID elemento, che svolge la stessa funzione di un percorso di file system.</span><span class="sxs-lookup"><span data-stu-id="75a07-174">Normally, it is part of an item ID list, which serves the same purpose as a file system path.</span></span> <span data-ttu-id="75a07-175">Tuttavia, invece della stringa di caratteri utilizzata per i percorsi, un elenco di ID elemento è una struttura [**ItemId**](/windows/desktop/api/Shtypes/ns-shtypes-itemidlist) .</span><span class="sxs-lookup"><span data-stu-id="75a07-175">However, instead of the character string used for paths, an item ID list is an [**ITEMIDLIST**](/windows/desktop/api/Shtypes/ns-shtypes-itemidlist) structure.</span></span> <span data-ttu-id="75a07-176">Questa struttura è una sequenza ordinata di uno o più ID elemento, terminati da un **valore null** a 2 byte.</span><span class="sxs-lookup"><span data-stu-id="75a07-176">This structure is an ordered sequence of one or more item IDs, terminated by a two-byte **NULL**.</span></span> <span data-ttu-id="75a07-177">Ogni ID elemento nell'elenco ID elemento corrisponde a un oggetto spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="75a07-177">Each item ID in the item ID list corresponds to a namespace object.</span></span> <span data-ttu-id="75a07-178">Il rispettivo ordine definisce un percorso nello spazio dei nomi, molto simile a un percorso file system.</span><span class="sxs-lookup"><span data-stu-id="75a07-178">Their order defines a path in the namespace, much like a file system path.</span></span>

<span data-ttu-id="75a07-179">Nella figura seguente è illustrata una rappresentazione schematica della struttura [**ItemId**](/windows/desktop/api/Shtypes/ns-shtypes-itemidlist) che corrisponde a C: \\ docs \\MyFile.htm.</span><span class="sxs-lookup"><span data-stu-id="75a07-179">The following illustration shows a schematic representation of the [**ITEMIDLIST**](/windows/desktop/api/Shtypes/ns-shtypes-itemidlist) structure that corresponds to C:\\MyDocs\\MyFile.htm.</span></span> <span data-ttu-id="75a07-180">Il nome visualizzato di ogni ID elemento viene visualizzato sopra.</span><span class="sxs-lookup"><span data-stu-id="75a07-180">The display name of each item ID is shown above it.</span></span> <span data-ttu-id="75a07-181">Le larghezze variabili dei membri **rimanenti** sono arbitrarie; viene illustrato il fatto che le dimensioni di questo membro possono variare.</span><span class="sxs-lookup"><span data-stu-id="75a07-181">The varying widths of the **abID** members are arbitrary; they illustrate the fact that the size of this member can vary.</span></span>

![illustrazione schematica di un PIDL](images/shell2.png)

### <a name="pidls"></a><span data-ttu-id="75a07-183">PIDL</span><span class="sxs-lookup"><span data-stu-id="75a07-183">PIDLs</span></span>

<span data-ttu-id="75a07-184">Per l'API shell, gli oggetti dello spazio dei nomi vengono in genere identificati da un puntatore alla relativa struttura [**ItemId**](/windows/desktop/api/Shtypes/ns-shtypes-itemidlist) o da un puntatore a un elenco di identificatori di elemento (PIDL).</span><span class="sxs-lookup"><span data-stu-id="75a07-184">For the Shell API, namespace objects are usually identified by a pointer to their [**ITEMIDLIST**](/windows/desktop/api/Shtypes/ns-shtypes-itemidlist) structure, or pointer to an item identifier list (PIDL).</span></span> <span data-ttu-id="75a07-185">Per praticità, il termine PIDL si riferisce in genere a questa documentazione alla struttura stessa anziché al puntatore.</span><span class="sxs-lookup"><span data-stu-id="75a07-185">For convenience, the term PIDL will generally refer in this documentation to the structure itself rather than the pointer to it.</span></span>

<span data-ttu-id="75a07-186">Il PIDL illustrato nella figura precedente viene definito PIDL *completo* o *assoluto*.</span><span class="sxs-lookup"><span data-stu-id="75a07-186">The PIDL shown in the preceding illustration is referred to as a *full*, or *absolute*, PIDL.</span></span> <span data-ttu-id="75a07-187">Un PIDL completo inizia dal desktop e contiene gli ID elemento di tutte le cartelle intermedie nel percorso.</span><span class="sxs-lookup"><span data-stu-id="75a07-187">A full PIDL starts from the desktop, and contains the item IDs of all intermediate folders in the path.</span></span> <span data-ttu-id="75a07-188">Termina con l'ID elemento dell'oggetto seguito da un **valore null** a due byte di terminazione.</span><span class="sxs-lookup"><span data-stu-id="75a07-188">It ends with the object's item ID followed by a terminating two-byte **NULL**.</span></span> <span data-ttu-id="75a07-189">Un PIDL completo è simile a un percorso completo e identifica in modo univoco l'oggetto nello spazio dei nomi della shell.</span><span class="sxs-lookup"><span data-stu-id="75a07-189">A full PIDL is similar to a fully qualified path and uniquely identifies the object in the Shell namespace.</span></span>

<span data-ttu-id="75a07-190">I PIDL completi vengono usati raramente.</span><span class="sxs-lookup"><span data-stu-id="75a07-190">Full PIDLs are used infrequently.</span></span> <span data-ttu-id="75a07-191">Molti metodi e funzioni prevedono un *PIDL relativo*.</span><span class="sxs-lookup"><span data-stu-id="75a07-191">Many functions and methods expect a *relative PIDL*.</span></span> <span data-ttu-id="75a07-192">La radice di un PIDL relativo è una cartella, non il desktop.</span><span class="sxs-lookup"><span data-stu-id="75a07-192">The root of a relative PIDL is a folder, not the desktop.</span></span> <span data-ttu-id="75a07-193">Come per i percorsi relativi, la serie di ID elemento che compongono la struttura definiscono un percorso nello spazio dei nomi tra due oggetti.</span><span class="sxs-lookup"><span data-stu-id="75a07-193">As with relative paths, the series of item IDs that make up the structure define a path in the namespace between two objects.</span></span> <span data-ttu-id="75a07-194">Anche se non identificano in modo univoco l'oggetto, sono in genere inferiori a un PIDL completo e sono sufficienti per molti scopi.</span><span class="sxs-lookup"><span data-stu-id="75a07-194">Although they do not uniquely identify the object, they are usually smaller than a full PIDL and sufficient for many purposes.</span></span>

<span data-ttu-id="75a07-195">I PIDL relativi più comunemente utilizzati, *PIDL a livello singolo*, sono relativi alla cartella padre dell'oggetto.</span><span class="sxs-lookup"><span data-stu-id="75a07-195">The most commonly used relative PIDLs, *single-level PIDLs*, are relative to the object's parent folder.</span></span> <span data-ttu-id="75a07-196">Contengono solo l'ID elemento dell'oggetto e un **valore null** di terminazione.</span><span class="sxs-lookup"><span data-stu-id="75a07-196">They contain only the object's item ID and a terminating **NULL**.</span></span> <span data-ttu-id="75a07-197">I PIDL a più livelli vengono usati anche per molti scopi.</span><span class="sxs-lookup"><span data-stu-id="75a07-197">Multi-level PIDLs are also used for many purposes.</span></span> <span data-ttu-id="75a07-198">Contengono due o più ID elemento e in genere definiscono un percorso da una cartella padre a un oggetto tramite una serie di una o più sottocartelle.</span><span class="sxs-lookup"><span data-stu-id="75a07-198">They contain two or more item IDs and typically define a path from a parent folder to an object through a series of one or more subfolders.</span></span> <span data-ttu-id="75a07-199">Si noti che un PIDL a un solo livello può essere ancora un PIDL completo.</span><span class="sxs-lookup"><span data-stu-id="75a07-199">Note that a single-level PIDL can still be a fully qualified PIDL.</span></span> <span data-ttu-id="75a07-200">In particolare, gli oggetti desktop sono elementi figlio del desktop, quindi i PIDL completi contengono un solo ID elemento.</span><span class="sxs-lookup"><span data-stu-id="75a07-200">In particular, desktop objects are children of the desktop, so their fully qualified PIDLs contain only one item ID.</span></span>

<span data-ttu-id="75a07-201">Come illustrato in [ottenere l'ID di una cartella](folder-id.md), l'API shell offre diversi modi per recuperare il PIDL di un oggetto.</span><span class="sxs-lookup"><span data-stu-id="75a07-201">As discussed in [Getting a Folder's ID](folder-id.md), the Shell API provides a number of ways to retrieve an object's PIDL.</span></span> <span data-ttu-id="75a07-202">Una volta creato, in genere è sufficiente usarlo per identificare l'oggetto quando si chiamano altre funzioni e metodi dell'API shell.</span><span class="sxs-lookup"><span data-stu-id="75a07-202">Once you have it, you commonly just use it to identify the object when you call other Shell API functions and methods.</span></span> <span data-ttu-id="75a07-203">In questo contesto, il contenuto interno di PIDL è opaco e irrilevante.</span><span class="sxs-lookup"><span data-stu-id="75a07-203">In this context, a PIDL's internal contents are opaque and irrelevant.</span></span> <span data-ttu-id="75a07-204">Ai fini di questa discussione, si può pensare a PIDL come token che rappresentano oggetti specifici dello spazio dei nomi e concentrarsi su come usarli per le attività comuni.</span><span class="sxs-lookup"><span data-stu-id="75a07-204">For the purposes of this discussion, think of PIDLs as tokens that represent particular namespace objects, and focus on how to use them for common tasks.</span></span>

### <a name="allocating-pidls"></a><span data-ttu-id="75a07-205">Allocazione di PIDL</span><span class="sxs-lookup"><span data-stu-id="75a07-205">Allocating PIDLs</span></span>

<span data-ttu-id="75a07-206">Sebbene PIDL abbia una certa somiglianza con i percorsi, l'uso di tali percorsi richiede un approccio leggermente diverso.</span><span class="sxs-lookup"><span data-stu-id="75a07-206">Although PIDLs have some similarity to paths, using them requires a somewhat different approach.</span></span> <span data-ttu-id="75a07-207">La differenza principale è la modalità di allocazione e deallocazione della memoria.</span><span class="sxs-lookup"><span data-stu-id="75a07-207">The primary difference is in how to allocate and deallocate memory for them.</span></span>

<span data-ttu-id="75a07-208">Analogamente alla stringa utilizzata per un percorso, è necessario allocare memoria per un PIDL.</span><span class="sxs-lookup"><span data-stu-id="75a07-208">Like the string used for a path, memory must be allocated for a PIDL.</span></span> <span data-ttu-id="75a07-209">Se un'applicazione crea un PIDL, deve allocare memoria sufficiente per la struttura [**ItemId**](/windows/desktop/api/Shtypes/ns-shtypes-itemidlist) .</span><span class="sxs-lookup"><span data-stu-id="75a07-209">If an application creates a PIDL, it must allocate sufficient memory for the [**ITEMIDLIST**](/windows/desktop/api/Shtypes/ns-shtypes-itemidlist) structure.</span></span> <span data-ttu-id="75a07-210">Per la maggior parte dei casi illustrati in questo articolo, la shell crea il PIDL e gestisce l'allocazione della memoria.</span><span class="sxs-lookup"><span data-stu-id="75a07-210">For most of the cases discussed here, the Shell creates the PIDL and handles memory allocation.</span></span> <span data-ttu-id="75a07-211">Indipendentemente da ciò che ha allocato il PIDL, l'applicazione è in genere responsabile della deallocazione di PIDL quando non è più necessaria.</span><span class="sxs-lookup"><span data-stu-id="75a07-211">Regardless of what allocated the PIDL, the application is usually responsible for deallocating the PIDL when it is no longer needed.</span></span>

<span data-ttu-id="75a07-212">Usare la funzione [**CoTaskMemAlloc**](/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemalloc) per allocare PIDL e la funzione [**CoTaskMemFree**](/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemfree) per deallocarla.</span><span class="sxs-lookup"><span data-stu-id="75a07-212">Use the [**CoTaskMemAlloc**](/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemalloc) function to allocate the PIDL, and the [**CoTaskMemFree**](/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemfree) function to deallocate it.</span></span>

 

 
