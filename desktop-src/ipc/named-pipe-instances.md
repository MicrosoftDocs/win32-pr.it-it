---
description: Strategie per la comunicazione con più client pipe e la manutenzione di più istanze di pipe.
ms.assetid: c764bde5-e053-4124-b859-1d2839ada918
title: Istanze named pipe
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: b4b7d1ec591996e83853ae6faede899ecd4e4ea2
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/08/2021
ms.locfileid: "106315599"
---
# <a name="named-pipe-instances"></a><span data-ttu-id="c9816-103">Istanze named pipe</span><span class="sxs-lookup"><span data-stu-id="c9816-103">Named Pipe Instances</span></span>

<span data-ttu-id="c9816-104">Il server pipe più semplice crea una singola istanza di una pipe, si connette a un singolo client, comunica con il client, si disconnette dal client, chiude l'handle della pipe e termina.</span><span class="sxs-lookup"><span data-stu-id="c9816-104">The simplest pipe server creates a single instance of a pipe, connects to a single client, communicates with the client, disconnects from the client, closes the pipe handle, and terminates.</span></span> <span data-ttu-id="c9816-105">Tuttavia, è più comune che un server pipe comunichi con più client pipe.</span><span class="sxs-lookup"><span data-stu-id="c9816-105">However, it is more common for a pipe server to communicate with multiple pipe clients.</span></span> <span data-ttu-id="c9816-106">Un server pipe può utilizzare una singola istanza di pipe per connettersi a più client pipe connettendosi a e disconnettendosi da ogni client in sequenza, ma le prestazioni potrebbero essere insufficienti.</span><span class="sxs-lookup"><span data-stu-id="c9816-106">A pipe server could use a single pipe instance to connect with multiple pipe clients by connecting to and disconnecting from each client in sequence, but performance would be poor.</span></span> <span data-ttu-id="c9816-107">Il server pipe deve creare più istanze di pipe per gestire in modo efficiente più client simultaneamente.</span><span class="sxs-lookup"><span data-stu-id="c9816-107">The pipe server must create multiple pipe instances to efficiently handle multiple clients simultaneously.</span></span>

<span data-ttu-id="c9816-108">Sono disponibili tre strategie di base per la manutenzione di più istanze di pipe.</span><span class="sxs-lookup"><span data-stu-id="c9816-108">There are three basic strategies for servicing multiple pipe instances.</span></span>

-   <span data-ttu-id="c9816-109">Creare un thread separato per ogni istanza della pipe.</span><span class="sxs-lookup"><span data-stu-id="c9816-109">Create a separate thread for each instance of the pipe.</span></span> <span data-ttu-id="c9816-110">Per un esempio di server pipe multithreading, vedere [server pipe multithreading](multithreaded-pipe-server.md).</span><span class="sxs-lookup"><span data-stu-id="c9816-110">For an example of a multithreaded pipe server, see [Multithreaded Pipe Server](multithreaded-pipe-server.md).</span></span>
-   <span data-ttu-id="c9816-111">Utilizzare operazioni sovrapposte specificando una struttura [**sovrapposta**](/windows/desktop/api/minwinbase/ns-minwinbase-overlapped) nelle funzioni [**ReadFile**](/windows/desktop/api/fileapi/nf-fileapi-readfile), [**WriteFile**](/windows/desktop/api/fileapi/nf-fileapi-writefile)e [**ConnectNamedPipe**](/windows/win32/api/namedpipeapi/nf-namedpipeapi-connectnamedpipe) .</span><span class="sxs-lookup"><span data-stu-id="c9816-111">Use overlapped operations by specifying an [**OVERLAPPED**](/windows/desktop/api/minwinbase/ns-minwinbase-overlapped) structure in the [**ReadFile**](/windows/desktop/api/fileapi/nf-fileapi-readfile), [**WriteFile**](/windows/desktop/api/fileapi/nf-fileapi-writefile), and [**ConnectNamedPipe**](/windows/win32/api/namedpipeapi/nf-namedpipeapi-connectnamedpipe) functions.</span></span> <span data-ttu-id="c9816-112">Per un esempio, vedere [named pipe server con I/O sovrapposti](named-pipe-server-using-overlapped-i-o.md).</span><span class="sxs-lookup"><span data-stu-id="c9816-112">For an example, see [Named Pipe Server Using Overlapped I/O](named-pipe-server-using-overlapped-i-o.md).</span></span>
-   <span data-ttu-id="c9816-113">Usare le operazioni sovrapposte usando le funzioni [**ReadFileEx**](/windows/desktop/api/fileapi/nf-fileapi-readfileex) e [**WriteFileEx**](/windows/desktop/api/fileapi/nf-fileapi-writefileex) , che specificano una routine di completamento da eseguire al termine dell'operazione.</span><span class="sxs-lookup"><span data-stu-id="c9816-113">Use overlapped operations by using the [**ReadFileEx**](/windows/desktop/api/fileapi/nf-fileapi-readfileex) and [**WriteFileEx**](/windows/desktop/api/fileapi/nf-fileapi-writefileex) functions, which specify a completion routine to be executed when the operation is completed.</span></span> <span data-ttu-id="c9816-114">Per un esempio, vedere [named pipe server usando le routine di completamento](named-pipe-server-using-completion-routines.md).</span><span class="sxs-lookup"><span data-stu-id="c9816-114">For an example, see [Named Pipe Server Using Completion Routines](named-pipe-server-using-completion-routines.md).</span></span>

<span data-ttu-id="c9816-115">Il server pipe multithreading è più facile da scrivere, perché il thread per ogni istanza gestisce le comunicazioni per un singolo client pipe.</span><span class="sxs-lookup"><span data-stu-id="c9816-115">The multithreaded pipe server is easiest to write, because the thread for each instance handles communications for a single pipe client.</span></span> <span data-ttu-id="c9816-116">Il sistema alloca il tempo del processore a ogni thread in base alle esigenze.</span><span class="sxs-lookup"><span data-stu-id="c9816-116">The system allocates processor time to each thread as needed.</span></span> <span data-ttu-id="c9816-117">Tuttavia, ogni thread utilizza risorse di sistema, uno svantaggio per un server pipe che gestisce un numero elevato di client.</span><span class="sxs-lookup"><span data-stu-id="c9816-117">But each thread uses system resources, which is a disadvantage for a pipe server that handles a large number of clients.</span></span>

<span data-ttu-id="c9816-118">Con un server a thread singolo, è più semplice coordinare le operazioni che interessano più client ed è più facile proteggere le risorse condivise dall'accesso simultaneo da parte di più client.</span><span class="sxs-lookup"><span data-stu-id="c9816-118">With a single-threaded server, it is easier to coordinate operations that affect multiple clients, and it is easier to protect shared resources from simultaneous access by multiple clients.</span></span> <span data-ttu-id="c9816-119">La sfida di un server a thread singolo è la necessità di coordinare le operazioni sovrapposte per allocazione del tempo del processore per la gestione delle esigenze simultanee dei client.</span><span class="sxs-lookup"><span data-stu-id="c9816-119">The challenge of a single-threaded server is that it requires coordination of overlapped operations to allocate processor time for handling the simultaneous needs of clients.</span></span>

 

 
