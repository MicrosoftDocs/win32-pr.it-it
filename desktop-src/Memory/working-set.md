---
description: Il working set di un processo è il set di pagine nello spazio degli indirizzi virtuali del processo che attualmente risiede nella memoria fisica.
ms.assetid: ff05276a-1d40-4844-b649-10e32e3f1937
title: Working Set
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: f54ed26e9809ebffd01edb30f48f36d398689e88
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/08/2021
ms.locfileid: "106313821"
---
# <a name="working-set"></a><span data-ttu-id="6b027-103">Working Set</span><span class="sxs-lookup"><span data-stu-id="6b027-103">Working Set</span></span>

<span data-ttu-id="6b027-104">Il working set di un processo è il set di pagine nello spazio degli indirizzi virtuali del processo che attualmente risiede nella memoria fisica.</span><span class="sxs-lookup"><span data-stu-id="6b027-104">The working set of a process is the set of pages in the virtual address space of the process that are currently resident in physical memory.</span></span> <span data-ttu-id="6b027-105">Il working set contiene solo allocazioni di memoria paginabili; le allocazioni di memoria non paginabili come AWE ( [Address Windowing Extensions](address-windowing-extensions.md) ) o le [allocazioni di pagine di grandi dimensioni](large-page-support.md) non sono incluse nel working set.</span><span class="sxs-lookup"><span data-stu-id="6b027-105">The working set contains only pageable memory allocations; nonpageable memory allocations such as [Address Windowing Extensions](address-windowing-extensions.md) (AWE) or [large page allocations](large-page-support.md) are not included in the working set.</span></span>

<span data-ttu-id="6b027-106">Quando un processo fa riferimento a una memoria paginabile che non si trova attualmente nella working set, si verifica un *errore di pagina* .</span><span class="sxs-lookup"><span data-stu-id="6b027-106">When a process references pageable memory that is not currently in its working set, a *page fault* occurs.</span></span> <span data-ttu-id="6b027-107">Il gestore errori di pagina sistema tenta di risolvere l'errore di pagina e, in caso di esito positivo, la pagina viene aggiunta al working set.</span><span class="sxs-lookup"><span data-stu-id="6b027-107">The system page fault handler attempts to resolve the page fault and, if it succeeds, the page is added to the working set.</span></span> <span data-ttu-id="6b027-108">L'accesso a AWE o alle allocazioni di pagine di grandi dimensioni non provoca mai un errore di pagina, perché queste allocazioni non sono paginabili.</span><span class="sxs-lookup"><span data-stu-id="6b027-108">(Accessing AWE or large page allocations never causes a page fault, because these allocations are not pageable .)</span></span>

<span data-ttu-id="6b027-109">È necessario risolvere un *errore di pagina hardware* leggendo il contenuto della pagina dall' *Archivio di backup* della pagina, ovvero il file di paging di sistema o un file mappato alla memoria creato dal processo.</span><span class="sxs-lookup"><span data-stu-id="6b027-109">A *hard page fault* must be resolved by reading page contents from the page's *backing store*, which is either the system paging file or a memory-mapped file created by the process.</span></span> <span data-ttu-id="6b027-110">È possibile risolvere un *errore di pagina software* senza accedere all'archivio di backup.</span><span class="sxs-lookup"><span data-stu-id="6b027-110">A *soft page fault* can be resolved without accessing the backing store.</span></span> <span data-ttu-id="6b027-111">Si verifica un errore di pagina software quando:</span><span class="sxs-lookup"><span data-stu-id="6b027-111">A soft page fault occurs when:</span></span>

-   <span data-ttu-id="6b027-112">La pagina si trova nel working set di un altro processo, quindi è già residente in memoria.</span><span class="sxs-lookup"><span data-stu-id="6b027-112">The page is in the working set of some other process, so it is already resident in memory.</span></span>
-   <span data-ttu-id="6b027-113">La pagina è in fase di transizione, perché è stata rimossa dal working set di tutti i processi che stavano usando la pagina e non è ancora stata reimpiegata oppure è già residente in seguito a un'operazione di prelettura di gestione memoria.</span><span class="sxs-lookup"><span data-stu-id="6b027-113">The page is in transition, because it either has been removed from the working sets of all processes that were using the page and has not yet been repurposed, or it is already resident as a result of a memory manager prefetch operation.</span></span>
-   <span data-ttu-id="6b027-114">Un processo fa riferimento a una pagina virtuale allocata per la prima volta (talvolta denominato *errore di richiesta-zero*).</span><span class="sxs-lookup"><span data-stu-id="6b027-114">A process references an allocated virtual page for the first time (sometimes called a *demand-zero fault*).</span></span>

<span data-ttu-id="6b027-115">Le pagine possono essere rimosse da un processo working set come risultato delle azioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="6b027-115">Pages can be removed from a process working set as a result of the following actions:</span></span>

-   <span data-ttu-id="6b027-116">Il processo riduce o svuota il working set chiamando la funzione [**SetProcessWorkingSetSize**](/windows/win32/api/winbase/nf-winbase-setprocessworkingsetsize), [**SetProcessWorkingSetSizeEx**](/windows/win32/api/memoryapi/nf-memoryapi-setprocessworkingsetsizeex) o [**EmptyWorkingSet**](/windows/win32/api/psapi/nf-psapi-emptyworkingset) .</span><span class="sxs-lookup"><span data-stu-id="6b027-116">The process reduces or empties the working set by calling the [**SetProcessWorkingSetSize**](/windows/win32/api/winbase/nf-winbase-setprocessworkingsetsize), [**SetProcessWorkingSetSizeEx**](/windows/win32/api/memoryapi/nf-memoryapi-setprocessworkingsetsizeex) or [**EmptyWorkingSet**](/windows/win32/api/psapi/nf-psapi-emptyworkingset) function.</span></span>
-   <span data-ttu-id="6b027-117">Il processo chiama la funzione [**VirtualUnlock**](/windows/win32/api/memoryapi/nf-memoryapi-virtualunlock) su un intervallo di memoria non bloccato.</span><span class="sxs-lookup"><span data-stu-id="6b027-117">The process calls the [**VirtualUnlock**](/windows/win32/api/memoryapi/nf-memoryapi-virtualunlock) function on a memory range that is not locked.</span></span>
-   <span data-ttu-id="6b027-118">Il processo esegue l'unmapping di una visualizzazione mappata di un file tramite la funzione [**UnmapViewOfFile**](/windows/win32/api/memoryapi/nf-memoryapi-unmapviewoffile) .</span><span class="sxs-lookup"><span data-stu-id="6b027-118">The process unmaps a mapped view of a file using the [**UnmapViewOfFile**](/windows/win32/api/memoryapi/nf-memoryapi-unmapviewoffile) function.</span></span>
-   <span data-ttu-id="6b027-119">Il gestore della memoria taglia le pagine dalla working set per creare una maggiore quantità di memoria disponibile.</span><span class="sxs-lookup"><span data-stu-id="6b027-119">The memory manager trims pages from the working set to create more available memory.</span></span>
-   <span data-ttu-id="6b027-120">Il gestore della memoria deve rimuovere una pagina dal working set per creare spazio per una nuova pagina, ad esempio perché il working set è alla dimensione massima.</span><span class="sxs-lookup"><span data-stu-id="6b027-120">The memory manager must remove a page from the working set to make room for a new page (for example, because the working set is at its maximum size).</span></span>

<span data-ttu-id="6b027-121">Se più processi condividono una pagina, la rimozione della pagina dalla working set di un processo non influisce su altri processi.</span><span class="sxs-lookup"><span data-stu-id="6b027-121">If several processes share a page, removing the page from the working set of one process does not affect other processes.</span></span> <span data-ttu-id="6b027-122">Quando una pagina viene rimossa dai working set di tutti i processi che lo stavano utilizzando, la pagina diventa una *pagina di transizione*.</span><span class="sxs-lookup"><span data-stu-id="6b027-122">After a page is removed from the working sets of all processes that were using it, the page becomes a *transition page*.</span></span> <span data-ttu-id="6b027-123">Le pagine di transizione rimangono memorizzate nella cache fino a quando non viene fatto nuovamente riferimento a una pagina da parte di un processo o riproposta, ad esempio riempita con zeri e assegnata a un altro processo.</span><span class="sxs-lookup"><span data-stu-id="6b027-123">Transition pages remain cached in RAM until the page is either referenced again by some process or repurposed (for example, filled with zeros and given to another process).</span></span> <span data-ttu-id="6b027-124">Se una pagina di transizione è stata modificata dopo l'ultima scrittura su disco (ovvero, se la pagina è "Dirty"), la pagina deve essere scritta nell'archivio di backup prima di poterla reimpiegare.</span><span class="sxs-lookup"><span data-stu-id="6b027-124">If a transition page has been modified since it was last written to disk (that is, if the page is "dirty"), then the page must be written to its backing store before it can be repurposed.</span></span> <span data-ttu-id="6b027-125">Il sistema può iniziare a scrivere pagine di transizione Dirty nell'archivio di backup non appena tali pagine diventano disponibili.</span><span class="sxs-lookup"><span data-stu-id="6b027-125">The system may start writing dirty transition pages to their backing store as soon as such pages become available.</span></span>

<span data-ttu-id="6b027-126">Ogni processo ha dimensioni minime e massime working set che influiscono sul comportamento di paging della memoria virtuale del processo.</span><span class="sxs-lookup"><span data-stu-id="6b027-126">Each process has a minimum and maximum working set size that affect the virtual memory paging behavior of the process.</span></span> <span data-ttu-id="6b027-127">Per ottenere le dimensioni correnti del working set di un processo specificato, utilizzare la funzione [**GetProcessMemoryInfo**](/windows/win32/api/psapi/nf-psapi-getprocessmemoryinfo) .</span><span class="sxs-lookup"><span data-stu-id="6b027-127">To obtain the current size of the working set of a specified process, use the [**GetProcessMemoryInfo**](/windows/win32/api/psapi/nf-psapi-getprocessmemoryinfo) function.</span></span> <span data-ttu-id="6b027-128">Per ottenere o modificare le dimensioni working set minime e massime, usare le funzioni [**GetProcessWorkingSetSizeEx**](/windows/win32/api/memoryapi/nf-memoryapi-getprocessworkingsetsizeex) e [**SetProcessWorkingSetSizeEx**](/windows/win32/api/memoryapi/nf-memoryapi-setprocessworkingsetsizeex) .</span><span class="sxs-lookup"><span data-stu-id="6b027-128">To obtain or change the minimum and maximum working set sizes, use the [**GetProcessWorkingSetSizeEx**](/windows/win32/api/memoryapi/nf-memoryapi-getprocessworkingsetsizeex) and [**SetProcessWorkingSetSizeEx**](/windows/win32/api/memoryapi/nf-memoryapi-setprocessworkingsetsizeex) functions.</span></span>

<span data-ttu-id="6b027-129">Il Application Programming Interface di stato del processo (PSAPI) fornisce una serie di funzioni che restituiscono informazioni dettagliate sul working set di un processo.</span><span class="sxs-lookup"><span data-stu-id="6b027-129">The process status application programming interface (PSAPI) provides a number of functions that return detailed information about the working set of a process.</span></span> <span data-ttu-id="6b027-130">Per informazioni dettagliate, vedere [working set information](../psapi/working-set-information.md).</span><span class="sxs-lookup"><span data-stu-id="6b027-130">For details, see [Working Set Information](../psapi/working-set-information.md).</span></span>

 

 
