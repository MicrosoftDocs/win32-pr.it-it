---
description: Sessioni audio
ms.assetid: b8a1b656-a582-4112-99e9-bd575719ebb3
title: Sessioni audio
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 57ae67a3eafe7a76add2fad192823868304e860d
ms.sourcegitcommit: c7add10d695482e1ceb72d62b8a4ebd84ea050f7
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/07/2021
ms.locfileid: "104127674"
---
# <a name="audio-sessions"></a><span data-ttu-id="1f945-103">Sessioni audio</span><span class="sxs-lookup"><span data-stu-id="1f945-103">Audio Sessions</span></span>

<span data-ttu-id="1f945-104">Una *sessione audio* è un gruppo di flussi audio correlati che un client WASAPI può gestire collettivamente.</span><span class="sxs-lookup"><span data-stu-id="1f945-104">An *audio session* is a group of related audio streams that a WASAPI client can manage collectively.</span></span> <span data-ttu-id="1f945-105">I client possono controllare il livello di volume e lo stato di muting di ogni singola sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-105">Clients can control the volume level and muting state of each individual session.</span></span> <span data-ttu-id="1f945-106">Il sistema applica in modo uniforme le impostazioni relative al volume e al silenziamento specificate dal client a tutti i flussi della sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-106">The system applies client-specified volume and mute settings uniformly to all of the streams in the session.</span></span>

<span data-ttu-id="1f945-107">Quando un client Inizializza un flusso audio, assegna il flusso audio a una sessione audio.</span><span class="sxs-lookup"><span data-stu-id="1f945-107">When a client initializes an audio stream, it assigns the audio stream to an audio session.</span></span> <span data-ttu-id="1f945-108">Per altre informazioni, vedere [**IAudioClient:: Initialize**](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-initialize).</span><span class="sxs-lookup"><span data-stu-id="1f945-108">For more information, see [**IAudioClient::Initialize**](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-initialize).</span></span>

<span data-ttu-id="1f945-109">Una sessione audio contiene flussi di rendering o flussi di acquisizione, ma non entrambi.</span><span class="sxs-lookup"><span data-stu-id="1f945-109">An audio session contains either rendering streams or capture streams, but not both.</span></span> <span data-ttu-id="1f945-110">Per impostazione predefinita, le impostazioni volume e mute per una sessione di rendering sono persistenti tra i riavvii del sistema.</span><span class="sxs-lookup"><span data-stu-id="1f945-110">By default, the volume and mute settings for a rendering session are persistent across system restarts.</span></span> <span data-ttu-id="1f945-111">Le impostazioni volume e mute per una sessione di acquisizione non sono permanenti.</span><span class="sxs-lookup"><span data-stu-id="1f945-111">The volume and mute settings for a capture session are not persistent.</span></span> <span data-ttu-id="1f945-112">Una sessione che contiene flussi che operano in modalità loopback viene gestita come una sessione di acquisizione.</span><span class="sxs-lookup"><span data-stu-id="1f945-112">(A session that contains streams that operate in loopback mode is treated the same as a capture session.</span></span> <span data-ttu-id="1f945-113">Ovvero le impostazioni della sessione non sono permanenti.</span><span class="sxs-lookup"><span data-stu-id="1f945-113">That is, the session settings are not persistent.</span></span> <span data-ttu-id="1f945-114">Per ulteriori informazioni sulla modalità loopback, vedere la pagina relativa alla [registrazione di loopback](loopback-recording.md).</span><span class="sxs-lookup"><span data-stu-id="1f945-114">For more information about loopback mode, see [Loopback Recording](loopback-recording.md).)</span></span>

<span data-ttu-id="1f945-115">Ogni flusso audio appartiene esattamente a una sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-115">Every audio stream belongs to exactly one session.</span></span> <span data-ttu-id="1f945-116">Un client assegna un flusso audio a una determinata sessione al momento dell'inizializzazione dell'oggetto flusso.</span><span class="sxs-lookup"><span data-stu-id="1f945-116">A client assigns an audio stream to a particular session at the time that it initializes the stream object.</span></span> <span data-ttu-id="1f945-117">Il flusso mantiene l'appartenenza della sessione per la durata del flusso.</span><span class="sxs-lookup"><span data-stu-id="1f945-117">The stream retains its membership in the session for the lifetime of the stream.</span></span> <span data-ttu-id="1f945-118">Dopo la creazione di un oggetto flusso, l'oggetto esiste fino a quando un client non rilascia l'ultimo riferimento conteggiato all'oggetto, quindi l'oggetto viene eliminato.</span><span class="sxs-lookup"><span data-stu-id="1f945-118">After a stream object is created, the object exists until a client releases the last counted reference to the object, and then the object is deleted.</span></span>

<span data-ttu-id="1f945-119">Anche se un client non è in grado di modificare la sessione a cui è assegnato un flusso esistente, può ottenere un effetto simile eliminando il flusso (rilasciando tutti i riferimenti a esso), creando un nuovo flusso per sostituire il flusso eliminato e assegnando il nuovo flusso a un'altra sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-119">Although a client cannot change the session to which an existing stream is assigned, it can achieve a similar effect by deleting the stream (by releasing all references to it), creating a new stream to replace the deleted stream, and assigning the new stream to another session.</span></span>

<span data-ttu-id="1f945-120">Ogni sessione di rendering rappresenta un subset dei flussi che formano la combinazione globale che riproduce un particolare [dispositivo di endpoint audio](audio-endpoint-devices.md).</span><span class="sxs-lookup"><span data-stu-id="1f945-120">Each rendering session represents a subset of the streams that form the global mix that plays through a particular [audio endpoint device](audio-endpoint-devices.md).</span></span> <span data-ttu-id="1f945-121">La combinazione globale combina tutte le sessioni di tutte le applicazioni che condividono il dispositivo.</span><span class="sxs-lookup"><span data-stu-id="1f945-121">The global mix combines all of the sessions from all of the applications that share the device.</span></span>

<span data-ttu-id="1f945-122">Spesso un'applicazione con più flussi assegna tutti i relativi flussi alla stessa sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-122">Frequently, an application with several streams assigns all of its streams to the same session.</span></span> <span data-ttu-id="1f945-123">Tuttavia, l'applicazione può scegliere di assegnare flussi diversi a sessioni diverse.</span><span class="sxs-lookup"><span data-stu-id="1f945-123">However, the application can, as an option, assign different streams to different sessions.</span></span> <span data-ttu-id="1f945-124">Qualsiasi flusso che l'applicazione non assegna in modo esplicito a una sessione appartiene alla sessione predefinita.</span><span class="sxs-lookup"><span data-stu-id="1f945-124">Any stream that the application does not explicitly assign to a session belongs to the default session.</span></span>

<span data-ttu-id="1f945-125">Le applicazioni audio tipiche devono evitare di modificare le impostazioni di volume e di silenziamento per le sessioni.</span><span class="sxs-lookup"><span data-stu-id="1f945-125">Typical audio applications should avoid modifying the volume and mute settings for sessions.</span></span> <span data-ttu-id="1f945-126">Al contrario, gli utenti controllano queste impostazioni tramite le interfacce utente dei programmi di controllo.</span><span class="sxs-lookup"><span data-stu-id="1f945-126">Instead, users control these settings through the user interfaces of control programs.</span></span> <span data-ttu-id="1f945-127">Ad esempio, in Windows Vista, il programma fornito dal sistema, Sndvol.exe, Visualizza un controllo del volume e il controllo di silenziamento per ogni sessione di rendering attiva o di recente attiva nel sistema.</span><span class="sxs-lookup"><span data-stu-id="1f945-127">For example, in Windows Vista, the system-supplied program, Sndvol.exe, displays a volume control and mute control for each active or recently active rendering session in the system.</span></span> <span data-ttu-id="1f945-128">Tramite questi controlli, gli utenti possono modificare le impostazioni del volume e del silenziamento per tutte le sessioni nel sistema.</span><span class="sxs-lookup"><span data-stu-id="1f945-128">Through these controls, users can adjust the volume and mute settings for all of the sessions in the system.</span></span>

<span data-ttu-id="1f945-129">Attualmente il programma sndvol Visualizza i controlli volume solo per i dispositivi endpoint di rendering audio.</span><span class="sxs-lookup"><span data-stu-id="1f945-129">The Sndvol program currently displays volume controls for audio-rendering endpoint devices only.</span></span> <span data-ttu-id="1f945-130">Non vengono visualizzati i controlli volume per i dispositivi di acquisizione audio.</span><span class="sxs-lookup"><span data-stu-id="1f945-130">It does not display volume controls for audio-capture devices.</span></span>

<span data-ttu-id="1f945-131">Una sessione è attiva se contiene uno o più flussi attivi.</span><span class="sxs-lookup"><span data-stu-id="1f945-131">A session is active if it contains one or more active streams.</span></span> <span data-ttu-id="1f945-132">Un flusso attivo è nello *stato in esecuzione*.</span><span class="sxs-lookup"><span data-stu-id="1f945-132">An active stream is in the *running state*.</span></span> <span data-ttu-id="1f945-133">Un flusso inattivo è nello *stato interrotto*.</span><span class="sxs-lookup"><span data-stu-id="1f945-133">An inactive stream is in the *stopped state*.</span></span> <span data-ttu-id="1f945-134">Una sessione diventa attiva quando il primo flusso diventa attivo.</span><span class="sxs-lookup"><span data-stu-id="1f945-134">A session becomes active when its first stream becomes active.</span></span> <span data-ttu-id="1f945-135">Una sessione diventa inattiva quando l'ultimo flusso attivo diventa inattivo.</span><span class="sxs-lookup"><span data-stu-id="1f945-135">A session becomes inactive when its last active stream becomes inactive.</span></span> <span data-ttu-id="1f945-136">Dopo che una sessione è rimasta inattiva per un determinato periodo di tempo, il sistema modifica lo stato della sessione da inattivo a scaduto.</span><span class="sxs-lookup"><span data-stu-id="1f945-136">After a session has been inactive for a period of time, the system changes the state of the session from inactive to expired.</span></span>

<span data-ttu-id="1f945-137">Sndvol Visualizza i controlli volume e mute per tutte le sessioni di rendering attive e inattive.</span><span class="sxs-lookup"><span data-stu-id="1f945-137">Sndvol displays volume and mute controls for all active and inactive rendering sessions.</span></span> <span data-ttu-id="1f945-138">Sndvol rimuove i controlli volume e mute per una sessione quando lo stato della sessione passa da inattivo a scaduto o quando la sessione viene terminata.</span><span class="sxs-lookup"><span data-stu-id="1f945-138">Sndvol removes the volume and mute controls for a session when the state of the session changes from inactive to expired, or when the session terminates.</span></span> <span data-ttu-id="1f945-139">Una sessione termina quando viene eliminato l'ultimo flusso, ovvero quando un client rilascia il conteggio dei riferimenti finali nell'ultimo oggetto flusso rimanente della sessione. L'unica eccezione a questa regola è rappresentata dai suoni delle notifiche di sistema.</span><span class="sxs-lookup"><span data-stu-id="1f945-139">(A session terminates when the last of its streams is deleted; that is, when a client releases the final reference count on the last remaining stream object in the session.) The one exception to this rule is for system-notification sounds.</span></span> <span data-ttu-id="1f945-140">Sndvol Visualizza sempre i controlli volume e mute per i suoni di notifica del sistema indipendentemente dallo stato della sessione per questi suoni.</span><span class="sxs-lookup"><span data-stu-id="1f945-140">Sndvol always displays the volume and mute controls for system-notification sounds regardless of the state of the session for these sounds.</span></span>

<span data-ttu-id="1f945-141">In genere, un flusso appartiene a una sessione che si estende solo al processo che contiene l'applicazione che ha creato il flusso.</span><span class="sxs-lookup"><span data-stu-id="1f945-141">Typically, a stream belongs to a session that spans only the process that contains the application that created the stream.</span></span> <span data-ttu-id="1f945-142">Tuttavia, le applicazioni hanno la possibilità di definire sessioni tra processi che combinano flussi da due o più processi.</span><span class="sxs-lookup"><span data-stu-id="1f945-142">However, applications have the option of defining cross-process sessions that combine streams from two or more processes.</span></span>

<span data-ttu-id="1f945-143">WASAPI supporta le sessioni tra processi principalmente, in modo che:</span><span class="sxs-lookup"><span data-stu-id="1f945-143">WASAPI supports cross-process sessions primarily so that:</span></span>

-   <span data-ttu-id="1f945-144">Il programma sndvol può presentare all'utente un controllo volume singolo per gestire i suoni delle notifiche di sistema in tutte le applicazioni.</span><span class="sxs-lookup"><span data-stu-id="1f945-144">The Sndvol program can present the user with a single volume control to manage system-notification sounds across all applications.</span></span>
-   <span data-ttu-id="1f945-145">Un lettore multimediale che viene eseguito in un processo può trasmettere contenuti protetti a un programma di decrittografia eseguito in un altro processo.</span><span class="sxs-lookup"><span data-stu-id="1f945-145">A media player that runs in one process can stream protected content to a decryption program that runs in another process.</span></span>

<span data-ttu-id="1f945-146">Analogamente alle impostazioni di controllo per le sessioni di rendering specifiche del processo, le impostazioni di controllo per le sessioni di rendering tra processi sono, per impostazione predefinita, permanenti tra i riavvii del sistema.</span><span class="sxs-lookup"><span data-stu-id="1f945-146">Similar to the control settings for process-specific rendering sessions, the control settings for cross-process rendering sessions are, by default, persistent across system restarts.</span></span> <span data-ttu-id="1f945-147">WASAPI fornisce questo comportamento principalmente per i vantaggi dei suoni di notifica del sistema, che devono mantenere le impostazioni di volume e di silenziamento dell'utente in quanto la combinazione di applicazioni varia nel tempo.</span><span class="sxs-lookup"><span data-stu-id="1f945-147">WASAPI provides this behavior primarily for the benefit of system-notification sounds, which must retain the user's volume and mute settings as the application mix varies over time.</span></span>

<span data-ttu-id="1f945-148">Il programma sndvol etichetta il controllo volume per ogni sessione con un nome visualizzato e un'icona.</span><span class="sxs-lookup"><span data-stu-id="1f945-148">The Sndvol program labels the volume control for each session with a display name and an icon.</span></span> <span data-ttu-id="1f945-149">Un client ha la possibilità di assegnare in modo esplicito un nome visualizzato e un'icona a una sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-149">A client has the option of explicitly assigning a display name and icon to a session.</span></span> <span data-ttu-id="1f945-150">Se il client non fornisce questi elementi, sndvol visualizzerà invece un nome predefinito e un'icona predefinita.</span><span class="sxs-lookup"><span data-stu-id="1f945-150">If the client does not provide these items, then Sndvol displays a default name and default icon instead.</span></span> <span data-ttu-id="1f945-151">Il nome predefinito incorpora informazioni come il titolo della finestra dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="1f945-151">The default name incorporates information such as the title of the application window.</span></span> <span data-ttu-id="1f945-152">L'icona predefinita è l'icona della finestra dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="1f945-152">The default icon is the icon for the application window.</span></span> <span data-ttu-id="1f945-153">Solo nel caso di sessioni specifiche del processo, queste impostazioni predefinite forniscono agli utenti informazioni significative.</span><span class="sxs-lookup"><span data-stu-id="1f945-153">Only in the case of process-specific sessions do these defaults provide meaningful information to users.</span></span> <span data-ttu-id="1f945-154">Si noti che una sessione tra processi può essere associata a più di un'applicazione.</span><span class="sxs-lookup"><span data-stu-id="1f945-154">Note that a cross-process session can be associated with more than one application.</span></span> <span data-ttu-id="1f945-155">In questo caso, solo un nome visualizzato e un'icona specificati dal client sono significativi.</span><span class="sxs-lookup"><span data-stu-id="1f945-155">In this case, only a client-provided display name and icon are meaningful.</span></span>

<span data-ttu-id="1f945-156">Sebbene le impostazioni volume e mute per una sessione di rendering siano, per impostazione predefinita, permanenti tra i riavvii del sistema, il nome visualizzato e l'icona specificati dal client non lo sono.</span><span class="sxs-lookup"><span data-stu-id="1f945-156">Although the volume and mute settings for a rendering session are, by default, persistent across system restarts, the client-provided display name and icon are not.</span></span> <span data-ttu-id="1f945-157">Per assicurarsi che sndvol visualizzi il nome e l'icona forniti dal client, il client deve assegnare in modo esplicito il nome e l'icona alla sessione nel momento in cui il client assegna il primo flusso alla sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-157">To ensure that Sndvol displays the client-provided name and icon, the client must explicitly assign the name and icon to the session at the time that the client assigns the first stream to the session.</span></span> <span data-ttu-id="1f945-158">Il sistema mantiene il nome visualizzato e l'icona di una sessione solo fino al termine della sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-158">The system retains the display name and icon for a session only until the session terminates.</span></span>

<span data-ttu-id="1f945-159">Ogni sessione è identificata da un GUID della sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-159">Each session is identified by a session GUID.</span></span> <span data-ttu-id="1f945-160">Quando un client apre un flusso, il client assegna tale flusso a una determinata sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-160">At the time that a client opens a stream, the client assigns that stream to a particular session.</span></span> <span data-ttu-id="1f945-161">Il client fornisce le seguenti due informazioni per identificare la sessione:</span><span class="sxs-lookup"><span data-stu-id="1f945-161">The client supplies the following two pieces of information to identify that session:</span></span>

-   <span data-ttu-id="1f945-162">GUID della sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-162">A session GUID.</span></span>
-   <span data-ttu-id="1f945-163">Se la sessione è una sessione specifica tra processi o processi, una sessione specifica del processo contiene solo i flussi del processo del client.</span><span class="sxs-lookup"><span data-stu-id="1f945-163">Whether the session is a cross-process or process-specific session—a process-specific session contains only streams from the client's process.</span></span>

<span data-ttu-id="1f945-164">Queste informazioni sono sufficienti per distinguere una determinata sessione da tutte le altre sessioni nello stesso computer.</span><span class="sxs-lookup"><span data-stu-id="1f945-164">This information is sufficient to distinguish a particular session from all other sessions in the same computer.</span></span> <span data-ttu-id="1f945-165">Il GUID della sessione per una sessione specifica del processo identifica in modo univoco la sessione solo nell'ambito del processo a cui appartiene la sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-165">The session GUID for a process-specific session uniquely identifies the session only within the scope of the process that owns the session.</span></span> <span data-ttu-id="1f945-166">Al contrario, il GUID della sessione per una sessione tra processi è univoco nell'ambito di tutti i processi in esecuzione nel computer.</span><span class="sxs-lookup"><span data-stu-id="1f945-166">In contrast, the session GUID for a cross-process session is unique within the scope of all of the processes that are running on the computer.</span></span>

<span data-ttu-id="1f945-167">Nel caso di una sessione specifica del processo, il sistema utilizza una combinazione di GUID di sessione e ID processo per identificare in modo univoco la sessione nell'ambito del computer.</span><span class="sxs-lookup"><span data-stu-id="1f945-167">In the case of a process-specific session, the system uses a combination of session GUID and process ID to uniquely identify the session within the scope of the computer.</span></span> <span data-ttu-id="1f945-168">Pertanto, se i client in due processi diversi assegnano i rispettivi flussi a due sessioni specifiche del processo con GUID di sessione identici, il sistema considera le sessioni come separate perché gli ID del processo sono diversi.</span><span class="sxs-lookup"><span data-stu-id="1f945-168">Thus, if clients in two different processes assign their respective streams to two process-specific sessions with identical session GUIDs, the system treats the sessions as separate because their process IDs are different.</span></span> <span data-ttu-id="1f945-169">Inoltre, se una sessione tra processi usa lo stesso GUID della sessione di una o più sessioni specifiche del processo, il sistema considera la sessione tra processi come diversa dalle sessioni specifiche del processo, anche se condividono lo stesso GUID della sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-169">In addition, if a cross-process session uses the same session GUID as one or more process-specific sessions, the system treats the cross-process session as distinct from the process-specific sessions, even though they share the same session GUID.</span></span>

<span data-ttu-id="1f945-170">Ad esempio, in Windows Vista, le API di livello superiore, come le funzioni **waveOutXxx** multimediali di Windows e DirectSound, in genere assegnano i flussi audio creati alle sessioni predefinite, specifiche del processo, identificate dal GUID del valore GUID della sessione \_ null.</span><span class="sxs-lookup"><span data-stu-id="1f945-170">For example, in Windows Vista, higher-level APIs such as the Windows multimedia **waveOutXxx** functions and DirectSound typically assign the audio streams that they create to default, process-specific sessions that are identified by the session GUID value GUID\_NULL.</span></span> <span data-ttu-id="1f945-171">Per i client di queste API, la sessione predefinita per ogni processo client è separata dalle sessioni predefinite per gli altri processi client, anche se le sessioni hanno GUID di sessione identici.</span><span class="sxs-lookup"><span data-stu-id="1f945-171">For clients of these APIs, the default session for each client process is separate from the default sessions for other client processes, even though the sessions have identical session GUIDs.</span></span> <span data-ttu-id="1f945-172">Inoltre, se una o più applicazioni assegnano flussi alla sessione tra processi identificata dal GUID del valore GUID della sessione \_ null, il sistema considera questa sessione tra processi come separata dalle sessioni predefinite specifiche del processo che condividono lo stesso GUID della sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-172">In addition, if one or more applications assign streams to the cross-process session that is identified by the session GUID value GUID\_NULL, then the system treats this cross-process session as separate from the default, process-specific sessions that share the same session GUID.</span></span> <span data-ttu-id="1f945-173">Di conseguenza, il programma sndvol Visualizza un controllo del volume separato per ogni sessione del client predefinita, specifica del processo, e visualizza un ulteriore controllo del volume per la sessione tra processi identificato dal GUID del valore GUID della sessione \_ null, se tale sessione esiste.</span><span class="sxs-lookup"><span data-stu-id="1f945-173">Accordingly, the Sndvol program displays a separate volume control for each client's default, process-specific session, and it displays an additional volume control for the cross-process session that is identified by the session GUID value GUID\_NULL, if that session exists.</span></span>

<span data-ttu-id="1f945-174">Ogni sessione è associata a un solo dispositivo endpoint audio.</span><span class="sxs-lookup"><span data-stu-id="1f945-174">Each session is associated with only one audio endpoint device.</span></span> <span data-ttu-id="1f945-175">Se due sessioni hanno GUID di sessione e ID processo identici ma sono associati a dispositivi diversi, il sistema considera le due sessioni come separate.</span><span class="sxs-lookup"><span data-stu-id="1f945-175">If two sessions have identical session GUIDs and process IDs but are associated with different devices, then the system treats the two sessions as separate.</span></span> <span data-ttu-id="1f945-176">Una sessione non può mai contenere entrambi i flussi di acquisizione e rendering perché un flusso di acquisizione può essere associato solo a un dispositivo di acquisizione e un flusso di rendering può essere associato solo a un dispositivo di rendering.</span><span class="sxs-lookup"><span data-stu-id="1f945-176">A session can never contain both capture and rendering streams because a capture stream can be associated only with a capture device and a rendering stream can be associated only with a rendering device.</span></span>

<span data-ttu-id="1f945-177">Come indicato in precedenza, le impostazioni volume e mute per una sessione sono persistenti tra i riavvii del sistema.</span><span class="sxs-lookup"><span data-stu-id="1f945-177">As mentioned previously, the volume and mute settings for a session are persistent across system restarts.</span></span> <span data-ttu-id="1f945-178">Due o più istanze di un'applicazione possono creare sessioni specifiche del processo con GUID di sessione identici.</span><span class="sxs-lookup"><span data-stu-id="1f945-178">Two or more instances of an application can create process-specific sessions with identical session GUIDs.</span></span> <span data-ttu-id="1f945-179">Tramite il programma sndvol, l'utente può selezionare impostazioni diverse per volume e mute per ognuna di queste sessioni.</span><span class="sxs-lookup"><span data-stu-id="1f945-179">Through the Sndvol program, the user can select different volume and mute settings for each of these sessions.</span></span> <span data-ttu-id="1f945-180">Al termine di queste sessioni, il sistema mantiene le impostazioni di controllo di una sola di queste sessioni, ovvero l'ultima sessione da terminare.</span><span class="sxs-lookup"><span data-stu-id="1f945-180">After these sessions terminate, the system retains the control settings of only one of these sessions—the last session to terminate.</span></span> <span data-ttu-id="1f945-181">In seguito, se una nuova istanza dell'applicazione crea una sessione specifica del processo con lo stesso GUID della sessione precedente, la sessione eredita le impostazioni del volume e del silenziamento precedentemente salvate.</span><span class="sxs-lookup"><span data-stu-id="1f945-181">Later, if a new instance of the application creates a process-specific session with the same session GUID as before, that session inherits the previously saved volume and mute settings.</span></span>

<span data-ttu-id="1f945-182">Un'applicazione non deve tentare di aggiungere un flusso o controllare il livello del volume di una sessione di proprietà di un'altra applicazione non correlata.</span><span class="sxs-lookup"><span data-stu-id="1f945-182">An application should not attempt to add a stream to or control the volume level of a session that is owned by another, unrelated application.</span></span> <span data-ttu-id="1f945-183">Inoltre, un'applicazione non deve tentare di controllare il livello di volume della sessione gestita dal sistema per i suoni di notifica.</span><span class="sxs-lookup"><span data-stu-id="1f945-183">In addition, an application should not attempt to control the volume level of the system-managed session for notification sounds.</span></span> <span data-ttu-id="1f945-184">Tuttavia, un'applicazione può riprodurre un suono attraverso la sessione di sistema per i suoni di notifica chiamando la funzione **PlaySound** .</span><span class="sxs-lookup"><span data-stu-id="1f945-184">However, an application can play a sound through the system session for notification sounds by calling the **PlaySound** function.</span></span> <span data-ttu-id="1f945-185">Per ulteriori informazioni, vedere [la pagina relativa ai suoni di notifica per le applicazioni audio legacy](notification-sounds-for-legacy-audio-applications.md).</span><span class="sxs-lookup"><span data-stu-id="1f945-185">For more information, see [Notification Sounds for Legacy Audio Applications](notification-sounds-for-legacy-audio-applications.md).</span></span>

<span data-ttu-id="1f945-186">Un'applicazione può registrarsi per ricevere notifiche quando cambia lo stato di una sessione.</span><span class="sxs-lookup"><span data-stu-id="1f945-186">An application can register to receive notifications when the state of a session changes.</span></span> <span data-ttu-id="1f945-187">Per ulteriori informazioni, vedere [eventi di sessione audio](audio-session-events.md).</span><span class="sxs-lookup"><span data-stu-id="1f945-187">For more information, see [Audio Session Events](audio-session-events.md).</span></span>

<span data-ttu-id="1f945-188">In rari casi, un'applicazione che crea una sessione specifica del processo potrebbe dover consolidare il controllo delle sessioni specifiche del processo per due o più istanze dell'applicazione in un unico controllo volume in sndvol.</span><span class="sxs-lookup"><span data-stu-id="1f945-188">In rare cases, an application that creates a process-specific session might need to consolidate the control of the process-specific sessions for two or more application instances under a single volume control in Sndvol.</span></span> <span data-ttu-id="1f945-189">Per ulteriori informazioni, vedere [parametri di raggruppamento](grouping-parameters.md).</span><span class="sxs-lookup"><span data-stu-id="1f945-189">For more information, see [Grouping Parameters](grouping-parameters.md).</span></span>

## <a name="related-topics"></a><span data-ttu-id="1f945-190">Argomenti correlati</span><span class="sxs-lookup"><span data-stu-id="1f945-190">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="1f945-191">Guida per programmatori</span><span class="sxs-lookup"><span data-stu-id="1f945-191">Programming Guide</span></span>](programming-guide.md)
</dt> </dl>

 

 



