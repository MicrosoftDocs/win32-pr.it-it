---
description: Se i dati sono coerenti, i dati nel server e in tutti i client vengono sincronizzati. Un tipo di sistema software che fornisce dati coerenza è un sistema di controllo delle revisioni (RCS).
ms.assetid: cd33d20e-bf25-4a50-9b20-344495554434
title: Coerenza dati
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: f300296ff0fdc807beb95e2c03662814957bedb7
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/08/2021
ms.locfileid: "103884342"
---
# <a name="data-coherency"></a><span data-ttu-id="0ddcd-104">Coerenza dati</span><span class="sxs-lookup"><span data-stu-id="0ddcd-104">Data Coherency</span></span>

<span data-ttu-id="0ddcd-105">I dati *coerenti* sono dati identici in tutta la rete.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-105">Data that is *coherent* is data that is the same across the network.</span></span> <span data-ttu-id="0ddcd-106">In altre parole, se i dati sono coerenti, i dati nel server e in tutti i client vengono sincronizzati.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-106">In other words, if data is coherent, data on the server and all the clients is synchronized.</span></span> <span data-ttu-id="0ddcd-107">Un tipo di sistema software che fornisce dati coerenza è un sistema di controllo delle revisioni (RCS).</span><span class="sxs-lookup"><span data-stu-id="0ddcd-107">One type of software system that provides data coherency is a revision control system (RCS).</span></span> <span data-ttu-id="0ddcd-108">Un sistema di questo tipo è in genere abbastanza semplice, con un solo utente autorizzato a modificare un file specificato alla volta.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-108">Such a system is usually fairly simple, with only one user allowed to modify a specified file at a time.</span></span> <span data-ttu-id="0ddcd-109">Altri utenti possono leggere il file, ma non modificarlo.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-109">Others can read the file but cannot change it.</span></span>

<span data-ttu-id="0ddcd-110">Si dice che l'utente che può modificare un file è stato estratto. L'utente quindi archivia il file modificato in modo che altri utenti possano visualizzare le modifiche.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-110">The user who can change a file is said to have checked it out. The user then checks in the modified file so that others may see the changes.</span></span> <span data-ttu-id="0ddcd-111">Solo dopo che l'utente ha controllato un file di nuovo, è possibile estrarlo da un altro utente.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-111">Only after the user has checked a file back in can another user check it out.</span></span>

<span data-ttu-id="0ddcd-112">Un RCS richiede l'intervento attivo degli utenti per operare in modo utile.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-112">An RCS requires the active intervention of users to operate in a useful manner.</span></span> <span data-ttu-id="0ddcd-113">Un file system che opera in una rete deve gestire automaticamente il problema.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-113">A file system that operates across a network should handle the problem automatically.</span></span>

<span data-ttu-id="0ddcd-114">La memorizzazione nella cache locale dei dati coerenti è piuttosto semplice quando si dispone di un thread in un client che accede a un file in rete alla volta.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-114">Providing local caching of coherent data is fairly simple when you have one thread on one client accessing a file across the network at a time.</span></span> <span data-ttu-id="0ddcd-115">Nella maggior parte dei casi, tuttavia, molti thread diversi in uno o più computer potrebbero leggere lo stesso file.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-115">However, in most situations many different threads on one or more computers may be reading the same file.</span></span> <span data-ttu-id="0ddcd-116">Questa situazione è ancora piuttosto semplice.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-116">This situation is still fairly straightforward.</span></span> <span data-ttu-id="0ddcd-117">Poiché i dati nel file sono statici, ogni computer client può avere la propria copia locale senza alcuna implicazione per i dati coerenza.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-117">Because the data in the file is static, each client computer can have its own local copy with no implications for data coherency.</span></span>

<span data-ttu-id="0ddcd-118">Una situazione più comune è costituita da un thread che modifica il file e da numerosi altri thread che la leggono.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-118">A more common situation is one thread modifying the file, and a lot of other threads reading it.</span></span> <span data-ttu-id="0ddcd-119">Nel momento in cui si verifica un'operazione di scrittura, tutte le cache locali del file sono obsolete.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-119">The moment a write operation occurs, all of the local caches of that file are obsolete.</span></span> <span data-ttu-id="0ddcd-120">Il server deve notificare a ogni client di abbandonare la cache.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-120">The server must notify each client to abandon its cache.</span></span> <span data-ttu-id="0ddcd-121">Tutte le operazioni di lettura successive per il file devono essere eseguite attraverso la rete.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-121">Any subsequent read operations for the file must be performed across the network.</span></span>

<span data-ttu-id="0ddcd-122">In un'altra situazione comune, più thread in uno o più client di rete potrebbero provare a scrivere nello stesso file.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-122">In another common situation, multiple threads on one or more network clients might try to write to the same file.</span></span> <span data-ttu-id="0ddcd-123">Questa situazione è simile a quella in cui più utenti RCS desiderano apportare modifiche allo stesso file.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-123">This situation is similar to a one in which several RCS users all want to make changes to the same file.</span></span> <span data-ttu-id="0ddcd-124">Ogni utente in sequenza deve estrarre il file, apportare modifiche e quindi riprovare a eseguire il file.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-124">Each user in sequence must check out the file, make changes, and then check the file back in.</span></span> <span data-ttu-id="0ddcd-125">Allo stesso modo, in uno schema di memorizzazione nella cache locale, il server deve consegnare il privilegio di scrivere in un file in un solo thread client alla volta.</span><span class="sxs-lookup"><span data-stu-id="0ddcd-125">Similarly, in a local caching scheme the server must hand off the privilege of writing to a file to one client thread at a time.</span></span>

 

 



