---
title: Ottimizzazione delle prestazioni dei DVD per i giochi di Windows
description: Questo articolo illustra come ottimizzare le prestazioni dei DVD per i giochi di Windows.
ms.assetid: 01e8dc7d-4ba7-40dd-d845-a20000201ae1
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: bb15063d0441423ccb3a9f67db84caa134f801c6
ms.sourcegitcommit: 592c9bbd22ba69802dc353bcb5eb30699f9e9403
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 08/20/2020
ms.locfileid: "106300047"
---
# <a name="optimizing-dvd-performance-for-windows-games"></a><span data-ttu-id="0e6f8-103">Ottimizzazione delle prestazioni dei DVD per i giochi di Windows</span><span class="sxs-lookup"><span data-stu-id="0e6f8-103">Optimizing DVD performance for Windows Games</span></span>

<span data-ttu-id="0e6f8-104">Una percentuale elevata di computer che eseguono Windows hanno un'unità DVD e molti giochi sono disponibili su DVD.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-104">A high percentage of computers that run Windows have a DVD drive, and many games ship on DVD.</span></span> <span data-ttu-id="0e6f8-105">Di conseguenza, è consigliabile assicurarsi che i giochi usino l'unità DVD al suo pieno vantaggio.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-105">As a result, we recommend that you ensure that your games use the DVD drive to its full advantage.</span></span> <span data-ttu-id="0e6f8-106">Comprendendo il modo in cui i dati vengono letti da un DVD e il modo in cui la posizione dei dati influisca sul tempo di lettura, è possibile ridurre i tempi di caricamento e migliorare le prestazioni complessive durante la riproduzione del gioco.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-106">By understanding how data is read from a DVD and how the data's location affects its reading time, you can reduce loading times and improve the overall performance during game play.</span></span> <span data-ttu-id="0e6f8-107">Questo articolo illustra come ottimizzare le prestazioni dei DVD per i giochi di Windows.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-107">This article discusses how to optimize DVD performance for Windows games.</span></span>

-   [<span data-ttu-id="0e6f8-108">Layout di base di un DVD</span><span class="sxs-lookup"><span data-stu-id="0e6f8-108">Basic Layout of a DVD</span></span>](#basic-layout-of-a-dvd)
-   [<span data-ttu-id="0e6f8-109">Lettura da un DVD</span><span class="sxs-lookup"><span data-stu-id="0e6f8-109">Reading from a DVD</span></span>](#reading-from-a-dvd)
-   [<span data-ttu-id="0e6f8-110">Lettura degli errori</span><span class="sxs-lookup"><span data-stu-id="0e6f8-110">Reading Errors</span></span>](#reading-errors)
-   [<span data-ttu-id="0e6f8-111">Velocità effettiva dei dati</span><span class="sxs-lookup"><span data-stu-id="0e6f8-111">Data Throughput</span></span>](#data-throughput)
-   [<span data-ttu-id="0e6f8-112">Esempi di velocità effettiva sprecata</span><span class="sxs-lookup"><span data-stu-id="0e6f8-112">Examples of Wasted Throughput</span></span>](#examples-of-wasted-throughput)
-   [<span data-ttu-id="0e6f8-113">Lettura sincrona rispetto a asincrona</span><span class="sxs-lookup"><span data-stu-id="0e6f8-113">Reading Synchronously vs. Asynchronously</span></span>](#reading-synchronously-vs-asynchronously)
-   [<span data-ttu-id="0e6f8-114">Lettura ottimale</span><span class="sxs-lookup"><span data-stu-id="0e6f8-114">Reading Optimally</span></span>](#reading-optimally)
-   [<span data-ttu-id="0e6f8-115">Compatibilità con DVD</span><span class="sxs-lookup"><span data-stu-id="0e6f8-115">DVD Compatibility</span></span>](#dvd-compatibility)
-   [<span data-ttu-id="0e6f8-116">Summary</span><span class="sxs-lookup"><span data-stu-id="0e6f8-116">Summary</span></span>](#summary)

## <a name="basic-layout-of-a-dvd"></a><span data-ttu-id="0e6f8-117">Layout di base di un DVD</span><span class="sxs-lookup"><span data-stu-id="0e6f8-117">Basic Layout of a DVD</span></span>

<span data-ttu-id="0e6f8-118">Questa figura illustra il layout di base di un DVD.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-118">This figure shows the basic layout of a DVD.</span></span>

![layout DVD](images/dvdsector.png)

<span data-ttu-id="0e6f8-120">I dati in un DVD vengono archiviati come spirale continua, ad esempio in un CD; Tuttavia, i file vengono suddivisi in blocchi e settori.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-120">Data on a DVD is stored as a continuous spiral, like on a CD; however, the files are broken up into blocks and sectors.</span></span> <span data-ttu-id="0e6f8-121">I file vengono distribuiti nei blocchi del codice di correzione degli errori (ECC) e ogni blocco è diviso in settori di 16 2 KB, ovvero 32 KB di dati in ogni blocco.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-121">Files are spread over error correction code (ECC) blocks, and each block is divided into sixteen 2-KB sectors (that is, 32 KB of data in each block).</span></span> <span data-ttu-id="0e6f8-122">I file sono allineati lungo i limiti del settore e qualsiasi spazio inutilizzato in un settore viene lasciato vuoto.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-122">Files are aligned along the sector boundaries, and any unused space in a sector is left empty.</span></span> <span data-ttu-id="0e6f8-123">Se un file ha solo 10 byte, il resto dello spazio in quel settore da 2 KB viene sprecato; quindi, quando possibile, aggregare i file in incrementi di 2 KB per ottenere la migliore densità di dati.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-123">If a file has only 10 bytes, then the rest of the space in that 2-KB sector is wasted; so when possible, bundle files into 2-KB increments to get the best data density.</span></span> <span data-ttu-id="0e6f8-124">Tenere presente che queste specifiche sono solo per DVD e per CD e HD-DVD sono disponibili specifiche differenti.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-124">Be aware that these specifications are for DVD only, and CD and HD-DVD have different specifications.</span></span>

## <a name="reading-from-a-dvd"></a><span data-ttu-id="0e6f8-125">Lettura da un DVD</span><span class="sxs-lookup"><span data-stu-id="0e6f8-125">Reading from a DVD</span></span>

<span data-ttu-id="0e6f8-126">Di seguito è illustrata la sequenza di esecuzione di un'unità DVD alla ricezione di una richiesta di lettura da un DVD:</span><span class="sxs-lookup"><span data-stu-id="0e6f8-126">Here is the sequence that a DVD drive executes upon receiving a request to read from a DVD:</span></span>

1.  <span data-ttu-id="0e6f8-127">Modificare i livelli, se necessario</span><span class="sxs-lookup"><span data-stu-id="0e6f8-127">Change layers, if necessary</span></span>
2.  <span data-ttu-id="0e6f8-128">Seek</span><span class="sxs-lookup"><span data-stu-id="0e6f8-128">Seek</span></span>
3.  <span data-ttu-id="0e6f8-129">Riattivazione dell'unità di prelievo ottico (OPU) per la lettura dei dati</span><span class="sxs-lookup"><span data-stu-id="0e6f8-129">Refocus the optical pickup unit (OPU) to read data</span></span>
4.  <span data-ttu-id="0e6f8-130">Controllare la posizione effettiva</span><span class="sxs-lookup"><span data-stu-id="0e6f8-130">Check the actual position</span></span>
5.  <span data-ttu-id="0e6f8-131">Modifica e Ripeti fino a quando non vengono trovati i dati corretti</span><span class="sxs-lookup"><span data-stu-id="0e6f8-131">Adjust and repeat until the correct data is found</span></span>

<span data-ttu-id="0e6f8-132">Le operazioni di lettura unità sono quantizzate in modo diverso, a seconda che si tratti di letture di unità logiche o letture di unità fisiche.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-132">Drive reading operations are quantized differently, depending on whether they are logical drive reads or physical drive reads.</span></span> <span data-ttu-id="0e6f8-133">Le letture di unità logiche possono leggere solo una quantità intera di settori DVD, mentre una richiesta di lettura dell'unità fisica può leggere solo una quantità intera di blocchi ECC.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-133">Logical drive reads can only read an integer quantity of DVD sectors, while a physical drive read request can only read an integer quantity of ECC blocks.</span></span> <span data-ttu-id="0e6f8-134">In genere, l'unità fisica riceve una richiesta di lettura; verrà eseguito un tentativo di riempimento della cache.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-134">Typically, physical drive receives a read request; it will try to fill its cache.</span></span> <span data-ttu-id="0e6f8-135">Le dimensioni della cache dell'unità DVD dipendono dalle specifiche delle singole unità.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-135">The DVD drive cache size depends on the individual drive's specifications.</span></span>

<span data-ttu-id="0e6f8-136">Quando un'unità DVD riceve una richiesta di lettura che supera la dimensione della cache, la richiesta viene suddivisa in richieste di dimensioni della cache.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-136">When a DVD drive gets a read request that exceeds the cache size, the request is broken down into cache-sized requests.</span></span> <span data-ttu-id="0e6f8-137">L'unità cerca il blocco ECC che contiene il primo settore della richiesta e legge l'intero blocco ECC.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-137">The drive seeks the ECC block that contains the first sector of the request and reads the whole ECC block.</span></span> <span data-ttu-id="0e6f8-138">Il firmware dell'unità decodifica il blocco ECC e quindi legge il blocco ECC successivo.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-138">The drive firmware decodes the ECC block and then reads the next ECC block.</span></span> <span data-ttu-id="0e6f8-139">Il processo viene ripetuto fino a quando non viene riempita la cache dell'unità o tutte le richieste vengono soddisfatte.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-139">The process is repeated till the drive cache is filled or all requests are fulfilled.</span></span> <span data-ttu-id="0e6f8-140">Il kernel legge quindi i dati decodificati dalla cache dell'unità.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-140">The kernel then reads the decoded data from the drive cache.</span></span> <span data-ttu-id="0e6f8-141">Scarica quindi la cache e avvia la successiva operazione di lettura, se vengono mantenute le richieste di lettura.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-141">It then flushes the cache and starts the next read operation, if any read requests remain.</span></span>

> [!Note]  
> <span data-ttu-id="0e6f8-142">Ogni lettura non memorizzata nella cache Scarica la cache dell'unità.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-142">Every uncached read flushes the drive cache.</span></span>

 

## <a name="reading-errors"></a><span data-ttu-id="0e6f8-143">Lettura degli errori</span><span class="sxs-lookup"><span data-stu-id="0e6f8-143">Reading Errors</span></span>

<span data-ttu-id="0e6f8-144">Le unità DVD e DVD non sono perfette ed è possibile che si verifichino errori durante la lettura.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-144">DVDs and DVD drives are not perfect, and errors can occur during reading.</span></span> <span data-ttu-id="0e6f8-145">Analogamente a CDs, le parti di un DVD possono diventare illeggibili da polvere o graffi.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-145">Like CDs, portions of a DVD can become unreadable from dust or scratches.</span></span> <span data-ttu-id="0e6f8-146">Se una parte di un blocco è illeggibile, l'intero blocco viene considerato illeggibile.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-146">If any part of a block is unreadable, the whole block is considered unreadable.</span></span> <span data-ttu-id="0e6f8-147">Se si verifica un errore di lettura, l'unità tenta di rileggere il blocco ECC.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-147">If a read error occurs, the drive tries re-reading the ECC block.</span></span> <span data-ttu-id="0e6f8-148">Se il blocco è ancora illeggibile, l'unità interrompe l'operazione di lettura e restituisce un valore al kernel che indica che il blocco è stato illeggibile.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-148">If the block is still unreadable, the drive aborts the read operation and returns a value to the kernel that indicates that the block was unreadable.</span></span> <span data-ttu-id="0e6f8-149">Il kernel decide quindi quale passaggio eseguire successivamente.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-149">The kernel then decides what step to take next.</span></span> <span data-ttu-id="0e6f8-150">Il kernel può eseguire nuovamente la richiesta, interrompere completamente la lettura oppure ruotare l'unità e rieseguire la richiesta.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-150">The kernel can either reissue the request, abort the reading altogether, or spin the drive down and reissue the request.</span></span>

## <a name="data-throughput"></a><span data-ttu-id="0e6f8-151">Velocità effettiva dei dati</span><span class="sxs-lookup"><span data-stu-id="0e6f8-151">Data Throughput</span></span>

<span data-ttu-id="0e6f8-152">La velocità effettiva dei dati di un'unità DVD dipende da più fattori: la posizione dei dati richiesti, il modo in cui il disco è pulito o graffiato, il numero di flussi letti dal disco, le dimensioni dei buffer associati a tali flussi e le specifiche della singola unità.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-152">The data throughput of a DVD drive depends on multiple factors: the location of requested data, how clean or scratched the disk is, the number of streams that are being read from the disk, the size of the buffers associated with those streams, and the specifications of the individual drive.</span></span> <span data-ttu-id="0e6f8-153">La velocità effettiva dipende anche dal fatto che l'unità abbia una velocità angolare costante (CAV) o una velocità lineare costante (CLV).</span><span class="sxs-lookup"><span data-stu-id="0e6f8-153">Throughput also depends on whether the drive has constant angular velocity (CAV) or constant linear velocity (CLV).</span></span> <span data-ttu-id="0e6f8-154">Se un'unità ruota con CAV, il disco si gira alla stessa velocità indipendentemente dalla posizione in cui si trova l'unità di prelievo ottico (OPU).</span><span class="sxs-lookup"><span data-stu-id="0e6f8-154">If a drive spins with CAV, the disc spins at the same speed regardless of where the optical pickup unit (OPU) is located.</span></span> <span data-ttu-id="0e6f8-155">Ciò significa che la traccia dei dati viene spostata oltre il OPU più velocemente perché il OPU si avvicina al bordo esterno del disco. Con CLV, il disco si sposta più lentamente mentre il OPU si sposta verso l'esterno, quindi la traccia dei dati passa oltre il OPU a una velocità costante.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-155">This means that the data track moves past the OPU faster as the OPU gets closer to the outer edge of the disc. With CLV, the disc spins more slowly as the OPU moves outward, so the data track moves past the OPU at a constant speed.</span></span> <span data-ttu-id="0e6f8-156">Le unità DVD nella maggior parte dei PC usano CLV.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-156">The DVD drives in most PCs use CLV.</span></span>

<span data-ttu-id="0e6f8-157">Mentre l'unità Cerca e modifica i livelli, i dati non possono essere letti dal disco. È consigliabile ridurre al minimo queste operazioni, soprattutto quando si leggono i dati per una schermata di caricamento iniziale.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-157">While the drive is seeking and changing layers, data can't be read from the disc. It is a good practice to minimize these operations, especially when reading data for an initial loading screen.</span></span>

## <a name="examples-of-wasted-throughput"></a><span data-ttu-id="0e6f8-158">Esempi di velocità effettiva sprecata</span><span class="sxs-lookup"><span data-stu-id="0e6f8-158">Examples of Wasted Throughput</span></span>

<span data-ttu-id="0e6f8-159">Per comprendere in che modo la velocità effettiva dei dati può essere sprecata, considerare un'unità ipotetica e un DVD.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-159">To understand how data throughput can be wasted, consider a hypothetical drive and DVD.</span></span> <span data-ttu-id="0e6f8-160">Si supponga che sia necessario leggere un file al centro del disco.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-160">Let's assume that a file in the middle of the disc needs to be read.</span></span> <span data-ttu-id="0e6f8-161">La velocità effettiva dall'area del disco è circa 8,25 MB/sec. Se il tratto di ricerca è una metà o un terzo di pieno, il tempo medio di ricerca è 150 ms.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-161">The throughput from that area of the disc is approximately 8.25 MB/sec. If the seek stroke is a half or a third of full, then the average seek time is 150 ms.</span></span> <span data-ttu-id="0e6f8-162">In questo esempio, 1,2 MB (150 MS × 8,25 MB/sec) potrebbero essere stati letti nel momento in cui è stato richiesto solo per ottenere il OPU in cui è possibile leggere.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-162">In this example, 1.2 MB (150 ms × 8.25 MB/sec) could have been read in the time that it took just to get the OPU to where it can read.</span></span> <span data-ttu-id="0e6f8-163">L'aggiunta di una modifica di livello aumenta la velocità effettiva sprecata a 1,8 MB (225 ms × 8,25 MB/sec).</span><span class="sxs-lookup"><span data-stu-id="0e6f8-163">Adding a layer change raises the wasted throughput to 1.8 MB (225 ms × 8.25 MB/sec).</span></span>

<span data-ttu-id="0e6f8-164">Un altro esempio che dimostra una velocità effettiva sprecata è il caricamento di 20 file individuati in modo non corretto da un'unità CAV senza modifiche al livello.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-164">Another example that demonstrates wasted throughput is loading 20 poorly located files from a CAV drive with no layer changes.</span></span> <span data-ttu-id="0e6f8-165">Se il tempo di ricerca per ogni file, più la latenza prima che i dati possano essere letti, è di circa 200 ms, vengono spesi 4 secondi (20 file × 200 ms) solo cercando i dati.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-165">If the seek time for each file, plus the latency before data can be read, is approximately 200 ms, then 4 seconds (20 files × 200 ms) are spent just seeking the data.</span></span> <span data-ttu-id="0e6f8-166">Se i file si trovano sul diametro esterno e si leggono a 11 ×, la velocità effettiva è media di 15,2 MB/sec (11 velocità/12 velocità × 16 MB/sec).</span><span class="sxs-lookup"><span data-stu-id="0e6f8-166">If the files are located on the outer diameter and read at 11× speed, then the throughput averages 15.2 MB/sec (11 speed/12 speed × 16 MB/sec).</span></span> <span data-ttu-id="0e6f8-167">La velocità effettiva sprecata in questo esempio è circa 60,8 MB (15,2 MB/sec × 4 sec).</span><span class="sxs-lookup"><span data-stu-id="0e6f8-167">The wasted throughput in this example is approximately 60.8 MB (15.2 MB/sec × 4 sec).</span></span>

## <a name="reading-synchronously-vs-asynchronously"></a><span data-ttu-id="0e6f8-168">Lettura sincrona rispetto a asincrona</span><span class="sxs-lookup"><span data-stu-id="0e6f8-168">Reading Synchronously vs. Asynchronously</span></span>

<span data-ttu-id="0e6f8-169">La lettura asincrona è più efficiente della lettura sincrona.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-169">Asynchronous reading is more efficient than synchronous reading.</span></span> <span data-ttu-id="0e6f8-170">Quando si esegue la lettura in modo sincrono, uno o più blocchi di dati ECC vengono letti nella memoria di sistema prima di essere copiati nella memoria dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-170">When reading synchronously, one or more ECC blocks of data are read into system memory before being copied into application memory.</span></span> <span data-ttu-id="0e6f8-171">Al contrario, la lettura asincrona copia i blocchi ECC decodificati direttamente nella memoria dell'applicazione, evitando la cache L2 e creando un minor overhead della CPU.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-171">In contrast, asynchronous reading copies decoded ECC blocks directly to application memory, which avoids the L2 cache and creates less CPU overhead.</span></span> <span data-ttu-id="0e6f8-172">Per leggere in modo asincrono, usare il \_ flag Overlapped del flag file \_ quando si usa la funzione [**CreateFile**](/windows/desktop/api/fileapi/nf-fileapi-createfilea) per aprire i file.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-172">To read asynchronously, use the FILE\_FLAG\_OVERLAPPED flag when using the [**CreateFile**](/windows/desktop/api/fileapi/nf-fileapi-createfilea) function to open files.</span></span> <span data-ttu-id="0e6f8-173">Per eseguire l'I/O asincrono, per la funzione [**ReadFile**](/windows/desktop/api/fileapi/nf-fileapi-readfile) è necessaria anche una struttura sovrapposta valida.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-173">The [**ReadFile**](/windows/desktop/api/fileapi/nf-fileapi-readfile) function also needs a valid OVERLAPPED structure passed in to perform asynchronous I/O.</span></span>

<span data-ttu-id="0e6f8-174">Per ulteriori informazioni sull'I/O asincrono [, vedere i/o sincrono e asincrono](/windows/desktop/FileIO/synchronous-and-asynchronous-i-o).</span><span class="sxs-lookup"><span data-stu-id="0e6f8-174">More information on asynchronous I/O can be found at [Synchronous and Asynchronous I/O](/windows/desktop/FileIO/synchronous-and-asynchronous-i-o).</span></span>

## <a name="reading-optimally"></a><span data-ttu-id="0e6f8-175">Lettura ottimale</span><span class="sxs-lookup"><span data-stu-id="0e6f8-175">Reading Optimally</span></span>

<span data-ttu-id="0e6f8-176">Il principio migliore per la lettura da un DVD consiste nell'evitare la ricerca e la lettura di piccole quantità di dati.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-176">The best principle in reading from a DVD is to avoid seeking and reading small amounts of data.</span></span> <span data-ttu-id="0e6f8-177">Quando la quantità di dati letti è inferiore alla capacità di un blocco ECC, ovvero inferiore a 32 KB, il resto del blocco viene sprecato.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-177">When the amount of data read is less than the capacity of an ECC block — less than 32 KB — the rest of the block is wasted.</span></span> <span data-ttu-id="0e6f8-178">Poiché le dimensioni della cache variano da unità a unità, gli sviluppatori devono decidere una quantità minima di dati per le richieste di lettura e non ridurli.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-178">Since cache sizes vary from drive to drive, developers must decide on a minimum amount of data for read requests and not make any smaller than that.</span></span> <span data-ttu-id="0e6f8-179">Le dimensioni minime devono essere un multiplo Integer di un blocco ECC per evitare di sprecare tempo per la lettura e la decodifica dei dati che non verranno usati.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-179">The minimum size should be an integer multiple of an ECC block to avoid wasting time on reading and decoding data that won't be used.</span></span> <span data-ttu-id="0e6f8-180">È anche importante evitare di cercare tutti i costi, perché il tempo impiegato per la ricerca è il tempo impiegato per la lettura dei dati.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-180">It is also important to avoid seeking at all costs, because any time spent seeking is time spent not reading data.</span></span>

## <a name="dvd-compatibility"></a><span data-ttu-id="0e6f8-181">Compatibilità con DVD</span><span class="sxs-lookup"><span data-stu-id="0e6f8-181">DVD Compatibility</span></span>

<span data-ttu-id="0e6f8-182">Quando si rilascia un DVD, è necessario tenere presenti alcuni importanti problemi di compatibilità.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-182">There are some important compatibility issues to be aware of when releasing on DVD.</span></span> <span data-ttu-id="0e6f8-183">Per prima cosa, le unità DVD nei computer basati su Windows possono variare in base alle prestazioni, pertanto se il DVD presenta un requisito specifico per la velocità effettiva, è importante assicurarsi che l'hardware degli utenti soddisfi tali requisiti.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-183">First, DVD drives in Windows-based computers can vary in performance, so if your DVD has a specific requirement for throughput, it is important to make sure that your users' hardware meets those requirements.</span></span> <span data-ttu-id="0e6f8-184">Inoltre, i DVD multistrato possono causare problemi di compatibilità in alcune unità DVD.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-184">Also, multilayer DVDs can cause compatibility issues on the some DVD drives.</span></span> <span data-ttu-id="0e6f8-185">Per evitare questi problemi, è consigliabile distribuire un DVD a un solo livello o testare accuratamente un DVD a più livelli nella maggior parte delle unità prima del rilascio.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-185">To avoid these issues, it is advisable to deliver a single-layer DVD or to thoroughly test a multi-layer DVD on a majority of drives before release.</span></span>

## <a name="summary"></a><span data-ttu-id="0e6f8-186">Riepilogo</span><span class="sxs-lookup"><span data-stu-id="0e6f8-186">Summary</span></span>

<span data-ttu-id="0e6f8-187">Per migliorare le prestazioni del DVD, è possibile applicare alcune regole generali.</span><span class="sxs-lookup"><span data-stu-id="0e6f8-187">To improve DVD performance, some general rules can be applied.</span></span> <span data-ttu-id="0e6f8-188">Le tecniche seguenti consentono di ottimizzare la velocità effettiva e di ridurre i dati sprecati:</span><span class="sxs-lookup"><span data-stu-id="0e6f8-188">The following techniques can help maximize throughput and reduce wasted data:</span></span>

-   <span data-ttu-id="0e6f8-189">Evitare letture di dimensioni inferiori a 32 KB</span><span class="sxs-lookup"><span data-stu-id="0e6f8-189">Avoid reads that are smaller than 32 KB</span></span>
-   <span data-ttu-id="0e6f8-190">Layout dei dati per ridurre o eliminare le ricerche</span><span class="sxs-lookup"><span data-stu-id="0e6f8-190">Lay out data to reduce or eliminate seeks</span></span>
-   <span data-ttu-id="0e6f8-191">Layout dei dati nei limiti di blocco ECC</span><span class="sxs-lookup"><span data-stu-id="0e6f8-191">Lay out data on ECC block boundaries</span></span>
-   <span data-ttu-id="0e6f8-192">Massimizza la capacità aggregando i file di piccole dimensioni in blocchi da 2 KB e Riduci lo spazio di riempimento nei settori DVD</span><span class="sxs-lookup"><span data-stu-id="0e6f8-192">Maximize capacity by bundling small files into 2-KB blocks, and reduce padding space in DVD sectors</span></span>
-   <span data-ttu-id="0e6f8-193">Lettura asincrona per ridurre il carico della CPU e un utilizzo eccessivo della memoria</span><span class="sxs-lookup"><span data-stu-id="0e6f8-193">Read asynchronously to reduce CPU load and excessive memory usage</span></span>
-   <span data-ttu-id="0e6f8-194">Evitare di rilasciare DVD multistrato</span><span class="sxs-lookup"><span data-stu-id="0e6f8-194">Avoid releasing multilayer DVDs</span></span>

 

 