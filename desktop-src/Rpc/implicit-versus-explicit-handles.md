---
title: Handle impliciti e espliciti
description: Per dichiarare un handle di serializzazione, usare il tipo di handle primitivo handle \_ t.
ms.assetid: 70d8665f-d793-46fc-bcbf-ecb24e746786
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: bcead663ae7eee8d0cdb95a73e7ae58935773cef
ms.sourcegitcommit: 592c9bbd22ba69802dc353bcb5eb30699f9e9403
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 08/20/2020
ms.locfileid: "103872842"
---
# <a name="implicit-vs-explicit-handles"></a><span data-ttu-id="c3a73-103">Handle impliciti e espliciti</span><span class="sxs-lookup"><span data-stu-id="c3a73-103">Implicit vs. Explicit Handles</span></span>

<span data-ttu-id="c3a73-104">Per dichiarare un handle di serializzazione, usare il tipo di handle primitivo [**handle \_ t**](/windows/desktop/Midl/handle-t).</span><span class="sxs-lookup"><span data-stu-id="c3a73-104">To declare a serialization handle, use the primitive handle type [**handle\_t**](/windows/desktop/Midl/handle-t).</span></span> <span data-ttu-id="c3a73-105">Gli handle di serializzazione possono essere espliciti o impliciti.</span><span class="sxs-lookup"><span data-stu-id="c3a73-105">Serialization handles can be explicit or implicit.</span></span> <span data-ttu-id="c3a73-106">Specificare gli handle impliciti nell'ACF dell'applicazione usando l'attributo **\[ \_ handle \] implicito** .</span><span class="sxs-lookup"><span data-stu-id="c3a73-106">Specify implicit handles in your application's ACF by using the **\[implicit\_handle\]** attribute.</span></span> <span data-ttu-id="c3a73-107">Il compilatore MIDL genererà una variabile globale dell'handle di serializzazione.</span><span class="sxs-lookup"><span data-stu-id="c3a73-107">The MIDL compiler will generate a global serialization handle variable.</span></span> <span data-ttu-id="c3a73-108">Le procedure di serializzazione con un handle implicito utilizzano questa variabile globale per accedere a un contesto di serializzazione valido.</span><span class="sxs-lookup"><span data-stu-id="c3a73-108">Serialization procedures with an implicit handle use this global variable in order to access a valid serializing context.</span></span>

<span data-ttu-id="c3a73-109">Quando si utilizza la codifica dei tipi, le routine generate che supportano la serializzazione di un particolare tipo utilizzano l'handle implicito globale per accedere al contesto di serializzazione.</span><span class="sxs-lookup"><span data-stu-id="c3a73-109">When using type encoding, the generated routines supporting serialization of a particular type use the global implicit handle to access the serialization context.</span></span> <span data-ttu-id="c3a73-110">Si noti che le routine remote possono richiedere l'uso dell'handle implicito come handle di binding.</span><span class="sxs-lookup"><span data-stu-id="c3a73-110">Note that remote routines may need to use the implicit handle as a binding handle.</span></span> <span data-ttu-id="c3a73-111">Verificare che l'handle implicito sia impostato su un handle di serializzazione valido prima di effettuare una chiamata di serializzazione.</span><span class="sxs-lookup"><span data-stu-id="c3a73-111">Be sure that the implicit handle is set to a valid serializing handle prior to making a serializing call.</span></span>

<span data-ttu-id="c3a73-112">Un handle esplicito viene specificato come parametro del prototipo della procedura di serializzazione nel file IDL oppure può essere specificato anche usando l'attributo **\[ \_ handle \] esplicito** in ACF.</span><span class="sxs-lookup"><span data-stu-id="c3a73-112">An explicit handle is specified as a parameter of the serialization procedure prototype in the IDL file, or it can also be specified by using the **\[explicit\_handle\]** attribute in the ACF.</span></span> <span data-ttu-id="c3a73-113">Il parametro dell'handle esplicito viene utilizzato per stabilire il contesto di serializzazione appropriato per la procedura.</span><span class="sxs-lookup"><span data-stu-id="c3a73-113">The explicit handle parameter is used to establish the proper serialization context for the procedure.</span></span> <span data-ttu-id="c3a73-114">Per stabilire il contesto corretto nel caso della serializzazione dei tipi, il compilatore genera le routine di supporto che usano il parametro esplicito [**handle \_ t**](/windows/desktop/Midl/handle-t) come handle di serializzazione.</span><span class="sxs-lookup"><span data-stu-id="c3a73-114">To establish the correct context in the case of type serialization, the compiler generates the supporting routines that use explicit [**handle\_t**](/windows/desktop/Midl/handle-t) parameter as the serialization handle.</span></span> <span data-ttu-id="c3a73-115">È necessario fornire un handle di serializzazione valido quando si chiama una routine di serializzazione o una routine di supporto del tipo di serializzazione.</span><span class="sxs-lookup"><span data-stu-id="c3a73-115">You must supply a valid serializing handle when calling a serialization procedure or serialization type support routine.</span></span>

 

 