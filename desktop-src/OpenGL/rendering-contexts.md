---
title: Contesti di rendering
description: Un contesto di rendering OpenGL è una porta attraverso la quale tutti i comandi di OpenGL passano. Ogni thread che effettua chiamate OpenGL deve avere un contesto di rendering corrente. I contesti di rendering collegano OpenGL ai sistemi Windows windowing.
ms.assetid: 9fbbb0be-2db4-4bfc-9a5c-a43d71554abc
keywords:
- OpenGL per Windows, contesti di rendering
- rendering di contesti OpenGL
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 53ac2ac6c5948da2b7372d600377666cd9e4e074
ms.sourcegitcommit: 2d531328b6ed82d4ad971a45a5131b430c5866f7
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 09/16/2019
ms.locfileid: "104330983"
---
# <a name="rendering-contexts"></a><span data-ttu-id="1afde-107">Contesti di rendering</span><span class="sxs-lookup"><span data-stu-id="1afde-107">Rendering Contexts</span></span>

<span data-ttu-id="1afde-108">Un *contesto di rendering* OpenGL è una porta attraverso la quale tutti i comandi di OpenGL passano.</span><span class="sxs-lookup"><span data-stu-id="1afde-108">An OpenGL *rendering context* is a port through which all OpenGL commands pass.</span></span> <span data-ttu-id="1afde-109">Ogni thread che effettua chiamate OpenGL deve avere un contesto di rendering corrente.</span><span class="sxs-lookup"><span data-stu-id="1afde-109">Every thread that makes OpenGL calls must have a current rendering context.</span></span> <span data-ttu-id="1afde-110">I contesti di rendering collegano OpenGL ai sistemi Windows windowing.</span><span class="sxs-lookup"><span data-stu-id="1afde-110">Rendering contexts link OpenGL to the Windows windowing systems.</span></span>

<span data-ttu-id="1afde-111">Un'applicazione specifica un contesto di dispositivo Windows quando crea un contesto di rendering.</span><span class="sxs-lookup"><span data-stu-id="1afde-111">An application specifies a Windows device context when it creates a rendering context.</span></span> <span data-ttu-id="1afde-112">Questo contesto di rendering è adatto per il disegno sul dispositivo a cui fa riferimento il contesto di dispositivo specificato.</span><span class="sxs-lookup"><span data-stu-id="1afde-112">This rendering context is suitable for drawing on the device that the specified device context references.</span></span> <span data-ttu-id="1afde-113">In particolare, il contesto di rendering ha lo stesso formato pixel del contesto di dispositivo.</span><span class="sxs-lookup"><span data-stu-id="1afde-113">In particular, the rendering context has the same pixel format as the device context.</span></span> <span data-ttu-id="1afde-114">Per altre informazioni, vedere [rendering delle funzioni di contesto](rendering-context-functions.md).</span><span class="sxs-lookup"><span data-stu-id="1afde-114">For more information, see [Rendering Context Functions](rendering-context-functions.md).</span></span>

<span data-ttu-id="1afde-115">Nonostante questa relazione, un contesto di rendering non corrisponde a un contesto di dispositivo.</span><span class="sxs-lookup"><span data-stu-id="1afde-115">Despite this relationship, a rendering context is not the same as a device context.</span></span> <span data-ttu-id="1afde-116">Un contesto di dispositivo contiene informazioni pertinenti al componente grafico (GDI) di Windows.</span><span class="sxs-lookup"><span data-stu-id="1afde-116">A device context contains information pertinent to the graphics component (GDI) of Windows.</span></span> <span data-ttu-id="1afde-117">Un contesto di rendering contiene informazioni pertinenti per OpenGL.</span><span class="sxs-lookup"><span data-stu-id="1afde-117">A rendering context contains information pertinent to OpenGL.</span></span> <span data-ttu-id="1afde-118">Un contesto di dispositivo deve essere specificato in modo esplicito in una chiamata GDI.</span><span class="sxs-lookup"><span data-stu-id="1afde-118">A device context must be explicitly specified in a GDI call.</span></span> <span data-ttu-id="1afde-119">Un contesto di rendering è implicito in una chiamata OpenGL.</span><span class="sxs-lookup"><span data-stu-id="1afde-119">A rendering context is implicit in an OpenGL call.</span></span> <span data-ttu-id="1afde-120">È necessario impostare il formato pixel di un contesto di dispositivo prima di creare un contesto di rendering.</span><span class="sxs-lookup"><span data-stu-id="1afde-120">You should set a device context's pixel format before creating a rendering context.</span></span>

<span data-ttu-id="1afde-121">Un thread che effettua chiamate OpenGL deve avere un contesto di rendering corrente.</span><span class="sxs-lookup"><span data-stu-id="1afde-121">A thread that makes OpenGL calls must have a current rendering context.</span></span> <span data-ttu-id="1afde-122">Se un'applicazione effettua chiamate OpenGL da un thread che non dispone di un contesto di rendering corrente, non viene eseguita alcuna operazione; la chiamata non ha alcun effetto.</span><span class="sxs-lookup"><span data-stu-id="1afde-122">If an application makes OpenGL calls from a thread that lacks a current rendering context, nothing happens; the call has no effect.</span></span> <span data-ttu-id="1afde-123">Un'applicazione crea in genere un contesto di rendering, la imposta come contesto di rendering corrente del thread e quindi chiama le funzioni OpenGL.</span><span class="sxs-lookup"><span data-stu-id="1afde-123">An application commonly creates a rendering context, sets it as a thread's current rendering context, and then calls OpenGL functions.</span></span> <span data-ttu-id="1afde-124">Al termine della chiamata alle funzioni OpenGL, l'applicazione separa il contesto di rendering dal thread, quindi Elimina il contesto di rendering.</span><span class="sxs-lookup"><span data-stu-id="1afde-124">When it finishes calling OpenGL functions, the application uncouples the rendering context from the thread, and then deletes the rendering context.</span></span> <span data-ttu-id="1afde-125">Una finestra può includere più contesti di rendering contemporaneamente, ma un thread può avere un solo contesto di rendering attivo e corrente.</span><span class="sxs-lookup"><span data-stu-id="1afde-125">A window can have multiple rendering contexts drawing to it at one time, but a thread can have only one current, active rendering context.</span></span>

<span data-ttu-id="1afde-126">A un contesto di rendering corrente è associato un contesto di dispositivo.</span><span class="sxs-lookup"><span data-stu-id="1afde-126">A current rendering context has an associated device context.</span></span> <span data-ttu-id="1afde-127">Il contesto di dispositivo non deve corrispondere allo stesso contesto di dispositivo usato durante la creazione del contesto di rendering, ma deve fare riferimento allo stesso dispositivo e avere lo stesso formato pixel.</span><span class="sxs-lookup"><span data-stu-id="1afde-127">That device context need not be the same device context as that used when the rendering context was created, but it must reference the same device and have the same pixel format.</span></span>

<span data-ttu-id="1afde-128">Un thread può avere un solo contesto di rendering corrente.</span><span class="sxs-lookup"><span data-stu-id="1afde-128">A thread can have only one current rendering context.</span></span> <span data-ttu-id="1afde-129">Un contesto di rendering può essere aggiornato a un solo thread.</span><span class="sxs-lookup"><span data-stu-id="1afde-129">A rendering context can be current to only one thread.</span></span>

 

 




