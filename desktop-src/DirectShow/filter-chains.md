---
description: Catene di filtri
ms.assetid: c17b3b58-65ab-4e83-91f2-54a995f22ddf
title: Catene di filtri
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: d46e374aca71b024773e4177d09e67c7ee6034ac
ms.sourcegitcommit: a47bd86f517de76374e4fff33cfeb613eb259a7e
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/06/2021
ms.locfileid: "103876826"
---
# <a name="filter-chains"></a><span data-ttu-id="c808a-103">Catene di filtri</span><span class="sxs-lookup"><span data-stu-id="c808a-103">Filter Chains</span></span>

<span data-ttu-id="c808a-104">Una *catena* di filtri è una sequenza di filtri che soddisfa le condizioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="c808a-104">A *filter chain* is a sequence of filters that meets the following conditions:</span></span>

-   <span data-ttu-id="c808a-105">Ogni filtro della catena ha al massimo un pin di input connesso e un pin di output connesso.</span><span class="sxs-lookup"><span data-stu-id="c808a-105">Each filter in the chain has at most one connected input pin and one connected output pin.</span></span>
-   <span data-ttu-id="c808a-106">È possibile attraversare tutti i filtri della catena senza attraversare i filtri all'esterno della catena.</span><span class="sxs-lookup"><span data-stu-id="c808a-106">It is possible to traverse every filter in the chain without traversing filters outside the chain.</span></span>

<span data-ttu-id="c808a-107">Nel diagramma seguente, ad esempio, i filtri A-B, C-D e F – G – H sono catene di filtri.</span><span class="sxs-lookup"><span data-stu-id="c808a-107">For example, in the following diagram, filters A–B, C–D, and F–G–H are filter chains.</span></span> <span data-ttu-id="c808a-108">Ogni sottocatena in F – G – H (F – G e G – H) è anche una catena di filtri.</span><span class="sxs-lookup"><span data-stu-id="c808a-108">Each subchain in F–G–H (F–G and G–H) is also a filter chain.</span></span> <span data-ttu-id="c808a-109">Una catena di filtri può essere costituita da un singolo filtro, quindi i filtri A, B, C, D, F, G e H sono anche catene di filtri distinti.</span><span class="sxs-lookup"><span data-stu-id="c808a-109">A filter chain can consist of a single filter, so filters A, B, C, D, F, G, and H are also distinct filter chains.</span></span> <span data-ttu-id="c808a-110">Filter E include due connessioni di input, pertanto qualsiasi sequenza di filtri che includa Filter E non è una catena di filtri.</span><span class="sxs-lookup"><span data-stu-id="c808a-110">Filter E has two input connections, so any sequence of filters that includes filter E is not a filter chain.</span></span>

![catena di filtri (esempio 1)](images/filter-chain1.png)

<span data-ttu-id="c808a-112">L'interfaccia [**IFilterChain**](/windows/desktop/api/Strmif/nn-strmif-ifilterchain) fornisce i metodi seguenti per controllare le catene di filtri:</span><span class="sxs-lookup"><span data-stu-id="c808a-112">The [**IFilterChain**](/windows/desktop/api/Strmif/nn-strmif-ifilterchain) interface provides the following methods for controlling filter chains:</span></span>



|                                                               |                                 |
|---------------------------------------------------------------|---------------------------------|
| [<span data-ttu-id="c808a-113">**IFilterChain::StartChain**</span><span class="sxs-lookup"><span data-stu-id="c808a-113">**IFilterChain::StartChain**</span></span>](/windows/desktop/api/Strmif/nf-strmif-ifilterchain-startchain)   | <span data-ttu-id="c808a-114">Avvia una catena.</span><span class="sxs-lookup"><span data-stu-id="c808a-114">Starts a chain.</span></span>                 |
| [<span data-ttu-id="c808a-115">**IFilterChain::StopChain**</span><span class="sxs-lookup"><span data-stu-id="c808a-115">**IFilterChain::StopChain**</span></span>](/windows/desktop/api/Strmif/nf-strmif-ifilterchain-stopchain)     | <span data-ttu-id="c808a-116">Arresta una catena.</span><span class="sxs-lookup"><span data-stu-id="c808a-116">Stops a chain.</span></span>                  |
| [<span data-ttu-id="c808a-117">**IFilterChain::P auseChain**</span><span class="sxs-lookup"><span data-stu-id="c808a-117">**IFilterChain::PauseChain**</span></span>](/windows/desktop/api/Strmif/nf-strmif-ifilterchain-pausechain)   | <span data-ttu-id="c808a-118">Sospende una catena.</span><span class="sxs-lookup"><span data-stu-id="c808a-118">Pauses a chain.</span></span>                 |
| [<span data-ttu-id="c808a-119">**IFilterChain::RemoveChain**</span><span class="sxs-lookup"><span data-stu-id="c808a-119">**IFilterChain::RemoveChain**</span></span>](/windows/desktop/api/Strmif/nf-strmif-ifilterchain-removechain) | <span data-ttu-id="c808a-120">Rimuove una catena dal grafico.</span><span class="sxs-lookup"><span data-stu-id="c808a-120">Removes a chain from the graph.</span></span> |



 

<span data-ttu-id="c808a-121">Non esiste alcun metodo specifico per l'aggiunta di una catena.</span><span class="sxs-lookup"><span data-stu-id="c808a-121">There is no specific method for adding a chain.</span></span> <span data-ttu-id="c808a-122">Per aggiungere una catena, inserire i nuovi filtri usando il metodo [**IFilterGraph:: AddFilter**](/windows/desktop/api/Strmif/nf-strmif-ifiltergraph-addfilter) .</span><span class="sxs-lookup"><span data-stu-id="c808a-122">To add a chain, insert the new filters using the [**IFilterGraph::AddFilter**](/windows/desktop/api/Strmif/nf-strmif-ifiltergraph-addfilter) method.</span></span> <span data-ttu-id="c808a-123">Connettere quindi i filtri chiamando [**IGraphBuilder:: Connect**](/windows/desktop/api/Strmif/nf-strmif-igraphbuilder-connect), [**IGraphBuilder:: Render**](/windows/desktop/api/Strmif/nf-strmif-igraphbuilder-render)o metodi simili.</span><span class="sxs-lookup"><span data-stu-id="c808a-123">Then connect the filters by calling [**IGraphBuilder::Connect**](/windows/desktop/api/Strmif/nf-strmif-igraphbuilder-connect), [**IGraphBuilder::Render**](/windows/desktop/api/Strmif/nf-strmif-igraphbuilder-render), or similar methods.</span></span>

<span data-ttu-id="c808a-124">Quando il grafico è in esecuzione, una catena di filtri può spostarsi tra l'esecuzione e l'arresto.</span><span class="sxs-lookup"><span data-stu-id="c808a-124">When the graph is running, a filter chain can switch between running and stopped.</span></span> <span data-ttu-id="c808a-125">Quando il grafico è sospeso, può passare da una pausa all'altra e viceversa.</span><span class="sxs-lookup"><span data-stu-id="c808a-125">When the graph is paused, it can switch between paused and stopped.</span></span> <span data-ttu-id="c808a-126">Queste sono le uniche transizioni di stato possibili con le catene di filtri.</span><span class="sxs-lookup"><span data-stu-id="c808a-126">These are the only state transitions possible with filter chains.</span></span>

## <a name="filter-chain-guidelines"></a><span data-ttu-id="c808a-127">Linee guida per la catena di filtri</span><span class="sxs-lookup"><span data-stu-id="c808a-127">Filter Chain Guidelines</span></span>

<span data-ttu-id="c808a-128">Quando si usano i metodi **IFilterChain** , è importante assicurarsi che i filtri nel grafico possano supportare le operazioni di concatenamento dei filtri.</span><span class="sxs-lookup"><span data-stu-id="c808a-128">When you use **IFilterChain** methods, it is important to make sure that the filters in the graph can support filter chaining operations.</span></span> <span data-ttu-id="c808a-129">In caso contrario, è possibile che si verifichino deadlock o errori del grafo.</span><span class="sxs-lookup"><span data-stu-id="c808a-129">Otherwise, you might cause deadlocks or graph errors.</span></span> <span data-ttu-id="c808a-130">I filtri connessi alla catena devono funzionare correttamente dopo che lo stato della catena è stato modificato.</span><span class="sxs-lookup"><span data-stu-id="c808a-130">Filters connected to the chain must function correctly after the chain changes state.</span></span>

<span data-ttu-id="c808a-131">Il modo migliore per usare **IFilterChain** è costituito da un set di filtri progettati in modo specifico per il concatenamento.</span><span class="sxs-lookup"><span data-stu-id="c808a-131">The best way to use **IFilterChain** is with a set of filters that you have designed specifically for chaining.</span></span> <span data-ttu-id="c808a-132">Usare le linee guida seguenti per assicurarsi che i filtri siano sicuri per le operazioni a catena di filtri.</span><span class="sxs-lookup"><span data-stu-id="c808a-132">Use the following guidelines to ensure that your filters are safe for filter chain operations.</span></span> <span data-ttu-id="c808a-133">Questi punti fanno riferimento al diagramma seguente.</span><span class="sxs-lookup"><span data-stu-id="c808a-133">These points refer to the following diagram.</span></span>

![catena di filtri (esempio 2)](images/filter-chain2.png)

-   <span data-ttu-id="c808a-135">Prima che lo stato della catena di filtri venga modificato, è necessario completare tutte le chiamate di elaborazione dati al limite della catena di filtri.</span><span class="sxs-lookup"><span data-stu-id="c808a-135">Before the filter chain's state changes, all data processing calls at the boundary of the filter chain must be completed.</span></span> <span data-ttu-id="c808a-136">Questa regola si applica ai metodi [**IMemInputPin:: Receive**](/windows/desktop/api/Strmif/nf-strmif-imeminputpin-receive), [**Ipin:: NewSegment**](/windows/desktop/api/Strmif/nf-strmif-ipin-newsegment)e [**Ipin:: EndOfStream**](/windows/desktop/api/Strmif/nf-strmif-ipin-endofstream).</span><span class="sxs-lookup"><span data-stu-id="c808a-136">This rule applies to the methods [**IMemInputPin::Receive**](/windows/desktop/api/Strmif/nf-strmif-imeminputpin-receive), [**IPin::NewSegment**](/windows/desktop/api/Strmif/nf-strmif-ipin-newsegment), and [**IPin::EndOfStream**](/windows/desktop/api/Strmif/nf-strmif-ipin-endofstream).</span></span> <span data-ttu-id="c808a-137">I filtri nella catena devono restituire dalle chiamate a questi metodi creati da filtri all'esterno della catena; i filtri e all'esterno della catena devono restituire dalle chiamate effettuate da filtri all'interno della catena.</span><span class="sxs-lookup"><span data-stu-id="c808a-137">Filters in the chain must return from calls to these methods made by filters outside the chain; and filters outside the chain must return from calls made by filters within the chain.</span></span>

<span data-ttu-id="c808a-138">Nel diagramma precedente, ad esempio, il filtro B deve completare qualsiasi chiamata di elaborazione dati dal filtro A e il filtro E deve terminare tutte le chiamate dal filtro D. Se i pin espongono le interfacce [**IPinFlowControl**](/windows/desktop/api/Strmif/nn-strmif-ipinflowcontrol) e [**IPinConnection**](/windows/desktop/api/Strmif/nn-strmif-ipinconnection) , è possibile eseguire il push dei dati tramite il grafo chiamando il metodo [**IPinFlowControl:: Block**](/windows/desktop/api/Strmif/nf-strmif-ipinflowcontrol-block) e [**IGraphConfig::P Ushthroughdata**](/windows/desktop/api/Strmif/nf-strmif-igraphconfig-pushthroughdata) , come descritto in [riconnessione dinamica](dynamic-reconnection.md).</span><span class="sxs-lookup"><span data-stu-id="c808a-138">For example, in the previous diagram, filter B must complete any data processing calls from filter A, and filter E must finish any calls from filter D. If the pins expose the [**IPinFlowControl**](/windows/desktop/api/Strmif/nn-strmif-ipinflowcontrol) and [**IPinConnection**](/windows/desktop/api/Strmif/nn-strmif-ipinconnection) interfaces, you can push the data through the graph by calling the [**IPinFlowControl::Block**](/windows/desktop/api/Strmif/nf-strmif-ipinflowcontrol-block) and [**IGraphConfig::PushThroughData**](/windows/desktop/api/Strmif/nf-strmif-igraphconfig-pushthroughdata) methods, as described in [Dynamic Reconnection](dynamic-reconnection.md).</span></span> <span data-ttu-id="c808a-139">I filtri possono inoltre supportare metodi privati per eseguire il push dei dati.</span><span class="sxs-lookup"><span data-stu-id="c808a-139">Filters might also support private methods for pushing the data.</span></span>

-   <span data-ttu-id="c808a-140">I filtri upstream devono prevedere che lo stato della catena venga modificato.</span><span class="sxs-lookup"><span data-stu-id="c808a-140">Upstream filters must expect the chain's state to change.</span></span> <span data-ttu-id="c808a-141">Nel diagramma precedente, ad esempio, si supponga che la catena venga arrestata, ma che il filtro A chiami **IMemInputPin:: Receive**.</span><span class="sxs-lookup"><span data-stu-id="c808a-141">For example, in the previous diagram, suppose the chain is stopped but filter A calls **IMemInputPin::Receive**.</span></span> <span data-ttu-id="c808a-142">La chiamata ha esito negativo e la risposta del filtro A consiste nell'arrestare lo streaming.</span><span class="sxs-lookup"><span data-stu-id="c808a-142">The call fails and the response of filter A is to stop streaming.</span></span> <span data-ttu-id="c808a-143">Quando l'applicazione riavvia la catena, non ha alcun effetto perché il filtro A non è più in streaming dei dati.</span><span class="sxs-lookup"><span data-stu-id="c808a-143">When the application restarts the chain, it has no effect because filter A is no longer streaming data.</span></span>
-   <span data-ttu-id="c808a-144">Anche i filtri downstream devono prevedere che lo stato della catena venga modificato.</span><span class="sxs-lookup"><span data-stu-id="c808a-144">Downstream filters must also expect the chain's state to change.</span></span> <span data-ttu-id="c808a-145">In caso contrario, il filtro downstream potrebbe bloccarsi durante l'attesa di campioni che non arrivano mai.</span><span class="sxs-lookup"><span data-stu-id="c808a-145">If not, the downstream filter might block while it waits for samples that never arrive.</span></span> <span data-ttu-id="c808a-146">I filtri multiplexer (MUX), ad esempio, richiedono spesso i dati di tutti i pin di input.</span><span class="sxs-lookup"><span data-stu-id="c808a-146">For example, multiplexer (MUX) filters often require data from all of their input pins.</span></span> <span data-ttu-id="c808a-147">L'arresto del flusso di dati da un pin di input potrebbe impedire l'elaborazione degli altri flussi.</span><span class="sxs-lookup"><span data-stu-id="c808a-147">Halting the flow of data from one input pin might block the other streams from processing.</span></span> <span data-ttu-id="c808a-148">Questo può causare il deadlock del grafico.</span><span class="sxs-lookup"><span data-stu-id="c808a-148">This can cause the graph to deadlock.</span></span>
-   <span data-ttu-id="c808a-149">Ogni connessione pin da un filtro all'esterno della catena a un filtro all'interno della catena deve avere un proprio allocatore, che non è condiviso da altre connessioni.</span><span class="sxs-lookup"><span data-stu-id="c808a-149">Each pin connection from a filter outside the chain to a filter within the chain should have its own allocator, which is not shared by other connections.</span></span> <span data-ttu-id="c808a-150">Quando la catena cambia stato o viene rimossa dal grafico, è possibile che venga eseguito il commit dell'allocatore.</span><span class="sxs-lookup"><span data-stu-id="c808a-150">When the chain changes state or is removed from the graph, the allocator might be decommitted.</span></span> <span data-ttu-id="c808a-151">Se altre connessioni utilizzano lo stesso allocatore, non possono più elaborare gli esempi.</span><span class="sxs-lookup"><span data-stu-id="c808a-151">If other connections were using the same allocator, they can no longer process samples.</span></span>
-   <span data-ttu-id="c808a-152">Non rimuovere una catena, a meno che i filtri connessi alla catena non supportino la disconnessione dinamica.</span><span class="sxs-lookup"><span data-stu-id="c808a-152">Do not remove a chain unless the filters connected to the chain support dynamic disconnection.</span></span> <span data-ttu-id="c808a-153">In genere, i filtri connessi supporteranno l'interfaccia **IPinConnection** o **IPinFlowControl** , ma potrebbero supportare invece le interfacce private.</span><span class="sxs-lookup"><span data-stu-id="c808a-153">Typically, the connected filters will support the **IPinConnection** or **IPinFlowControl** interface, but might support private interfaces instead.</span></span>

## <a name="related-topics"></a><span data-ttu-id="c808a-154">Argomenti correlati</span><span class="sxs-lookup"><span data-stu-id="c808a-154">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="c808a-155">Creazione di grafici dinamici</span><span class="sxs-lookup"><span data-stu-id="c808a-155">Dynamic Graph Building</span></span>](dynamic-graph-building.md)
</dt> </dl>

 

 



