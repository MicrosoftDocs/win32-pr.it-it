---
description: ID stringa di risorsa impostati dal chiamante da restituire nei dati xml per la visualizzazione di oggetti.
MS-HAID: vspixengine.Xml\_Resource\_Ids
MSHAttr:
- PreferredSiteName:MSDN
- PreferredLib:/library/windows/desktop
title: Xml_Resource_Ids enumerazione
ms.topic: reference
ms.date: 05/31/2018
ms.assetid: 1F2ECC78-73EC-4A59-8CF7-1BB7FB80244E
api_name:
- Xml_Resource_Ids
api_type:
- HeaderDef
api_location:
- vspixengine.h
topic_type:
- APIRef
- kbSyntax
ms.openlocfilehash: 29617a4c4f8e42fd4c6049a987c98e7edcf11d783a0d226fc9fc751d8d28c2ba
ms.sourcegitcommit: e858bbe701567d4583c50a11326e42d7ea51804b
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 08/11/2021
ms.locfileid: "119119075"
---
# <a name="span-idvspixenginexml_resource_idsspanxml_resource_ids-enumeration"></a><span id="vspixengine.xml_resource_ids"></span>Enumerazione \_ DEGLI ID risorsa \_ XML

ID stringa di risorsa impostati dal chiamante da restituire nei dati XML per la visualizzazione di oggetti

## <a name="syntax"></a>Sintassi


```C++
};
```

## <a name="constants"></a>Costanti

<span id="XML_DEVICE_INFORMATION"></span><span id="xml_device_information"></span>**INFORMAZIONI \_ SUL DISPOSITIVO \_ XML**  
ID risorsa per la stringa "device information" usata nei dati XML.

<span id="XML_IA_STATE"></span><span id="xml_ia_state"></span>**XML \_ IA \_ STATE**  
ID risorsa per la stringa "assembler di input" usata nei dati XML.

<span id="XML_VS_STATE"></span><span id="xml_vs_state"></span>**XML \_ VS \_ STATE**  
ID risorsa per la stringa "vertex shader stage" usata nei dati XML.

<span id="XML_GS_STATE"></span><span id="xml_gs_state"></span>**XML \_ GS \_ STATE**  
ID risorsa per la stringa "fase geometry shader" usata nei dati XML.

<span id="XML_RASTERIZER_STATE"></span><span id="xml_rasterizer_state"></span>**XML \_ RASTERIZER \_ STATE**  
ID risorsa per la stringa "fase di rasterizzazione" usata nei dati XML.

<span id="XML_PS_STATE"></span><span id="xml_ps_state"></span>**XML \_ PS \_ STATE**  
ID risorsa per la stringa "pixel shader fase" usata nei dati XML.

<span id="XML_OM_STATE"></span><span id="xml_om_state"></span>**XML \_ OM \_ STATE**  
ID risorsa per la stringa "output merger stage" usata nei dati XML.

<span id="XML_HS_STATE"></span><span id="xml_hs_state"></span>**XML \_ HS \_ STATE**  
ID risorsa per la stringa "hull shader stage" usata nei dati XML.

<span id="XML_DS_STATE"></span><span id="xml_ds_state"></span>**XML \_ DS \_ STATE**  
ID risorsa per la stringa "fase domain shader" usata nei dati XML.

<span id="XML_CS_STATE"></span><span id="xml_cs_state"></span>**XML \_ CS \_ STATE**  
ID risorsa per la stringa "compute shader stage" usata nei dati XML.

<span id="XML_CLASS_INSTANCE_11"></span><span id="xml_class_instance_11"></span>**ISTANZA DI CLASSE XML \_ \_ \_ 11**  
ID risorsa per la stringa "istanze di classe" usata nei dati XML.

<span id="XML_DEVICE_10"></span><span id="xml_device_10"></span>**DISPOSITIVO \_ XML \_ 10**  
ID risorsa per la stringa "device 10" usata nei dati XML.

<span id="XML_INPUT_LAYOUT_11"></span><span id="xml_input_layout_11"></span>**LAYOUT DI INPUT XML \_ \_ \_ 11**  
ID risorsa per la stringa "layout di input 11" usata nei dati XML.

<span id="XML_SAMPLER_STATE_11"></span><span id="xml_sampler_state_11"></span>**XML \_ SAMPLER \_ STATE \_ 11**  
ID risorsa per la stringa "sampler state 11" usata nei dati XML.

<span id="XML_RASTER_STATE_11"></span><span id="xml_raster_state_11"></span>**XML \_ RASTER \_ STATE \_ 11**  
ID risorsa per la stringa "stato raster 11" usata nei dati XML.

<span id="XML_BLEND_STATE_11"></span><span id="xml_blend_state_11"></span>**XML \_ BLEND \_ STATE \_ 11**  
ID risorsa per la stringa "blend state 11" usata nei dati XML.

<span id="XML_DEPTH_STENCIL_STATE_11"></span><span id="xml_depth_stencil_state_11"></span>**STATO STENCIL PROFONDITÀ XML \_ \_ \_ \_ 11**  
ID risorsa per la stringa "depth stencil stato 11" usata nei dati XML.

<span id="XML_UNORDERED_ACCESS_VIEW_11"></span><span id="xml_unordered_access_view_11"></span>**XML \_ UNORDERED \_ ACCESS VIEW \_ \_ 11**  
ID risorsa per la stringa "vista di accesso non ordinato 11" usata nei dati XML.

<span id="XML_SHADER_RESOURCE_VIEW_11"></span><span id="xml_shader_resource_view_11"></span>**VISUALIZZAZIONE RISORSE SHADER XML \_ \_ \_ \_ 11**  
ID risorsa per la stringa "shader resource view 11" usata nei dati XML.

<span id="XML_DEPTH_STENCIL_VIEW_11"></span><span id="xml_depth_stencil_view_11"></span>**VISUALIZZAZIONE STENCIL PROFONDITÀ XML \_ \_ \_ \_ 11**  
ID risorsa per la stringa "depth stencil vista 11" usata nei dati XML.

<span id="XML_RENDER_TARGET_VIEW_11"></span><span id="xml_render_target_view_11"></span>**VISUALIZZAZIONE DESTINAZIONE RENDERING XML \_ \_ \_ \_ 11**  
ID risorsa per la stringa "render target view 11" usata nei dati XML.

<span id="XML_DEVICE_11"></span><span id="xml_device_11"></span>**DISPOSITIVO XML \_ \_ 11**  
ID risorsa per la stringa "device 11" usata nei dati XML.

<span id="XML_SWAP_CHAIN"></span><span id="xml_swap_chain"></span>**CATENA \_ DI SCAMBIO \_ XML**  
ID risorsa per la stringa "catena di scambio" usata nei dati XML.

<span id="XML_INPUT_LAYOUT_10"></span><span id="xml_input_layout_10"></span>**LAYOUT DI INPUT XML \_ \_ \_ 10**  
ID risorsa per la stringa "layout di input 10" usata nei dati XML.

<span id="XML_SAMPLER_STATE_10"></span><span id="xml_sampler_state_10"></span>**XML \_ SAMPLER \_ STATE \_ 10**  
ID risorsa per la stringa "sampler state 10" usata nei dati XML.

<span id="XML_RASTER_STATE_10"></span><span id="xml_raster_state_10"></span>**XML \_ RASTER \_ STATE \_ 10**  
ID risorsa per la stringa "stato raster 10" usata nei dati XML.

<span id="XML_BLEND_STATE_10"></span><span id="xml_blend_state_10"></span>**XML \_ BLEND \_ STATE \_ 10**  
ID risorsa per la stringa "blend state 10" usata nei dati XML.

<span id="XML_DEPTH_STENCIL_STATE_10"></span><span id="xml_depth_stencil_state_10"></span>**STATO STENCIL PROFONDITÀ XML \_ \_ \_ \_ 10**  
ID risorsa per la stringa "depth stencil 10" usata nei dati XML.

<span id="XML_SHADER_RESOURCE_VIEW_10"></span><span id="xml_shader_resource_view_10"></span>**VISUALIZZAZIONE DELLE RISORSE SHADER XML \_ \_ \_ \_ 10**  
ID risorsa per la stringa "shader resource view 10" usata nei dati XML.

<span id="XML_DEPTH_STENCIL_VIEW_10"></span><span id="xml_depth_stencil_view_10"></span>**VISUALIZZAZIONE STENCIL PROFONDITÀ XML \_ \_ \_ \_ 10**  
ID risorsa per la stringa "depth stencil vista 10" usata nei dati XML.

<span id="XML_RENDER_TARGET_VIEW_10"></span><span id="xml_render_target_view_10"></span>**VISUALIZZAZIONE DESTINAZIONE DI RENDERING XML \_ \_ \_ \_ 10**  
ID risorsa per la stringa "render target view 10" usata nei dati XML.

<span id="XML_VERTEX_DECLARATION"></span><span id="xml_vertex_declaration"></span>**DICHIARAZIONE \_ DI VERTICE \_ XML**  
ID risorsa per la stringa "dichiarazione vertice" usata nei dati XML.

<span id="XML_STATE_BLOCK_STATE_10"></span><span id="xml_state_block_state_10"></span>**XML \_ STATE BLOCK STATE \_ \_ \_ 10**  
ID risorsa per la stringa "state block state 10" usata nei dati XML.

<span id="XML_DEVICE_CONTEXT_STATE_11"></span><span id="xml_device_context_state_11"></span>**XML \_ DEVICE CONTEXT STATE \_ \_ \_ 11**  
ID risorsa per la stringa "device context state 11" usata nei dati XML.

<span id="XML_DEVICE_STATE_10"></span><span id="xml_device_state_10"></span>**XML \_ DEVICE \_ STATE \_ 10**  
ID risorsa per la stringa "device state 10" usata nei dati XML.

<span id="XML_BACK_FACE"></span><span id="xml_back_face"></span>**XML \_ BACK \_ FACE**  
ID risorsa per la stringa "back face" usata nei dati XML.

<span id="XML_FRONT_FACE"></span><span id="xml_front_face"></span>**XML \_ FRONT \_ FACE**  
ID risorsa per la stringa "front face" usata nei dati XML.

<span id="XML_DEVICE_CREATION_INFORMATION"></span><span id="xml_device_creation_information"></span>**INFORMAZIONI \_ SULLA CREAZIONE DI DISPOSITIVI \_ \_ XML**  
ID risorsa per la stringa "device creation information" usata nei dati XML.

<span id="XML_OTHER_INFORMATION"></span><span id="xml_other_information"></span>**ALTRE \_ INFORMAZIONI \_ XML**  
ID risorsa per la stringa "altre informazioni" usata nei dati XML.

<span id="XML_MULTISAMPLING"></span><span id="xml_multisampling"></span>**XML \_ MULTISAMPLING**  
ID risorsa per la stringa "multisampling" usata nei dati XML.

<span id="XML_BACK_BUFFER"></span><span id="xml_back_buffer"></span>**BUFFER NASCOSTO XML \_ \_**  
ID risorsa per la stringa "back buffer" usata nei dati XML.

<span id="XML_GENERAL_INFORMATION"></span><span id="xml_general_information"></span>**INFORMAZIONI \_ GENERALI \_ XML**  
ID risorsa per la stringa "informazioni generali" usata nei dati XML.

<span id="XML_RENDER_TARGETS"></span><span id="xml_render_targets"></span>**DESTINAZIONI \_ DI RENDERING \_ XML**  
ID risorsa per la stringa "render targets" usata nei dati XML.

<span id="XML_BLEND_DESCRIPTION"></span><span id="xml_blend_description"></span>**DESCRIZIONE DI XML \_ \_ BLEND**  
ID risorsa per la stringa "blend description" usata nei dati XML.

<span id="XML_DEPTH_STENCIL"></span><span id="xml_depth_stencil"></span>**\_STENCIL DI PROFONDITÀ \_ XML**  
ID risorsa per la stringa "depth stencil" usata nei dati XML.

<span id="XML_FRONT_FACE_STENCIL"></span><span id="xml_front_face_stencil"></span>**XML \_ FRONT \_ FACE \_ STENCIL**  
ID risorsa per la stringa "stencil front face" usata nei dati XML.

<span id="XML_BACK_FACE_STENCIL"></span><span id="xml_back_face_stencil"></span>**XML \_ BACK \_ FACE \_ STENCIL**  
ID risorsa per la stringa "back face stencil" usata nei dati XML.

<span id="XML_DEVICE1_CREATION_INFORMATION"></span><span id="xml_device1_creation_information"></span>**INFORMAZIONI SULLA \_ CREAZIONE DI XML DEVICE1 \_ \_**  
ID risorsa per la stringa "device1 creation information" usata nei dati XML.

<span id="XML_VIEWPORTS"></span><span id="xml_viewports"></span>**VIEWPORT \_ XML**  
ID risorsa per la stringa "viewports" usata nei dati XML.

<span id="XML_SCISSOR_RECTS"></span><span id="xml_scissor_rects"></span>**\_RETTALI DI SCISSOR \_ XML**  
ID risorsa per la stringa "rettali di forbice" usata nei dati XML.

<span id="XML_RASTERIZER"></span><span id="xml_rasterizer"></span>**\_RASTERIZZAZIONE XML**  
ID risorsa per la stringa "rasterizer" usata nei dati XML.

<span id="XML_SHADER_GENERAL_INFORMATION"></span><span id="xml_shader_general_information"></span>**INFORMAZIONI GENERALI SU XML \_ \_ \_ SHADER**  
ID risorsa per la stringa "informazioni generali shader" usata nei dati XML.

<span id="XML_SHADER_RESOURCES"></span><span id="xml_shader_resources"></span>**RISORSE \_ DI SHADER \_ XML**  
ID risorsa per la stringa "risorse shader" usata nei dati XML.

<span id="XML_SAMPLER_STATES"></span><span id="xml_sampler_states"></span>**STATI \_ DEL SAMPLER \_ XML**  
ID risorsa per la stringa "sampler states" usata nei dati XML.

<span id="XML_CONSTANT_BUFFERS"></span><span id="xml_constant_buffers"></span>**BUFFER COSTANTI XML \_ \_**  
ID risorsa per la stringa "constant buffers" usata nei dati XML.

<span id="XML_STREAM_OUTPUT_BUFFERS"></span><span id="xml_stream_output_buffers"></span>**BUFFER \_ DI OUTPUT DEL FLUSSO \_ \_ XML**  
ID risorsa per la stringa "buffer di output di flusso" usata nei dati XML.

<span id="XML_INPUT_LAYOUT_GENERAL_INFORMATION"></span><span id="xml_input_layout_general_information"></span>**INFORMAZIONI \_ GENERALI SUL LAYOUT DI INPUT \_ \_ \_ XML**  
ID risorsa per la stringa "informazioni generali sul layout di input" usata nei dati XML.

<span id="XML_INPUT_LAYOUTS"></span><span id="xml_input_layouts"></span>**LAYOUT \_ DI INPUT \_ XML**  
ID risorsa per la stringa "layout di input" usata nei dati XML.

<span id="XML_VERTEX_BUFFERS"></span><span id="xml_vertex_buffers"></span>**BUFFER \_ DEI VERTICI \_ XML**  
ID risorsa per la stringa "vertex buffer" usata nei dati XML.

<span id="XML_INDEX_BUFFERS"></span><span id="xml_index_buffers"></span>**BUFFER \_ DI INDICE \_ XML**  
ID risorsa per la stringa "buffer di indice" usata nei dati XML.

<span id="XML_PRIMITIVE_TOP_GENERAL"></span><span id="xml_primitive_top_general"></span>**XML \_ PRIMITIVE \_ TOP \_ GENERAL**  
ID risorsa per la stringa "primitive top general" usata nei dati XML.

<span id="XML_BLEND_STATE"></span><span id="xml_blend_state"></span>**STATO DI XML \_ \_ BLEND**  
ID risorsa per la stringa "blend state" usata nei dati XML.

<span id="XML_UNORDERED_ACCESS_VIEWS"></span><span id="xml_unordered_access_views"></span>**VISTE DI ACCESSO NON \_ \_ \_ ORDINATE XML**  
ID risorsa per la stringa "viste di accesso non ordinate" usata nei dati XML.

<span id="XML_CLASS_INSTANCES"></span><span id="xml_class_instances"></span>**ISTANZE \_ DELLA CLASSE \_ XML**  
ID risorsa per la stringa "istanze di classe" usata nei dati XML.

<span id="XML_INSTANCE_INDEX"></span><span id="xml_instance_index"></span>**INDICE \_ DELL'ISTANZA \_ XML**  
ID risorsa per la stringa "instance index" usata nei dati XML.

<span id="XML_INSTANCE_NAME"></span><span id="xml_instance_name"></span>**NOME \_ ISTANZA \_ XML**  
ID risorsa per la stringa "nome istanza" usata nei dati XML.

<span id="XML_BASE_SAMPLER"></span><span id="xml_base_sampler"></span>**SAMPLER \_ DI BASE \_ XML**  
ID risorsa per la stringa "base sampler" usata nei dati XML.

<span id="XML_BASE_TEXTURE"></span><span id="xml_base_texture"></span>**TRAMA \_ DI BASE \_ XML**  
ID risorsa per la stringa "trama di base" usata nei dati XML.

<span id="XML_BASE_CONSTANT_BUFFER_OFFSET"></span><span id="xml_base_constant_buffer_offset"></span>**OFFSET \_ DEL BUFFER COSTANTE DI BASE \_ \_ \_ XML**  
ID risorsa per la stringa "base constant buffer offset" usata nei dati XML.

<span id="XML_CONSTANT_BUFFER"></span><span id="xml_constant_buffer"></span>**BUFFER COSTANTE XML \_ \_**  
ID risorsa per la stringa "constant buffer" usata nei dati XML.

<span id="XML_TYPE_ID"></span><span id="xml_type_id"></span>**ID TIPO XML \_ \_**  
ID risorsa per la stringa "type id" usata nei dati XML.

<span id="XML_INSTANCE_ID"></span><span id="xml_instance_id"></span>**ID ISTANZA XML \_ \_**  
ID risorsa per la stringa "id istanza" usata nei dati XML.

<span id="XML_TYPE_NAME"></span><span id="xml_type_name"></span>**NOME \_ DEL TIPO \_ XML**  
ID risorsa per la stringa "type name" usata nei dati XML.

<span id="XML_CREATED_BY"></span><span id="xml_created_by"></span>**XML \_ CREATO \_ DA**  
ID risorsa per la stringa "creata da" usata nei dati XML.

<span id="XML_CLASS_LINKAGE"></span><span id="xml_class_linkage"></span>**COLLEGAMENTO \_ DI CLASSI XML \_**  
ID risorsa per la stringa "class linkage" usata nei dati XML.

<span id="XML_DESCRIPTION"></span><span id="xml_description"></span>**DESCRIZIONE \_ XML**  
ID risorsa per la stringa "description" usata nei dati XML.

<span id="XML_SEMANTIC_NAME"></span><span id="xml_semantic_name"></span>**NOME \_ \_ SEMANTICO XML**  
ID risorsa per la stringa "nome semantico" usato nei dati XML.

<span id="XML_SEMANTIC_INDEX"></span><span id="xml_semantic_index"></span>**INDICE \_ \_ SEMANTICO XML**  
ID risorsa per la stringa "indice semantico" usato nei dati XML.

<span id="XML_FORMAT"></span><span id="xml_format"></span>**FORMATO \_ XML**  
ID risorsa per la stringa "format" usata nei dati XML.

<span id="XML_INPUT_SLOT"></span><span id="xml_input_slot"></span>**SLOT DI INPUT XML \_ \_**  
ID risorsa per la stringa "slot di input" usata nei dati XML.

<span id="XML_ALIGNED_BYTE_OFFSET"></span><span id="xml_aligned_byte_offset"></span>**OFFSET \_ BYTE ALLINEATO \_ \_ XML**  
ID risorsa per la stringa "aligned byte offset" usata nei dati XML.

<span id="XML_INPUT_SLOT_CLASS"></span><span id="xml_input_slot_class"></span>**CLASSE \_ SLOT DI INPUT \_ \_ XML**  
ID risorsa per la stringa "classe slot di input" usata nei dati XML.

<span id="XML_INSTANCE_DATA_STEP_RATE"></span><span id="xml_instance_data_step_rate"></span>**FREQUENZA \_ \_ PASSAGGIO DATI ISTANZA \_ \_ XML**  
ID risorsa per la stringa "instance data step rate" usata nei dati XML.

<span id="XML_ADDRESS_U"></span><span id="xml_address_u"></span>**INDIRIZZO XML \_ \_ U**  
ID risorsa per la stringa "address u" usata nei dati XML.

<span id="XML_ADDRESS_V"></span><span id="xml_address_v"></span>**INDIRIZZO XML \_ \_ V**  
ID risorsa per la stringa "address v" usata nei dati XML.

<span id="XML_ADDRESS_W"></span><span id="xml_address_w"></span>**INDIRIZZO XML \_ \_ W**  
ID risorsa per la stringa "address w" usata nei dati XML.

<span id="XML_MIN_LOD"></span><span id="xml_min_lod"></span>**XML \_ MIN \_ LOD**  
ID risorsa per la stringa "min lod" usata nei dati XML.

<span id="XML_MAX_LOD"></span><span id="xml_max_lod"></span>**XML \_ MAX \_ LOD**  
ID risorsa per la stringa "max lod" usata nei dati XML.

<span id="XML_FILTER"></span><span id="xml_filter"></span>**FILTRO \_ XML**  
ID risorsa per la stringa "filter" usata nei dati XML.

<span id="XML_MIP_SLICE"></span><span id="xml_mip_slice"></span>**SEZIONE \_ MIP \_ XML**  
ID risorsa per la stringa "sezione mip" usata nei dati XML.

<span id="XML_MOST_DETAILED_MIP"></span><span id="xml_most_detailed_mip"></span>**MIP \_ XML \_ PIÙ \_ DETTAGLIATO**  
ID risorsa per la stringa "mip più dettagliato" usato nei dati XML.

<span id="XML_MIP_LEVELS"></span><span id="xml_mip_levels"></span>**LIVELLI \_ MIP \_ XML**  
ID risorsa per la stringa "livelli mip" usata nei dati XML.

<span id="XML_FIRST_ARRAY_SLICE"></span><span id="xml_first_array_slice"></span>**SEZIONE \_ XML FIRST \_ \_ ARRAY**  
ID risorsa per la stringa "first array slice" usata nei dati XML.

<span id="XML_ARRAY_SIZE"></span><span id="xml_array_size"></span>**DIMENSIONI \_ DELLA MATRICE \_ XML**  
ID risorsa per la stringa "dimensione matrice" usata nei dati XML.

<span id="XML_RESOURCE"></span><span id="xml_resource"></span>**RISORSA \_ XML**  
ID risorsa per la stringa "resource" usata nei dati XML.

<span id="XML_RESOURCE_TYPE"></span><span id="xml_resource_type"></span>**TIPO \_ DI RISORSA \_ XML**  
ID risorsa per la stringa "resource type" usata nei dati XML.

<span id="XML_ELEMENT_OFFSET"></span><span id="xml_element_offset"></span>**OFFSET \_ \_ DELL'ELEMENTO XML**  
ID risorsa per la stringa "element offset" usata nei dati XML.

<span id="XML_ELEMENT_WIDTH"></span><span id="xml_element_width"></span>**LARGHEZZA \_ ELEMENTO \_ XML**  
ID risorsa per la stringa "larghezza elemento" usata nei dati XML.

<span id="XML_FIRST_WITH_SLICE"></span><span id="xml_first_with_slice"></span>**XML \_ FIRST \_ WITH \_ SLICE**  
ID risorsa per la stringa "first with slice" usata nei dati XML.

<span id="XML_BORDER_COLOR"></span><span id="xml_border_color"></span>**COLORE \_ BORDO \_ XML**  
ID risorsa per la stringa "colore bordo" utilizzata nei dati XML.

<span id="XML_SLOT"></span><span id="xml_slot"></span>**XML \_ SLOT**  
ID risorsa per la stringa "slot" usata nei dati XML.

<span id="XML_COMPARISON_FUNCTION"></span><span id="xml_comparison_function"></span>**FUNZIONE \_ DI CONFRONTO \_ XML**  
ID risorsa per la stringa "funzione di confronto" usata nei dati XML.

<span id="XML_MAX_ANISOTROPY"></span><span id="xml_max_anisotropy"></span>**XML \_ MAX \_ ANISOTROPY**  
ID risorsa per la stringa "max anisotropy" usata nei dati XML.

<span id="XML_MIP_LOD_BIAS"></span><span id="xml_mip_lod_bias"></span>**XML \_ MIP \_ LOD \_ BIAS**  
ID risorsa per la stringa "mip lod bias" usata nei dati XML.

<span id="XML_FILL_MODE"></span><span id="xml_fill_mode"></span>**MODALITÀ \_ RIEMPIMENTO \_ XML**  
ID risorsa per la stringa "modalità di riempimento" usata nei dati XML.

<span id="XML_CULL_MODE"></span><span id="xml_cull_mode"></span>**MODALITÀ \_ CULL \_ XML**  
ID risorsa per la stringa "cull mode" usata nei dati XML.

<span id="XML_FRONT_COUNTER_CLOCKWISE"></span><span id="xml_front_counter_clockwise"></span>**XML \_ FRONT \_ COUNTER \_ CLOCKWISE**  
ID risorsa per la stringa "front counterwise" usata nei dati XML.

<span id="XML_DEPTH_BIAS"></span><span id="xml_depth_bias"></span>**DISTORSIONE \_ DELLA PROFONDITÀ \_ XML**  
ID risorsa per la stringa "distorsione profondità" usata nei dati XML.

<span id="XML_DEPTH_BIAS_CLAMP"></span><span id="xml_depth_bias_clamp"></span>**XML \_ DEPTH \_ BIAS \_ CLAMP**  
ID risorsa per la stringa "depth bias clamp" usata nei dati XML.

<span id="XML_SLOPE_SCALE_DEPTH_BIAS"></span><span id="xml_slope_scale_depth_bias"></span>**DISTORSIONE \_ DELLA PROFONDITÀ DELLA SCALA DEL \_ \_ INCLINAZIONE \_ XML**  
ID risorsa per la stringa "distorsione della profondità della scala del inclinazione" usata nei dati XML.

<span id="XML_DEPTH_CLIP_ENABLE"></span><span id="xml_depth_clip_enable"></span>**XML \_ DEPTH \_ CLIP \_ ENABLE**  
ID risorsa per la stringa "depth clip enable" usata nei dati XML.

<span id="XML_SCISSOR_ENABLE"></span><span id="xml_scissor_enable"></span>**XML \_ SCISSOR \_ ENABLE**  
ID risorsa per la stringa "scissor enable" usata nei dati XML.

<span id="XML_MULTISAMPLE_ENABLE"></span><span id="xml_multisample_enable"></span>**ABILITAZIONE \_ DI MULTISAMPLE \_ XML**  
ID risorsa per la stringa "multisample enable" usata nei dati XML.

<span id="XML_ANTIALIASED_LINE_ENABLE"></span><span id="xml_antialiased_line_enable"></span>**XML \_ ANTIALIASED \_ LINE \_ ENABLE**  
ID risorsa per la stringa "antialiased line enable" usata nei dati XML.

<span id="XML_FUNCTION"></span><span id="xml_function"></span>**XML \_ FUNCTION**  
ID risorsa per la stringa "function" usata nei dati XML.

<span id="XML_PASS_OP"></span><span id="xml_pass_op"></span>**XML \_ PASS \_ OP**  
ID risorsa per la stringa "pass op" usata nei dati XML.

<span id="XML_FAIL_OP"></span><span id="xml_fail_op"></span>**XML \_ FAIL \_ OP**  
ID risorsa per la stringa "fail op" usata nei dati XML.

<span id="XML_DEPTH_FAIL_OP"></span><span id="xml_depth_fail_op"></span>**XML \_ DEPTH \_ FAIL \_ OP**  
ID risorsa per la stringa "depth fail op" usata nei dati XML.

<span id="XML_DEPTH_ENABLE"></span><span id="xml_depth_enable"></span>**XML \_ DEPTH \_ ENABLE**  
ID risorsa per la stringa "depth enable" usata nei dati XML.

<span id="XML_DEPTH_FUNCTION"></span><span id="xml_depth_function"></span>**FUNZIONE \_ XML DEPTH \_**  
ID risorsa per la stringa "depth function" usata nei dati XML.

<span id="XML_DEPTH_WRITE_MASK"></span><span id="xml_depth_write_mask"></span>**XML \_ DEPTH \_ WRITE \_ MASK**  
ID risorsa per la stringa "depth write mask" usata nei dati XML.

<span id="XML_STENCIL_ENABLE"></span><span id="xml_stencil_enable"></span>**ABILITAZIONE \_ STENCIL \_ XML**  
ID risorsa per la stringa "stencil enable" usata nei dati XML.

<span id="XML_STENCIL_READ_MASK"></span><span id="xml_stencil_read_mask"></span>**MASCHERA DI \_ \_ LETTURA STENCIL XML \_**  
ID risorsa per la stringa "stencil read mask" usata nei dati XML.

<span id="XML_STENCIL_WRITE_MASK"></span><span id="xml_stencil_write_mask"></span>**MASCHERA DI \_ SCRITTURA \_ STENCIL \_ XML**  
ID risorsa per la stringa "stencil write mask" usata nei dati XML.

<span id="XML_FIRST_ELEMENT"></span><span id="xml_first_element"></span>**PRIMO \_ ELEMENTO \_ XML**  
ID risorsa per la stringa "primo elemento" usata nei dati XML.

<span id="XML_NUM_ELEMENTS"></span><span id="xml_num_elements"></span>**ELEMENTI \_ NUM \_ XML**  
ID risorsa per la stringa "num elements" usata nei dati XML.

<span id="XML_FLAGS"></span><span id="xml_flags"></span>**FLAG \_ XML**  
ID risorsa per la stringa "flags" usata nei dati XML.

<span id="XML_W_SIZE"></span><span id="xml_w_size"></span>**XML \_ W \_ SIZE**  
ID risorsa per la stringa "w size" usata nei dati XML.

<span id="XML_FIRST_2D_ARRAY_FACE"></span><span id="xml_first_2d_array_face"></span>**XML \_ FIRST \_ 2D \_ ARRAY \_ FACE**  
ID risorsa per la stringa "first 2d array face" usata nei dati XML.

<span id="XML_NUM_CUBES"></span><span id="xml_num_cubes"></span>**CUBI \_ NUM \_ XML**  
ID risorsa per la stringa "num cubes" usata nei dati XML.

<span id="XML_FACTORY"></span><span id="xml_factory"></span>**XML \_ FACTORY**  
ID risorsa per la stringa "factory" usata nei dati XML.

<span id="XML_ADAPTER"></span><span id="xml_adapter"></span>**XML \_ ADAPTER**  
ID risorsa per la stringa "adapter" usata nei dati XML.

<span id="XML_HEIGHT"></span><span id="xml_height"></span>**ALTEZZA \_ XML**  
ID risorsa per la stringa "height" usata nei dati XML.

<span id="XML_WIDTH"></span><span id="xml_width"></span>**LARGHEZZA \_ XML**  
ID risorsa per la stringa "width" usata nei dati XML.

<span id="XML_CREATION_FLAGS"></span><span id="xml_creation_flags"></span>**FLAG \_ DI CREAZIONE \_ XML**  
ID risorsa per la stringa "flag di creazione" usata nei dati XML.

<span id="XML_SDK_VERSION"></span><span id="xml_sdk_version"></span>**VERSIONE \_ DELL'SDK \_ XML**  
ID risorsa per la stringa "versione sdk" usata nei dati XML.

<span id="XML_DRIVER_TYPE"></span><span id="xml_driver_type"></span>**TIPO \_ DI DRIVER \_ XML**  
ID risorsa per la stringa "tipo di driver" usata nei dati XML.

<span id="XML_FEATURE_LEVEL"></span><span id="xml_feature_level"></span>**LIVELLO \_ DI FUNZIONALITÀ \_ XML**  
ID risorsa per la stringa "feature level" usata nei dati XML.

<span id="XML_IMMEDIATE_CONTEXT"></span><span id="xml_immediate_context"></span>**CONTESTO \_ IMMEDIATO \_ XML**  
ID risorsa per la stringa "contesto immediato" usata nei dati XML.

<span id="XML_EXCEPTION_MODE"></span><span id="xml_exception_mode"></span>**MODALITÀ \_ DI ECCEZIONE \_ XML**  
ID risorsa per la stringa "exception mode" usata nei dati XML.

<span id="XML_REFRESH_RATE"></span><span id="xml_refresh_rate"></span>**FREQUENZA \_ DI AGGIORNAMENTO \_ XML**  
ID risorsa per la stringa "frequenza di aggiornamento" usata nei dati XML.

<span id="XML_SCANLINE_ORDERING"></span><span id="xml_scanline_ordering"></span>**ORDINAMENTO \_ SCANLINE \_ XML**  
ID risorsa per la stringa "scanline ordering" usata nei dati XML.

<span id="XML_SCALING"></span><span id="xml_scaling"></span>**SCALABILITÀ \_ XML**  
ID risorsa per la stringa "scaling" usata nei dati XML.

<span id="XML_USAGE"></span><span id="xml_usage"></span>**UTILIZZO \_ XML**  
ID risorsa per la stringa "usage" usata nei dati XML.

<span id="XML_BUFFER_COUNT"></span><span id="xml_buffer_count"></span>**CONTEGGIO \_ BUFFER \_ XML**  
ID risorsa per la stringa "buffer count" usata nei dati XML.

<span id="XML_WINDOWED"></span><span id="xml_windowed"></span>**XML \_ CON FINESTRA**  
ID risorsa per la stringa "windowed" usata nei dati XML.

<span id="XML_SWAP_EFFECT"></span><span id="xml_swap_effect"></span>**EFFETTO \_ DI SCAMBIO \_ XML**  
ID risorsa per la stringa "effetto swap" usata nei dati XML.

<span id="XML_STREAM"></span><span id="xml_stream"></span>**FLUSSO \_ XML**  
ID risorsa per la stringa "stream" usata nei dati XML.

<span id="XML_OFFSET"></span><span id="xml_offset"></span>**XML \_ OFFSET**  
ID risorsa per la stringa "offset" usata nei dati XML.

<span id="XML_TYPE"></span><span id="xml_type"></span>**TIPO \_ XML**  
ID risorsa per la stringa "type" usata nei dati XML.

<span id="XML_METHOD"></span><span id="xml_method"></span>**METODO \_ XML**  
ID risorsa per la stringa "method" usata nei dati XML.

<span id="XML_USAGE_INDEX"></span><span id="xml_usage_index"></span>**INDICE \_ DI UTILIZZO \_ XML**  
ID risorsa per la stringa "usage index" usata nei dati XML.

<span id="XML_BLEND_SOURCE"></span><span id="xml_blend_source"></span>**ORIGINE XML \_ \_ BLEND**  
ID risorsa per la stringa "blend source" usata nei dati XML.

<span id="XML_BLEND_DESTINATION"></span><span id="xml_blend_destination"></span>**DESTINAZIONE XML \_ \_ BLEND**  
ID risorsa per la stringa "blend destination" usata nei dati XML.

<span id="XML_BLEND_OP"></span><span id="xml_blend_op"></span>**XML \_ BLEND \_ OP**  
ID risorsa per la stringa "blend op" usata nei dati XML.

<span id="XML_ALPHA_BLEND_SOURCE"></span><span id="xml_alpha_blend_source"></span>**ORIGINE \_ DI XML ALPHA \_ \_ BLEND**  
ID risorsa per la stringa "alpha blend source" usata nei dati XML.

<span id="XML_ALPHA_BLEND_DEST"></span><span id="xml_alpha_blend_dest"></span>**DEST \_ DI XML ALPHA \_ BLEND \_**  
ID risorsa per la stringa "alpha blend dest" usata nei dati XML.

<span id="XML_ALPHA_BLEND_OP"></span><span id="xml_alpha_blend_op"></span>**XML \_ ALPHA \_ BLEND \_ OP**  
ID risorsa per la stringa "alpha blend op" usata nei dati XML.

<span id="XML_RENDER_TARGET_WRITE_MASK"></span><span id="xml_render_target_write_mask"></span>**MASCHERA \_ DI SCRITTURA DELLA DESTINAZIONE DI RENDERING \_ \_ \_ XML**  
ID risorsa per la stringa "render target write mask" usata nei dati XML.

<span id="XML_BLEND_ENABLE"></span><span id="xml_blend_enable"></span>**ABILITAZIONE DI XML \_ \_ BLEND**  
ID risorsa per la stringa "blend enable" usata nei dati XML.

<span id="XML_ALPHA_TO_COVERAGE_ENABLE"></span><span id="xml_alpha_to_coverage_enable"></span>**ABILITAZIONE \_ DA ALFA XML A CODE \_ \_ \_ COVERAGE**  
ID risorsa per la stringa "alpha to coverage enable" usata nei dati XML.

<span id="XML_BLEND_STATE_1"></span><span id="xml_blend_state_1"></span>**XML \_ BLEND \_ STATE \_ 1**  
ID risorsa per la stringa "blend state 1" usata nei dati XML.

<span id="XML_INDEPENDENT_BLEND_ENABLE"></span><span id="xml_independent_blend_enable"></span>**ABILITAZIONE DI XML \_ INDEPENDENT \_ BLEND \_**  
ID risorsa per la stringa "independent blend enable" usata nei dati XML.

<span id="XML_RENDER_TARGET_NUMBER"></span><span id="xml_render_target_number"></span>**NUMERO \_ DI DESTINAZIONE DI RENDERING \_ \_ XML**  
ID risorsa per la stringa "render target number" usata nei dati XML.

<span id="XML_BUFFER"></span><span id="xml_buffer"></span>**XML \_ BUFFER**  
ID risorsa per la stringa "buffer" usata nei dati XML.

<span id="XML_PRIMITIVE_TOPOLOGY"></span><span id="xml_primitive_topology"></span>**TOPOLOGIA \_ PRIMITIVA \_ XML**  
ID risorsa per la stringa "topologia primitiva" usata nei dati XML.

<span id="XML_STRIDE"></span><span id="xml_stride"></span>**XML \_ STRIDE**  
ID risorsa per la stringa "stride" usata nei dati XML.

<span id="XML_TOP"></span><span id="xml_top"></span>**XML \_ TOP**  
ID risorsa per la stringa "top" usata nei dati XML.

<span id="XML_RIGHT"></span><span id="xml_right"></span>**XML \_ RIGHT**  
ID risorsa per la stringa "right" usata nei dati XML.

<span id="XML_BOTTOM"></span><span id="xml_bottom"></span>**XML \_ INFERIORE**  
ID risorsa per la stringa "bottom" usata nei dati XML.

<span id="XML_LEFT"></span><span id="xml_left"></span>**XML \_ A SINISTRA**  
ID risorsa per la stringa "left" usata nei dati XML.

<span id="XML_TOP_LEFT_X"></span><span id="xml_top_left_x"></span>**XML \_ IN ALTO A SINISTRA \_ \_ X**  
ID risorsa per la stringa "top left x" usata nei dati XML.

<span id="XML_TOP_LEFT_Y"></span><span id="xml_top_left_y"></span>**XML \_ IN ALTO A SINISTRA \_ \_ Y**  
ID risorsa per la stringa "top left y" usata nei dati XML.

<span id="XML_MIN_DEPTH"></span><span id="xml_min_depth"></span>**PROFONDITÀ \_ MINIMA \_ XML**  
ID risorsa per la stringa "min depth" usata nei dati XML.

<span id="XML_MAX_DEPTH"></span><span id="xml_max_depth"></span>**PROFONDITÀ \_ MASSIMA \_ XML**  
ID risorsa per la stringa "max depth" usata nei dati XML.

<span id="XML_STENCIL_REFERENCE_VALUE"></span><span id="xml_stencil_reference_value"></span>**VALORE DI \_ RIFERIMENTO DELLO STENCIL \_ \_ XML**  
ID risorsa per la stringa "valore di riferimento dello stencil" usato nei dati XML.

<span id="XML_DEPTH_BUFFER"></span><span id="xml_depth_buffer"></span>**BUFFER \_ DI PROFONDITÀ \_ XML**  
ID risorsa per la stringa "depth buffer" usata nei dati XML.

<span id="XML_BLEND_FACTOR"></span><span id="xml_blend_factor"></span>**FATTORE \_ DI BLEND \_ XML**  
ID risorsa per la stringa "blend factor" usata nei dati XML.

<span id="XML_SAMPLE_MASK"></span><span id="xml_sample_mask"></span>**MASCHERA \_ DI ESEMPIO \_ XML**  
ID risorsa per la stringa "maschera di esempio" usata nei dati XML.

<span id="XML_DEPTH_STENCIL_STATE"></span><span id="xml_depth_stencil_state"></span>**STATO \_ DELLO STENCIL DI PROFONDITÀ \_ \_ XML**  
ID risorsa per la stringa "depth stencil stato" usato nei dati XML.

<span id="XML_SCISSOR_RECT"></span><span id="xml_scissor_rect"></span>**RETTA \_ DI SCISSOR \_ XML**  
ID risorsa per la stringa "scissor rect" usata nei dati XML.

<span id="XML_SAMPLER"></span><span id="xml_sampler"></span>**XML \_ SAMPLER**  
ID risorsa per la stringa "sampler" usata nei dati XML.

<span id="XML_SHADER"></span><span id="xml_shader"></span>**XML \_ SHADER**  
ID risorsa per la stringa "shader" usata nei dati XML.

<span id="XML_DEPTH_STENCIL_VIEW"></span><span id="xml_depth_stencil_view"></span>**VISUALIZZAZIONE \_ DEGLI STENCIL DI PROFONDITÀ \_ \_ XML**  
ID risorsa per la stringa "depth stencil visualizzazione" usata nei dati XML.

<span id="XML_SHADER_RESOURCE_VIEW"></span><span id="xml_shader_resource_view"></span>**VISUALIZZAZIONE \_ DELLE RISORSE DELLO SHADER \_ \_ XML**  
ID risorsa per la stringa "visualizzazione risorse shader" usata nei dati XML.

<span id="XML_INPUT_LAYOUT"></span><span id="xml_input_layout"></span>**LAYOUT \_ DI INPUT \_ XML**  
ID risorsa per la stringa "layout di input" usata nei dati XML.

<span id="XML_CLASS_INSTANCE"></span><span id="xml_class_instance"></span>**ISTANZA \_ DELLA CLASSE \_ XML**  
ID risorsa per la stringa "istanza di classe" usata nei dati XML.

<span id="XML_STREAM_OUTPUT_BUFFER"></span><span id="xml_stream_output_buffer"></span>**BUFFER \_ DI OUTPUT DEL \_ FLUSSO \_ XML**  
ID risorsa per la stringa "stream output buffer" usata nei dati XML.

<span id="XML_SHADER_RESOURCE"></span><span id="xml_shader_resource"></span>**RISORSA SHADER \_ \_ XML**  
ID risorsa per la stringa "risorsa shader" usata nei dati XML.

<span id="XML_RENDER_TARGET_INSTANCE"></span><span id="xml_render_target_instance"></span>**ISTANZA \_ DI DESTINAZIONE DI RENDERING \_ \_ XML**  
ID risorsa per la stringa "render target instance" usata nei dati XML.

<span id="XML_VERTEX"></span><span id="xml_vertex"></span>**VERTICE \_ XML**  
ID risorsa per la stringa "vertex" usata nei dati XML.

<span id="XML_VERTEX_BUFFER"></span><span id="xml_vertex_buffer"></span>**XML \_ VERTEX \_ BUFFER**  
ID risorsa per la stringa "vertex buffer" usata nei dati XML.

<span id="XML_INSTANCE"></span><span id="xml_instance"></span>**ISTANZA \_ XML**  
ID risorsa per la stringa "istanza" usata nei dati XML.

<span id="XML_VIEWPORT"></span><span id="xml_viewport"></span>**XML \_ VIEWPORT**  
ID risorsa per la stringa "viewport" usata nei dati XML.

<span id="XML_FIRSTCONSTANT"></span><span id="xml_firstconstant"></span>**XML \_ FIRSTCONSTANT**  
ID risorsa per la stringa "firstconstant" usata nei dati XML.

<span id="XML_NUMCONSTANTS"></span><span id="xml_numconstants"></span>**XML \_ NUMCONSTANTS**  
ID risorsa per la stringa "numconstants" usata nei dati XML.

<span id="XML_LOGIC_OP"></span><span id="xml_logic_op"></span>**OPERAZIONI \_ PER LA LOGICA \_ XML**  
ID risorsa per la stringa "logic op" usata nei dati XML.

<span id="XML_LOGICOPS_ENABLE"></span><span id="xml_logicops_enable"></span>**XML \_ LOGICOPS \_ ENABLE**  
ID risorsa per la stringa "logicops enable" usata nei dati XML.

<span id="XML_SHADER_FILE"></span><span id="xml_shader_file"></span>**XML \_ SHADER \_ FILE**  
ID risorsa per la stringa "file shader" usata nei dati XML.

<span id="XML_FILE_NAME"></span><span id="xml_file_name"></span>**NOME \_ FILE \_ XML**  
ID risorsa per la stringa "nome file" usata nei dati XML.

<span id="XML_10TEXTURE1D_DESC"></span><span id="xml_10texture1d_desc"></span>**XML \_ 10TEXTURE1D \_ DESC**  
ID risorsa per la stringa "10texture1d desc" usata nei dati XML.

<span id="XML_10TEXTURE2D_DESC"></span><span id="xml_10texture2d_desc"></span>**XML \_ 10TEXTURE2D \_ DESC**  
ID risorsa per la stringa "10texture2d desc" usata nei dati XML.

<span id="XML_10TEXTURE3D_DESC"></span><span id="xml_10texture3d_desc"></span>**XML \_ 10TEXTURE3D \_ DESC**  
ID risorsa per la stringa "10texture3d desc" usata nei dati XML.

<span id="XML_TEXTURE_ARRAYSIZE"></span><span id="xml_texture_arraysize"></span>**ARRAYSIZE TRAMA XML \_ \_**  
ID risorsa per la stringa "texture arraysize" usata nei dati XML.

<span id="XML_TEXTURE_MIPLEVEL"></span><span id="xml_texture_miplevel"></span>**TRAMA XML \_ \_ MIPLEVEL**  
ID risorsa per la stringa "texture miplevel" usata nei dati XML.

<span id="XML_CPUFLAGS"></span><span id="xml_cpuflags"></span>**XML \_ CPUFLAGS**  
ID risorsa per la stringa "cpuflags" usata nei dati XML.

<span id="XML_MISCFLAGS"></span><span id="xml_miscflags"></span>**XML \_ MISCFLAGS**  
ID risorsa per la stringa "miscflags" usata nei dati XML.

<span id="XML_BINDFLAGS"></span><span id="xml_bindflags"></span>**XML \_ BINDFLAGS**  
ID risorsa per la stringa "bindflags" usata nei dati XML.

<span id="XML_SAMPLEDESC_QUALITY"></span><span id="xml_sampledesc_quality"></span>**ESEMPIO XML \_ \_ QUALITYDESC**  
ID risorsa per la stringa "sampledesc quality" usata nei dati XML.

<span id="XML_SAMPLEDESC_COUNT"></span><span id="xml_sampledesc_count"></span>**CONTEGGIO \_ SAMPLEDESC \_ XML**  
ID risorsa per la stringa "sampledesc count" usata nei dati XML.

<span id="XML_DEPTH"></span><span id="xml_depth"></span>**PROFONDITÀ \_ XML**  
ID risorsa per la stringa "depth" usata nei dati XML.

<span id="XML_11TEXTURE1D_DESC"></span><span id="xml_11texture1d_desc"></span>**XML \_ 11TEXTURE1D \_ DESC**  
ID risorsa per la stringa "11texture1d desc" usata nei dati XML.

<span id="XML_11TEXTURE2D_DESC"></span><span id="xml_11texture2d_desc"></span>**XML \_ 11TEXTURE2D \_ DESC**  
ID risorsa per la stringa "11texture2d desc" usata nei dati XML.

<span id="XML_11TEXTURE3D_DESC"></span><span id="xml_11texture3d_desc"></span>**XML \_ 11TEXTURE3D \_ DESC**  
ID risorsa per la stringa "11texture3d desc" usata nei dati XML.

<span id="XML_BYTEWIDTH"></span><span id="xml_bytewidth"></span>**XML \_ BYTEWIDTH**  
ID risorsa per la stringa "bytewidth" usata nei dati XML.

<span id="XML_10BUFFER_DESC"></span><span id="xml_10buffer_desc"></span>**XML \_ 10BUFFER \_ DESC**  
ID risorsa per la stringa "10buffer desc" usata nei dati XML.

<span id="XML_11BUFFER_DESC"></span><span id="xml_11buffer_desc"></span>**XML \_ 11BUFFER \_ DESC**  
ID risorsa per la stringa "11buffer desc" usata nei dati XML.

<span id="XML_BYTESTRIDE"></span><span id="xml_bytestride"></span>**XML \_ BYTESTRIDE**  
ID risorsa per la stringa "bytestride" usata nei dati XML.

<span id="XML_STEREO"></span><span id="xml_stereo"></span>**XML \_ STEREO**  
ID risorsa per la stringa "stereo" usata nei dati XML.

<span id="XML_BACKGROUNDCOLOR"></span><span id="xml_backgroundcolor"></span>**COLORE \_ DI SFONDO XML**  
ID risorsa per la stringa "backgroundcolor" usata nei dati XML.

<span id="XML_ALPHA_MODE"></span><span id="xml_alpha_mode"></span>**MODALITÀ \_ ALFA \_ XML**  
ID risorsa per la stringa "modalità alfa" usata nei dati XML.

<span id="XML_RESTRICTTOOUTPUT"></span><span id="xml_restricttooutput"></span>**XML \_ RESTRICTTOOUTPUT**  
ID risorsa per la stringa "restricttooutput" usata nei dati XML.

<span id="XML_MODEROTATION"></span><span id="xml_moderotation"></span>**XML \_ MODEROTATION**  
ID risorsa per la stringa "moderotation" usata nei dati XML.

<span id="XML_ISTEMPORARYMONOSUPPORTED"></span><span id="xml_istemporarymonosupported"></span>**XML \_ ISTEMPORARYMONOSUPPORTED**  
ID risorsa per la stringa "istemporarymonosupported" usata nei dati XML.

<span id="XML_FORCED_SAMPLE_COUNT"></span><span id="xml_forced_sample_count"></span>**CONTEGGIO \_ CAMPIONE \_ \_ FORZATO XML**  
ID risorsa per la stringa "forced sample count" usata nei dati XML.

<span id="XML_QUERY"></span><span id="xml_query"></span>**XML \_ QUERY**  
ID risorsa per la stringa "query" usata nei dati XML.

<span id="XML_10QUERY_DESC"></span><span id="xml_10query_desc"></span>**XML \_ 10QUERY \_ DESC**  
ID risorsa per la stringa "10query desc" usata nei dati XML.

<span id="XML_11QUERY_DESC"></span><span id="xml_11query_desc"></span>**XML \_ 11QUERY \_ DESC**  
ID risorsa per la stringa "11query desc" usata nei dati XML.

<span id="XML_FLAGS_NONE"></span><span id="xml_flags_none"></span>**FLAG XML \_ \_ NONE**  
ID risorsa per la stringa "flags none" usata nei dati XML.

<span id="XML_BUFFERUSAGE"></span><span id="xml_bufferusage"></span>**XML \_ BUFFERUSAGE**  
ID risorsa per la stringa "bufferusage" usata nei dati XML.

<span id="XML_UNKNOWN"></span><span id="xml_unknown"></span>**XML \_ UNKNOWN**  
ID risorsa per la stringa "unknown" usata nei dati XML.

<span id="XML_MAXFRAMELATENCY"></span><span id="xml_maxframelatency"></span>**XML \_ MAXFRAMELATENCY**  
ID risorsa per la stringa "maxframelatency" usata nei dati XML.

<span id="XML_SOURCESIZEWIDTH"></span><span id="xml_sourcesizewidth"></span>**XML \_ SOURCESIZEWIDTH**  
ID risorsa per la stringa "sourcesizewidth" usata nei dati XML.

<span id="XML_SOURCESIZEHEIGHT"></span><span id="xml_sourcesizeheight"></span>**XML \_ SOURCESIZEHEIGHT**  
ID risorsa per la stringa "sourcesizeheight" usata nei dati XML.

<span id="XML_MATRIXTRANSFORM"></span><span id="xml_matrixtransform"></span>**XML \_ MATRIXTRANSFORM**  
ID risorsa per la stringa "matrixtransform" usata nei dati XML.

<span id="XML_TILED_RESOURCE_INFO"></span><span id="xml_tiled_resource_info"></span>**INFORMAZIONI \_ SULLE RISORSE \_ AFFIANCATE \_ XML**  
ID risorsa per la stringa "tiled resource info" usata nei dati XML.

<span id="XML_TILES_RESOURCE_SUBRESOURCE_TILINGS"></span><span id="xml_tiles_resource_subresource_tilings"></span>**RIQUADRI XML \_ \_ - \_ SOTTORISORSE \_ DELLE RISORSE**  
ID risorsa per la stringa "tiles resource subresource tilings" usata nei dati XML.

<span id="XML_DEVICE_CHILD_INFORMATION"></span><span id="xml_device_child_information"></span>**INFORMAZIONI \_ SUGLI ELEMENTI FIGLIO DEL DISPOSITIVO \_ \_ XML**  
ID risorsa per la stringa "device child information" usata nei dati XML.

<span id="XML_DEVICE"></span><span id="xml_device"></span>**DISPOSITIVO \_ XML**  
ID risorsa per la stringa "device" usata nei dati XML.

<span id="XML_OTHER"></span><span id="xml_other"></span>**XML \_ OTHER**  
ID risorsa per la stringa "other" usata nei dati XML.

<span id="XML_PREDICATE"></span><span id="xml_predicate"></span>**\_PREDICATO XML**  
ID risorsa per la stringa "predicate" usata nei dati XML.

<span id="XML_PREDICATE_VALUE"></span><span id="xml_predicate_value"></span>**VALORE \_ PREDICATO \_ XML**  
ID risorsa per la stringa "valore predicato" usata nei dati XML.

<span id="XML_DEVICE_CONTEXT_TYPE"></span><span id="xml_device_context_type"></span>**TIPO \_ DI CONTESTO DI DISPOSITIVO \_ \_ XML**  
ID risorsa per la stringa "tipo di contesto di dispositivo" usata nei dati XML.

<span id="XML_BUFFER_HANDLE"></span><span id="xml_buffer_handle"></span>**\_HANDLE DEL BUFFER \_ XML**  
ID risorsa per la stringa "handle del buffer" usata nei dati XML.

<span id="XML_DEVICE_12"></span><span id="xml_device_12"></span>**DISPOSITIVO \_ XML \_ 12**  
ID risorsa per la stringa "device 12" usata nei dati XML.

<span id="XML_MAX_RECORDING_COMMAND_LISTS"></span><span id="xml_max_recording_command_lists"></span>**ELENCHI DI \_ COMANDI XML MAX \_ RECORDING \_ \_**  
ID risorsa per la stringa "max recording command lists" usata nei dati XML.

<span id="XML_DEFAULT_COMMAND_QUEUE"></span><span id="xml_default_command_queue"></span>**CODA \_ DI COMANDI XML \_ \_ DEFAULT**  
ID risorsa per la stringa "default command queue" usata nei dati XML.

<span id="XML_D3D11_DEVICE_INFORMATION"></span><span id="xml_d3d11_device_information"></span>**INFORMAZIONI \_ SUL DISPOSITIVO XML D3D11 \_ \_**  
ID risorsa per la stringa "d3d11 device information" usata nei dati XML.

<span id="XML_D3D12_DEVICE_INFORMATION"></span><span id="xml_d3d12_device_information"></span>**INFORMAZIONI \_ SUL DISPOSITIVO XML D3D12 \_ \_**  
ID risorsa per la stringa "d3d12 device information" usata nei dati XML.

<span id="XML_CONSERVATIVE_RASTER"></span><span id="xml_conservative_raster"></span>**RASTER \_ \_ CONSERVATIVO XML**  
ID risorsa per la stringa "raster conservativo" usata nei dati XML.

<span id="XML_INPUT_LAYOUT_12"></span><span id="xml_input_layout_12"></span>**LAYOUT INPUT XML \_ \_ \_ 12**  
ID risorsa per la stringa "layout di input 12" usata nei dati XML.

<span id="XML_GRAPHICS_PIPELINE_STATE_12"></span><span id="xml_graphics_pipeline_state_12"></span>**STATO DELLA PIPELINE GRAFICA XML \_ \_ \_ \_ 12**  
ID risorsa per la stringa "graphics pipeline state 12" usata nei dati XML.

<span id="XML_BLEND_STATE_12"></span><span id="xml_blend_state_12"></span>**XML \_ BLEND \_ STATE \_ 12**  
ID risorsa per la stringa "blend state 12" usata nei dati XML.

<span id="XML_ROOT_SIGNATURE_12"></span><span id="xml_root_signature_12"></span>**FIRMA RADICE XML \_ \_ \_ 12**  
ID risorsa per la stringa "firma radice 12" usata nei dati XML.

<span id="XML_STREAM_OUTPUT_12"></span><span id="xml_stream_output_12"></span>**OUTPUT DEL FLUSSO XML \_ \_ \_ 12**  
ID risorsa per la stringa "stream output 12" usata nei dati XML.

<span id="XML_RASTERIZED_STREAM"></span><span id="xml_rasterized_stream"></span>**FLUSSO \_ \_ RASTERIZZATO XML**  
ID risorsa per la stringa "flusso rasterizzato" usato nei dati XML.

<span id="XML_SO_DECLARATION_ENTRY_12"></span><span id="xml_so_declaration_entry_12"></span>**VOCE DI \_ DICHIARAZIONE SO \_ XML \_ \_ 12**  
ID risorsa per la stringa "so declaration entry 12" usata nei dati XML.

<span id="XML_START_COMPONENT"></span><span id="xml_start_component"></span>**COMPONENTE \_ DI AVVIO \_ XML**  
ID risorsa per la stringa "start component" usata nei dati XML.

<span id="XML_COMPONENT_COUNT"></span><span id="xml_component_count"></span>**CONTEGGIO \_ COMPONENTI \_ XML**  
ID risorsa per la stringa "component count" usata nei dati XML.

<span id="XML_OUTPUT_SLOT"></span><span id="xml_output_slot"></span>**\_SLOT DI OUTPUT \_ XML**  
ID risorsa per la stringa "slot di output" usata nei dati XML.

<span id="XML_COMPUTE_PIPELINE_STATE_12"></span><span id="xml_compute_pipeline_state_12"></span>**STATO DELLA PIPELINE DI CALCOLO XML \_ \_ \_ \_ 12**  
ID risorsa per la stringa "stato pipeline di calcolo 12" usato nei dati XML.

<span id="XML_D3D12_SHADER_BYTECODE"></span><span id="xml_d3d12_shader_bytecode"></span>**XML \_ D3D12 \_ SHADER \_ BYTECODE**  
ID risorsa per la stringa "d3d12 shader bytecode" usata nei dati XML.

<span id="XML_D3D12_SHADER_BYTECODE_PTR"></span><span id="xml_d3d12_shader_bytecode_ptr"></span>**XML \_ D3D12 \_ SHADER \_ BYTECODE \_ PTR**  
ID risorsa per la stringa "d3d12 shader bytecode ptr" usata nei dati XML.

<span id="XML_D3D12_SHADER_BYTECODE_LENGTH"></span><span id="xml_d3d12_shader_bytecode_length"></span>**LUNGHEZZA \_ \_ \_ BYTECODE SHADER XML D3D12 \_**  
ID risorsa per la stringa "d3d12 shader bytecode length" usata nei dati XML.

<span id="XML_D3D12_VIEW_DIMENSION"></span><span id="xml_d3d12_view_dimension"></span>**DIMENSIONE \_ DI VISUALIZZAZIONE D3D12 \_ \_ XML**  
ID risorsa per la stringa "d3d12 view dimension" usata nei dati XML.

<span id="XML_D3D12_NOTHING_TO_DEFINE"></span><span id="xml_d3d12_nothing_to_define"></span>**XML \_ D3D12 \_ NULLA DA \_ \_ DEFINIRE**  
ID risorsa per la stringa "d3d12 nothing to define" usata nei dati XML.

<span id="XML_OFFSET_IN_BYTES"></span><span id="xml_offset_in_bytes"></span>**OFFSET \_ XML \_ IN \_ BYTE**  
ID risorsa per la stringa "offset in byte" usata nei dati XML.

<span id="XML_SIZE_IN_BYTES"></span><span id="xml_size_in_bytes"></span>**DIMENSIONI \_ XML \_ IN \_ BYTE**  
ID risorsa per la stringa "size in bytes" usata nei dati XML.

<span id="XML_SO_CUR_OFFSET"></span><span id="xml_so_cur_offset"></span>**XML \_ SO \_ CUR \_ OFFSET**  
ID risorsa per la stringa "so cur offset" usata nei dati XML.

<span id="XML_ROOT_SIGNATURE"></span><span id="xml_root_signature"></span>**FIRMA \_ RADICE \_ XML**  
ID risorsa per la stringa "firma radice" usata nei dati XML.

<span id="XML_NUM_PARAMETERS"></span><span id="xml_num_parameters"></span>**PARAMETRI \_ NUM \_ XML**  
ID risorsa per la stringa "parametri num" usata nei dati XML.

<span id="XML_ROOT_PARAMETERS"></span><span id="xml_root_parameters"></span>**PARAMETRI \_ RADICE \_ XML**  
ID risorsa per la stringa "parametri radice" usata nei dati XML.

<span id="XML_ROOT_PARAMETER"></span><span id="xml_root_parameter"></span>**PARAMETRO \_ RADICE \_ XML**  
ID risorsa per la stringa "parametro radice" usata nei dati XML.

<span id="XML_ROOT_PARAMETER_TYPE"></span><span id="xml_root_parameter_type"></span>**TIPO \_ DI PARAMETRO RADICE \_ \_ XML**  
ID risorsa per la stringa "tipo di parametro radice" usata nei dati XML.

<span id="XML_ROOT_PARAMETER_SHADER_VISIBILITY"></span><span id="xml_root_parameter_shader_visibility"></span>**VISIBILITÀ \_ DELLO SHADER DEI PARAMETRI RADICE \_ \_ \_ XML**  
ID risorsa per la stringa "visibilità shader del parametro radice" usata nei dati XML.

<span id="XML_DESCRIPTOR_HANDLE"></span><span id="xml_descriptor_handle"></span>**HANDLE \_ DEL DESCRITTORE \_ XML**  
ID risorsa per la stringa "handle descrittore" usato nei dati XML.

<span id="XML_DESCRIPTOR_HANDLE_PTR"></span><span id="xml_descriptor_handle_ptr"></span>**\_PTR \_ DELL'HANDLE \_ DEL DESCRITTORE XML**  
ID risorsa per la stringa "descriptor handle ptr" usato nei dati XML.

<span id="XML_ROOT_CONSTANTS"></span><span id="xml_root_constants"></span>**COSTANTI \_ RADICE \_ XML**  
ID risorsa per la stringa "costanti radice" usata nei dati XML.

<span id="XML_ROOT_DESCRIPTOR"></span><span id="xml_root_descriptor"></span>**DESCRITTORE \_ RADICE \_ XML**  
ID risorsa per la stringa "descrittore radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_SHADER_REGISTER"></span><span id="xml_root_descriptor_shader_register"></span>**REGISTRO \_ SHADER \_ DEL DESCRITTORE RADICE \_ \_ XML**  
ID risorsa per la stringa "registro shader del descrittore radice" usata nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_REGISTER_SPACE"></span><span id="xml_root_descriptor_register_space"></span>**SPAZIO \_ DI \_ REGISTRAZIONE DESCRITTORE RADICE \_ \_ XML**  
ID risorsa per la stringa "spazio registro descrittore radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_TABLE"></span><span id="xml_root_descriptor_table"></span>**TABELLA \_ DEL \_ DESCRITTORE RADICE \_ XML**  
ID risorsa per la stringa "tabella descrittore radice" usata nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_NUM_RANGES"></span><span id="xml_root_descriptor_num_ranges"></span>**INTERVALLI NUM \_ \_ DEL DESCRITTORE RADICE \_ \_ XML**  
ID risorsa per la stringa "intervallo num descrittore radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGES"></span><span id="xml_root_descriptor_ranges"></span>**INTERVALLI \_ DI \_ DESCRITTORI RADICE \_ XML**  
ID risorsa per la stringa "intervalli di descrittori radice" usata nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE"></span><span id="xml_root_descriptor_range"></span>**INTERVALLO \_ \_ DESCRITTORE RADICE \_ XML**  
ID risorsa per la stringa "intervallo descrittore radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_TYPE"></span><span id="xml_root_descriptor_range_type"></span>**TIPO \_ DI INTERVALLO \_ DESCRITTORE RADICE \_ \_ XML**  
ID risorsa per la stringa "tipo di intervallo descrittore radice" usata nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_COUNT"></span><span id="xml_root_descriptor_range_count"></span>**CONTEGGIO \_ \_ DELL'INTERVALLO DEL \_ DESCRITTORE RADICE \_ XML**  
ID risorsa per la stringa "conteggio intervallo descrittore radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_BASE_SHADER_REGISTER"></span><span id="xml_root_descriptor_range_base_shader_register"></span>**REGISTRO SHADER \_ \_ DI BASE \_ DELL'INTERVALLO \_ DI DESCRITTORI RADICE \_ \_ XML**  
ID risorsa per la stringa "registro shader di base dell'intervallo del descrittore radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_REGISTER_SPACE"></span><span id="xml_root_descriptor_range_register_space"></span>**SPAZIO \_ DI REGISTRAZIONE \_ \_ DELL'INTERVALLO DEL \_ DESCRITTORE RADICE \_ XML**  
ID risorsa per la stringa "spazio registro intervallo descrittore radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_OFFSET"></span><span id="xml_root_descriptor_range_offset"></span>**OFFSET \_ INTERVALLO \_ DESCRITTORE RADICE \_ \_ XML**  
ID risorsa per la stringa "offset intervallo descrittore radice" usato nei dati XML.

<span id="XML_ROOT_CONSTANTS_SHADER_REGISTER"></span><span id="xml_root_constants_shader_register"></span>**REGISTRO SHADER \_ \_ DELLE COSTANTI RADICE \_ \_ XML**  
ID risorsa per la stringa "registro shader delle costanti radice" usata nei dati XML.

<span id="XML_ROOT_CONSTANTS_REGISTER_SPACE"></span><span id="xml_root_constants_register_space"></span>**SPAZIO \_ DI REGISTRAZIONE DELLE COSTANTI \_ RADICE \_ XML \_**  
ID risorsa per la stringa "root constants register space" usata nei dati XML.

<span id="XML_ROOT_CONSTANTS_INT32_COUNT"></span><span id="xml_root_constants_int32_count"></span>**COSTANTI RADICE XML \_ \_ \_ INT32 \_ COUNT**  
ID risorsa per la stringa "costanti radice int32 count" usata nei dati XML.

<span id="XML_NOT_SET"></span><span id="xml_not_set"></span>**XML \_ NON \_ IMPOSTATO**  
ID risorsa per la stringa "not set" usata nei dati XML.

<span id="XML_ERROR_RETRIEVING_VALUE"></span><span id="xml_error_retrieving_value"></span>**ERRORE XML \_ DURANTE IL RECUPERO DEL \_ \_ VALORE**  
ID risorsa per la stringa "errore durante il recupero del valore" usato nei dati XML.

<span id="XML_VERTEX_SHADER"></span><span id="xml_vertex_shader"></span>**XML \_ VERTEX \_ SHADER**  
ID risorsa per la stringa "vertex shader" usata nei dati XML.

<span id="XML_PIXEL_SHADER"></span><span id="xml_pixel_shader"></span>**XML \_ PIXEL \_ SHADER**  
ID risorsa per la stringa "pixel shader" usata nei dati XML.

<span id="XML_DOMAIN_SHADER"></span><span id="xml_domain_shader"></span>**SHADER DI DOMINIO XML \_ \_**  
ID risorsa per la stringa "domain shader" usata nei dati XML.

<span id="XML_HULL_SHADER"></span><span id="xml_hull_shader"></span>**XML \_ HULL \_ SHADER**  
ID risorsa per la stringa "hull shader" usata nei dati XML.

<span id="XML_GEOMETRY_SHADER"></span><span id="xml_geometry_shader"></span>**XML \_ GEOMETRY \_ SHADER**  
ID risorsa per la stringa "geometry shader" usata nei dati XML.

<span id="XML_ROOT_SIGNATURE_OBJECT"></span><span id="xml_root_signature_object"></span>**OGGETTO \_ FIRMA \_ RADICE \_ XML**  
ID risorsa per la stringa "oggetto firma radice" usata nei dati XML.

<span id="XML_ROOT_SIGNATURE_DESERIALIZED_DESC"></span><span id="xml_root_signature_deserialized_desc"></span>**\_DESC \_ \_ DESERIALIZZATA DELLA FIRMA \_ RADICE XML**  
ID risorsa per la stringa "root signature deserialized desc" usata nei dati XML.

<span id="XML_COMMAND_QUEUE"></span><span id="xml_command_queue"></span>**CODA \_ DI COMANDI \_ XML**  
ID risorsa per la stringa "coda di comandi" usata nei dati XML.

<span id="XML_D3D12_COMMAND_LIST_TYPE_TYPE"></span><span id="xml_d3d12_command_list_type_type"></span>**TIPO DI ELENCO DI COMANDI XML \_ D3D12 \_ \_ \_ \_**  
ID risorsa per la stringa "d3d12 command list type type" usata nei dati XML.

<span id="XML_COMMAND_QUEUE_PRIORITY"></span><span id="xml_command_queue_priority"></span>**PRIORITÀ \_ CODA \_ COMANDI \_ XML**  
ID risorsa per la stringa "priorità coda comandi" usata nei dati XML.

<span id="XML_COMMAND_QUEUE_IS_DEFAULT"></span><span id="xml_command_queue_is_default"></span>**LA CODA DI COMANDI XML \_ \_ È \_ \_ PREDEFINITA**  
ID risorsa per la stringa "command queue is default" usata nei dati XML.

<span id="XML_COMMAND_ALLOCATOR"></span><span id="xml_command_allocator"></span>**\_ \_ ALLOCATORE DI COMANDI XML**  
ID risorsa per la stringa "allocatore comandi" usata nei dati XML.

<span id="XML_D3D12_RESOURCE"></span><span id="xml_d3d12_resource"></span>**RISORSA \_ D3D12 \_ XML**  
ID risorsa per la stringa "risorsa d3d12" usata nei dati XML.

<span id="XML_D3D12_DESCRIPTOR_HEAP"></span><span id="xml_d3d12_descriptor_heap"></span>**\_HEAP DEL DESCRITTORE XML D3D12 \_ \_**  
ID risorsa per la stringa "heap descrittore d3d12" usata nei dati XML.

<span id="XML_D3D12_DESCRIPTOR"></span><span id="xml_d3d12_descriptor"></span>**DESCRITTORE \_ XML D3D12 \_**  
ID risorsa per la stringa "descrittore d3d12" usato nei dati XML.

<span id="XML_D3D12_RTV"></span><span id="xml_d3d12_rtv"></span>**XML \_ D3D12 \_ RTV**  
ID risorsa per la stringa "d3d12 rtv" usata nei dati XML.

<span id="XML_D3D12_VBV"></span><span id="xml_d3d12_vbv"></span>**XML \_ D3D12 \_ VBV**  
ID risorsa per la stringa "d3d12 vbv" usata nei dati XML.

<span id="XML_D3D12_IBV"></span><span id="xml_d3d12_ibv"></span>**XML \_ D3D12 \_ IBV**  
ID risorsa per la stringa "d3d12 ibv" usata nei dati XML.

<span id="XML_D3D12_CBV"></span><span id="xml_d3d12_cbv"></span>**XML \_ D3D12 \_ CBV**  
ID risorsa per la stringa "d3d12 cbv" usata nei dati XML.

<span id="XML_D3D12_SRV"></span><span id="xml_d3d12_srv"></span>**XML \_ D3D12 \_ SRV**  
ID risorsa per la stringa "d3d12 srv" usata nei dati XML.

<span id="XML_D3D12_UAV"></span><span id="xml_d3d12_uav"></span>**XML \_ D3D12 \_ UAV**  
ID risorsa per la stringa "d3d12 uav" usata nei dati XML.

<span id="XML_D3D12_SOV"></span><span id="xml_d3d12_sov"></span>**XML \_ D3D12 \_ SOV**  
ID risorsa per la stringa "d3d12 sov" usata nei dati XML.

<span id="XML_D3D12_SAMPLER"></span><span id="xml_d3d12_sampler"></span>**XML \_ D3D12 \_ SAMPLER**  
ID risorsa per la stringa "d3d12 sampler" usata nei dati XML.

<span id="XML_D3D12_DSV"></span><span id="xml_d3d12_dsv"></span>**XML \_ D3D12 \_ DSV**  
ID risorsa per la stringa "d3d12 dsv" usata nei dati XML.

<span id="XML_TEXTURE_1D"></span><span id="xml_texture_1d"></span>**TRAMA XML \_ \_ 1D**  
ID risorsa per la stringa "texture 1d" usata nei dati XML.

<span id="XML_TEXTURE_1DARRAY"></span><span id="xml_texture_1darray"></span>**TRAMA XML \_ \_ 1DARRAY**  
ID risorsa per la stringa "texture 1darray" usata nei dati XML.

<span id="XML_TEXTURE_2D"></span><span id="xml_texture_2d"></span>**TRAMA XML \_ \_ 2D**  
ID risorsa per la stringa "texture 2d" usata nei dati XML.

<span id="XML_TEXTURE_2DARRAY"></span><span id="xml_texture_2darray"></span>**TRAMA XML \_ \_ 2DARRAY**  
ID risorsa per la stringa "texture 2darray" usata nei dati XML.

<span id="XML_TEXTURE_2DMS"></span><span id="xml_texture_2dms"></span>**XML \_ TEXTURE \_ 2DMS**  
ID risorsa per la stringa "texture 2dms" usata nei dati XML.

<span id="XML_TEXTURE_2DMSARRAY"></span><span id="xml_texture_2dmsarray"></span>**TRAMA XML \_ \_ 2DMSARRAY**  
ID risorsa per la stringa "texture 2dmsarray" usata nei dati XML.

<span id="XML_TEXTURE_3D"></span><span id="xml_texture_3d"></span>**TRAMA XML \_ \_ 3D**  
ID risorsa per la stringa "texture 3d" usata nei dati XML.

<span id="XML_MISCELLANEOUS"></span><span id="xml_miscellaneous"></span>**XML \_ MISCELLANEOUS**  
ID risorsa per la stringa "varie" usata nei dati XML.

<span id="XML_RENDER_TARGET_VIEW_12"></span><span id="xml_render_target_view_12"></span>**VISUALIZZAZIONE DESTINAZIONE RENDERING XML \_ \_ \_ \_ 12**  
ID risorsa per la stringa "render target view 12" usata nei dati XML.

<span id="XML_STRIDE_COUNT"></span><span id="xml_stride_count"></span>**CONTEGGIO \_ STRIDE \_ XML**  
ID risorsa per la stringa "stride count" usata nei dati XML.

<span id="XML_SHADER_ROOT_SIGNATURES"></span><span id="xml_shader_root_signatures"></span>**FIRME \_ RADICE DELLO SHADER \_ \_ XML**  
ID risorsa per la stringa "firme radice shader" usata nei dati XML.

<span id="XML_CMDLIST_ROOT_SIGNATURE_OBJECT"></span><span id="xml_cmdlist_root_signature_object"></span>**OGGETTO \_ FIRMA RADICE CMDLIST \_ \_ \_ XML**  
ID risorsa per la stringa "cmdlist root signature object" usata nei dati XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_COUNT"></span><span id="xml_input_layout_element_count"></span>**CONTEGGIO \_ ELEMENTI DEL LAYOUT DI INPUT \_ \_ \_ XML**  
ID risorsa per la stringa "input layout element count" usata nei dati XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_DESCRIPTIONS"></span><span id="xml_input_layout_element_descriptions"></span>**DESCRIZIONI \_ DEGLI ELEMENTI DI LAYOUT DI INPUT \_ \_ \_ XML**  
ID risorsa per la stringa "descrizioni degli elementi di layout di input" usata nei dati XML.

<span id="XML_SHADER_DESCRIPTION"></span><span id="xml_shader_description"></span>**DESCRIZIONE \_ DELLO SHADER \_ XML**  
ID risorsa per la stringa "descrizione shader" usata nei dati XML.

<span id="XML_PSO_ROOT_SIGNATURE_OBJECT"></span><span id="xml_pso_root_signature_object"></span>**OGGETTO \_ FIRMA RADICE PSO \_ \_ \_ XML**  
ID risorsa per la stringa "pso root signature object" usata nei dati XML.

<span id="XML_BUFFER_LOCATION"></span><span id="xml_buffer_location"></span>**PERCORSO \_ DEL BUFFER \_ XML**  
ID risorsa per la stringa "posizione del buffer" usata nei dati XML.

<span id="XML_BUFFER_FILLED_LOCATION"></span><span id="xml_buffer_filled_location"></span>**PERCORSO \_ DI RIEMPIMENTO DEL BUFFER \_ \_ XML**  
ID risorsa per la stringa "buffer filled location" usata nei dati XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS"></span><span id="xml_d3d12_virtual_address"></span>**INDIRIZZO \_ VIRTUALE D3D12 \_ \_ XML**  
ID risorsa per la stringa "d3d12 virtual address" usata nei dati XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS_OFFSET"></span><span id="xml_d3d12_virtual_address_offset"></span>**OFFSET \_ DELL'INDIRIZZO VIRTUALE D3D12 \_ \_ \_ XML**  
ID risorsa per la stringa "d3d12 virtual address offset" usata nei dati XML.

<span id="XML_D3D12_OFFSET_IN_BYTES"></span><span id="xml_d3d12_offset_in_bytes"></span>**OFFSET \_ D3D12 XML \_ \_ IN \_ BYTE**  
ID risorsa per la stringa "d3d12 offset in byte" usata nei dati XML.

<span id="XML_MAPPED_TILES"></span><span id="xml_mapped_tiles"></span>**\_RIQUADRI \_ MAPPATI XML**  
ID risorsa per la stringa "riquadri mappati" usata nei dati XML.

<span id="XML_POOL_TILES"></span><span id="xml_pool_tiles"></span>**RIQUADRI \_ DEL POOL \_ XML**  
ID risorsa per la stringa "riquadri del pool" usata nei dati XML.

<span id="XML_TILE"></span><span id="xml_tile"></span>**RIQUADRO \_ XML**  
ID risorsa per la stringa "tile" usata nei dati XML.

<span id="XML_OPTIONS"></span><span id="xml_options"></span>**OPZIONI \_ XML**  
ID risorsa per la stringa "options" usata nei dati XML.

<span id="XML_TILE_LIST_MODE"></span><span id="xml_tile_list_mode"></span>**MODALITÀ \_ ELENCO \_ RIQUADRI \_ XML**  
ID risorsa per la stringa "tile list mode" usata nei dati XML.

<span id="XML_TILE_POOL_SLOT"></span><span id="xml_tile_pool_slot"></span>**\_SLOT DEL POOL DI \_ RIQUADRI \_ XML**  
ID risorsa per la stringa "slot del pool di riquadri" usata nei dati XML.

<span id="XML_TILE_COORDINATE"></span><span id="xml_tile_coordinate"></span>**COORDINATA \_ DEL RIQUADRO \_ XML**  
ID risorsa per la stringa "coordinata del riquadro" usata nei dati XML.

<span id="XML_HEAP_TYPE"></span><span id="xml_heap_type"></span>**TIPO \_ HEAP \_ XML**  
ID risorsa per la stringa "tipo heap" usata nei dati XML.

<span id="XML_RESOURCE_USAGE"></span><span id="xml_resource_usage"></span>**UTILIZZO \_ DELLE RISORSE \_ XML**  
ID risorsa per la stringa "resource usage" usata nei dati XML.

<span id="XML_RESOURCE_USAGES"></span><span id="xml_resource_usages"></span>**UTILIZZI \_ DELLE \_ RISORSE XML**  
ID risorsa per la stringa "resource usages" usata nei dati XML.

<span id="XML_SUBRESOURCE"></span><span id="xml_subresource"></span>**\_SOTTORISORSA XML**  
ID risorsa per la stringa "subresource" usata nei dati XML.

<span id="XML_SUBRESOURCE_USAGE"></span><span id="xml_subresource_usage"></span>**UTILIZZO \_ DELLE SOTTORISORSE \_ XML**  
ID risorsa per la stringa "subresource usage" usata nei dati XML.

<span id="XML_COMPUTE_SHADER"></span><span id="xml_compute_shader"></span>**XML \_ COMPUTE \_ SHADER**  
ID risorsa per la stringa "compute shader" usata nei dati XML.

<span id="XML_COMMAND_LIST_12"></span><span id="xml_command_list_12"></span>**XML \_ COMMAND \_ LIST \_ 12**  
ID risorsa per la stringa "command list 12" usata nei dati XML.

<span id="XML_PSO_RENDER_TARGET_FORMATS"></span><span id="xml_pso_render_target_formats"></span>**FORMATI DI \_ DESTINAZIONE DI RENDERING PSO \_ \_ \_ XML**  
ID risorsa per la stringa "pso render target formats" usata nei dati XML.

<span id="XML_RENDER_TARGET_VIEWS"></span><span id="xml_render_target_views"></span>**VISUALIZZAZIONI \_ DELLE DESTINAZIONE DI RENDERING \_ \_ XML**  
ID risorsa per la stringa "render target views" usata nei dati XML.

<span id="XML_SHADER_RESOURCE_VIEWS"></span><span id="xml_shader_resource_views"></span>**VISUALIZZAZIONI \_ DELLE \_ RISORSE SHADER \_ XML**  
ID risorsa per la stringa "shader resource views" usata nei dati XML.

## <a name="requirements"></a>Requisiti

<table><colgroup><col style="width: 50%" /><col style="width: 50%" /></colgroup><tbody><tr class="odd"><td><p>Intestazione</p></td><td>Vspixengine.h</td></tr></tbody></table>

 

 



