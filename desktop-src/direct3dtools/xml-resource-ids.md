---
description: ID di stringa di risorsa impostati dal chiamante da restituire nei dati XML per la visualizzazione degli oggetti.
MS-HAID: vspixengine.Xml\_Resource\_Ids
MSHAttr:
- PreferredSiteName:MSDN
- PreferredLib:/library/windows/desktop
title: Enumerazione Xml_Resource_Ids
ms.topic: reference
ms.date: 05/31/2018
ms.assetid: 1F2ECC78-73EC-4A59-8CF7-1BB7FB80244E
api_name:
- Xml_Resource_Ids
api_type:
- HeaderDef
api_location:
- vspixengine.h
topic_type:
- APIRef
- kbSyntax
ms.openlocfilehash: 1dca06e32962d35eed7ff52034648299ba0ddc89
ms.sourcegitcommit: a47bd86f517de76374e4fff33cfeb613eb259a7e
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 01/06/2021
ms.locfileid: "104225487"
---
# <a name="span-idvspixenginexml_resource_idsspanxml_resource_ids-enumeration"></a><span id="vspixengine.xml_resource_ids"></span>Enumerazione degli ID di \_ risorsa XML \_

ID di stringa di risorsa impostati dal chiamante da restituire nei dati XML per la visualizzazione di oggetti

## <a name="syntax"></a>Sintassi


```C++
};
```

## <a name="constants"></a>Costanti

<span id="XML_DEVICE_INFORMATION"></span><span id="xml_device_information"></span>**\_ \_ informazioni sul dispositivo XML**  
ID di risorsa per la stringa "informazioni sul dispositivo" usato nei dati XML.

<span id="XML_IA_STATE"></span><span id="xml_ia_state"></span>**\_stato Ia \_ XML**  
ID di risorsa per la stringa "input assembler" usato nei dati XML.

<span id="XML_VS_STATE"></span><span id="xml_vs_state"></span>**\_stato di confronto XML \_**  
ID di risorsa per la stringa "vertex shader stage" usato nei dati XML.

<span id="XML_GS_STATE"></span><span id="xml_gs_state"></span>**\_stato GS \_ XML**  
ID di risorsa per la stringa "Geometry shader stage" usato nei dati XML.

<span id="XML_RASTERIZER_STATE"></span><span id="xml_rasterizer_state"></span>**\_stato di rasterizzazione XML \_**  
ID risorsa per la stringa "fase di rasterizzazione" usata nei dati XML.

<span id="XML_PS_STATE"></span><span id="xml_ps_state"></span>**\_stato PS \_ XML**  
ID di risorsa per la stringa "pixel shader stage" utilizzato nei dati XML.

<span id="XML_OM_STATE"></span><span id="xml_om_state"></span>**\_stato om \_ XML**  
ID risorsa per la stringa "fase di Unione dell'output" usata nei dati XML.

<span id="XML_HS_STATE"></span><span id="xml_hs_state"></span>**\_stato HS \_ XML**  
ID risorsa per la stringa "fase di Hull shader" usata nei dati XML.

<span id="XML_DS_STATE"></span><span id="xml_ds_state"></span>**\_stato DS \_ XML**  
ID risorsa per la stringa "Domain shader stage" usato nei dati XML.

<span id="XML_CS_STATE"></span><span id="xml_cs_state"></span>**\_stato cs \_ XML**  
ID risorsa per la stringa "compute shader stage" usato nei dati XML.

<span id="XML_CLASS_INSTANCE_11"></span><span id="xml_class_instance_11"></span>**\_Istanza della classe XML \_ \_ 11**  
ID risorsa per la stringa "istanze di classe" usata nei dati XML.

<span id="XML_DEVICE_10"></span><span id="xml_device_10"></span>**\_Dispositivo XML \_ 10**  
ID di risorsa per la stringa "Device 10" usato nei dati XML.

<span id="XML_INPUT_LAYOUT_11"></span><span id="xml_input_layout_11"></span>**\_Layout di input XML \_ \_ 11**  
ID risorsa per la stringa "input layout 11" utilizzato nei dati XML.

<span id="XML_SAMPLER_STATE_11"></span><span id="xml_sampler_state_11"></span>**\_Stato campionatore XML \_ \_ 11**  
ID risorsa per la stringa "campionatore stato 11" usato nei dati XML.

<span id="XML_RASTER_STATE_11"></span><span id="xml_raster_state_11"></span>**\_Stato raster \_ XML \_ 11**  
ID risorsa per la stringa "stato raster 11" utilizzato nei dati XML.

<span id="XML_BLEND_STATE_11"></span><span id="xml_blend_state_11"></span>**\_Stato di Blend XML \_ \_ 11**  
ID di risorsa per la stringa "Blend state 11" usato nei dati XML.

<span id="XML_DEPTH_STENCIL_STATE_11"></span><span id="xml_depth_stencil_state_11"></span>**\_ \_ Stato stencil profondità \_ XML \_ 11**  
ID risorsa per la stringa "depth stencil stato 11" utilizzato nei dati XML.

<span id="XML_UNORDERED_ACCESS_VIEW_11"></span><span id="xml_unordered_access_view_11"></span>**\_Vista di accesso non ordinato XML \_ \_ \_ 11**  
ID risorsa per la stringa "vista di accesso non ordinato 11" utilizzata nei dati XML.

<span id="XML_SHADER_RESOURCE_VIEW_11"></span><span id="xml_shader_resource_view_11"></span>**\_ \_ Visualizzazione risorse shader XML \_ \_ 11**  
ID risorsa per la stringa "shader Resource View 11" usato nei dati XML.

<span id="XML_DEPTH_STENCIL_VIEW_11"></span><span id="xml_depth_stencil_view_11"></span>**\_ \_ Visualizzazione stencil profondità \_ XML \_ 11**  
ID di risorsa per la stringa "depth stencil View 11" usato nei dati XML.

<span id="XML_RENDER_TARGET_VIEW_11"></span><span id="xml_render_target_view_11"></span>**\_ \_ Visualizzazione destinazione rendering \_ XML \_ 11**  
ID risorsa per la stringa "visualizzazione destinazione rendering 11" utilizzata nei dati XML.

<span id="XML_DEVICE_11"></span><span id="xml_device_11"></span>**\_Dispositivo XML \_ 11**  
ID di risorsa per la stringa "Device 11" usato nei dati XML.

<span id="XML_SWAP_CHAIN"></span><span id="xml_swap_chain"></span>**\_catena di scambio XML \_**  
ID risorsa per la stringa "scambio catena" utilizzato nei dati XML.

<span id="XML_INPUT_LAYOUT_10"></span><span id="xml_input_layout_10"></span>**\_Layout di input XML \_ \_ 10**  
ID di risorsa per la stringa "input layout 10" usato nei dati XML.

<span id="XML_SAMPLER_STATE_10"></span><span id="xml_sampler_state_10"></span>**\_Stato campionatore XML \_ \_ 10**  
ID risorsa per la stringa "campionatore stato 10" usato nei dati XML.

<span id="XML_RASTER_STATE_10"></span><span id="xml_raster_state_10"></span>**\_Stato raster \_ XML \_ 10**  
ID risorsa per la stringa "stato raster 10" utilizzato nei dati XML.

<span id="XML_BLEND_STATE_10"></span><span id="xml_blend_state_10"></span>**\_Stato di Blend XML \_ \_ 10**  
ID di risorsa per la stringa "Blend state 10" usato nei dati XML.

<span id="XML_DEPTH_STENCIL_STATE_10"></span><span id="xml_depth_stencil_state_10"></span>**\_ \_ Stato stencil profondità \_ XML \_ 10**  
ID risorsa per la stringa "depth stencil stato 10" utilizzato nei dati XML.

<span id="XML_SHADER_RESOURCE_VIEW_10"></span><span id="xml_shader_resource_view_10"></span>**\_ \_ Visualizzazione risorse shader XML \_ \_ 10**  
ID risorsa per la stringa "shader Resource View 10" usato nei dati XML.

<span id="XML_DEPTH_STENCIL_VIEW_10"></span><span id="xml_depth_stencil_view_10"></span>**\_ \_ Visualizzazione stencil profondità \_ XML \_ 10**  
ID di risorsa per la stringa "depth stencil View 10" usato nei dati XML.

<span id="XML_RENDER_TARGET_VIEW_10"></span><span id="xml_render_target_view_10"></span>**\_ \_ Visualizzazione destinazione rendering \_ XML \_ 10**  
ID risorsa per la stringa "visualizzazione destinazione rendering 10" utilizzata nei dati XML.

<span id="XML_VERTEX_DECLARATION"></span><span id="xml_vertex_declaration"></span>**\_dichiarazione vertice \_ XML**  
ID di risorsa per la stringa "Vertex Declaration" usato nei dati XML.

<span id="XML_STATE_BLOCK_STATE_10"></span><span id="xml_state_block_state_10"></span>**\_Stato del blocco di stato XML \_ \_ \_ 10**  
ID risorsa per la stringa "stato blocco di stato 10" utilizzato nei dati XML.

<span id="XML_DEVICE_CONTEXT_STATE_11"></span><span id="xml_device_context_state_11"></span>**\_Stato del contesto del dispositivo XML \_ \_ \_ 11**  
ID risorsa per la stringa "stato del contesto di dispositivo 11" usato nei dati XML.

<span id="XML_DEVICE_STATE_10"></span><span id="xml_device_state_10"></span>**\_Stato dispositivo \_ XML \_ 10**  
ID risorsa per la stringa "stato dispositivo 10" utilizzato nei dati XML.

<span id="XML_BACK_FACE"></span><span id="xml_back_face"></span>**\_back- \_ Face XML**  
ID di risorsa per la stringa "back-face" usato nei dati XML.

<span id="XML_FRONT_FACE"></span><span id="xml_front_face"></span>**front-end XML \_ \_**  
ID di risorsa per la stringa "front-end" usato nei dati XML.

<span id="XML_DEVICE_CREATION_INFORMATION"></span><span id="xml_device_creation_information"></span>**\_ \_ informazioni sulla creazione del dispositivo XML \_**  
ID risorsa per la stringa "informazioni sulla creazione del dispositivo" usato nei dati XML.

<span id="XML_OTHER_INFORMATION"></span><span id="xml_other_information"></span>**\_altre \_ informazioni XML**  
ID di risorsa per la stringa "altre informazioni" usato nei dati XML.

<span id="XML_MULTISAMPLING"></span><span id="xml_multisampling"></span>**\_campionamento multiplo XML**  
ID risorsa per la stringa "multisampling" utilizzata nei dati XML.

<span id="XML_BACK_BUFFER"></span><span id="xml_back_buffer"></span>**\_buffer nascosto \_ XML**  
ID risorsa per la stringa "buffer nascosto" utilizzato nei dati XML.

<span id="XML_GENERAL_INFORMATION"></span><span id="xml_general_information"></span>**\_informazioni generali \_ XML**  
ID risorsa per la stringa "informazioni generali" usata nei dati XML.

<span id="XML_RENDER_TARGETS"></span><span id="xml_render_targets"></span>**\_destinazioni di rendering XML \_**  
ID di risorsa per la stringa "destinazioni di rendering" utilizzato nei dati XML.

<span id="XML_BLEND_DESCRIPTION"></span><span id="xml_blend_description"></span>**\_Descrizione di Blend XML \_**  
ID risorsa per la stringa "Description Blend" usata nei dati XML.

<span id="XML_DEPTH_STENCIL"></span><span id="xml_depth_stencil"></span>**\_stencil profondità \_ XML**  
ID di risorsa per la stringa "depth stencil" utilizzato nei dati XML.

<span id="XML_FRONT_FACE_STENCIL"></span><span id="xml_front_face_stencil"></span>**\_stencil front \_ -end XML \_**  
ID risorsa per la stringa "stencil Front Face" usato nei dati XML.

<span id="XML_BACK_FACE_STENCIL"></span><span id="xml_back_face_stencil"></span>**\_stencil back- \_ Face \_ XML**  
ID di risorsa per la stringa "stencil back-face" usato nei dati XML.

<span id="XML_DEVICE1_CREATION_INFORMATION"></span><span id="xml_device1_creation_information"></span>**\_ \_ Informazioni sulla creazione di DEVICE1 XML \_**  
ID di risorsa per la stringa "device1 Creation Information" usato nei dati XML.

<span id="XML_VIEWPORTS"></span><span id="xml_viewports"></span>**\_viewport XML**  
ID di risorsa per la stringa "viewports" utilizzato nei dati XML.

<span id="XML_SCISSOR_RECTS"></span><span id="xml_scissor_rects"></span>**\_rettangoli a forbice XML \_**  
ID risorsa per la stringa "Scissor rects" usata nei dati XML.

<span id="XML_RASTERIZER"></span><span id="xml_rasterizer"></span>**\_rasterizzatore XML**  
ID risorsa per la stringa "rasterizzatore" utilizzato nei dati XML.

<span id="XML_SHADER_GENERAL_INFORMATION"></span><span id="xml_shader_general_information"></span>**\_ \_ informazioni generali sullo shader \_ XML**  
ID risorsa per la stringa "informazioni generali shader" utilizzato nei dati XML.

<span id="XML_SHADER_RESOURCES"></span><span id="xml_shader_resources"></span>**\_risorse shader XML \_**  
ID risorsa per la stringa "risorse shader" utilizzato nei dati XML.

<span id="XML_SAMPLER_STATES"></span><span id="xml_sampler_states"></span>**\_Stati del campionatore XML \_**  
ID di risorsa per la stringa "stati campionatore" usato nei dati XML.

<span id="XML_CONSTANT_BUFFERS"></span><span id="xml_constant_buffers"></span>**\_buffer costanti \_ XML**  
ID risorsa per la stringa "buffer costanti" utilizzato nei dati XML.

<span id="XML_STREAM_OUTPUT_BUFFERS"></span><span id="xml_stream_output_buffers"></span>**\_buffer di \_ output del flusso XML \_**  
ID risorsa per la stringa "buffer output flusso" utilizzato nei dati XML.

<span id="XML_INPUT_LAYOUT_GENERAL_INFORMATION"></span><span id="xml_input_layout_general_information"></span>**\_ \_ \_ informazioni generali sul layout di input \_ XML**  
ID di risorsa per la stringa "informazioni generali sul layout di input" utilizzato nei dati XML.

<span id="XML_INPUT_LAYOUTS"></span><span id="xml_input_layouts"></span>**\_layout di input XML \_**  
ID di risorsa per la stringa "layout di input" usato nei dati XML.

<span id="XML_VERTEX_BUFFERS"></span><span id="xml_vertex_buffers"></span>**\_buffer dei vertici XML \_**  
ID di risorsa per la stringa "vertex buffers" usato nei dati XML.

<span id="XML_INDEX_BUFFERS"></span><span id="xml_index_buffers"></span>**\_buffer di indice XML \_**  
ID di risorsa per la stringa "buffer di indice" utilizzato nei dati XML.

<span id="XML_PRIMITIVE_TOP_GENERAL"></span><span id="xml_primitive_top_general"></span>**\_primitiva XML primitiva \_ \_ generale**  
ID risorsa per la stringa "primitiva principale generale" usata nei dati XML.

<span id="XML_BLEND_STATE"></span><span id="xml_blend_state"></span>**\_stato di Blend XML \_**  
ID risorsa per la stringa "stato di Blend" usato nei dati XML.

<span id="XML_UNORDERED_ACCESS_VIEWS"></span><span id="xml_unordered_access_views"></span>**viste di accesso non \_ ordinato XML \_ \_**  
ID risorsa per la stringa "viste di accesso non ordinate" utilizzato nei dati XML.

<span id="XML_CLASS_INSTANCES"></span><span id="xml_class_instances"></span>**\_istanze di classi XML \_**  
ID risorsa per la stringa "istanze di classe" usata nei dati XML.

<span id="XML_INSTANCE_INDEX"></span><span id="xml_instance_index"></span>**\_Indice istanza \_ XML**  
ID di risorsa per la stringa "index instance" usato nei dati XML.

<span id="XML_INSTANCE_NAME"></span><span id="xml_instance_name"></span>**\_nome istanza \_ XML**  
ID risorsa per la stringa "nome istanza" utilizzato nei dati XML.

<span id="XML_BASE_SAMPLER"></span><span id="xml_base_sampler"></span>**\_campionatore di base XML \_**  
ID di risorsa per la stringa "Sampler di base" usato nei dati XML.

<span id="XML_BASE_TEXTURE"></span><span id="xml_base_texture"></span>**\_trama di base XML \_**  
ID risorsa per la stringa "trama di base" usata nei dati XML.

<span id="XML_BASE_CONSTANT_BUFFER_OFFSET"></span><span id="xml_base_constant_buffer_offset"></span>**\_offset del \_ \_ buffer costante di base \_ XML**  
ID risorsa per la stringa "offset del buffer costante di base" usato nei dati XML.

<span id="XML_CONSTANT_BUFFER"></span><span id="xml_constant_buffer"></span>**\_buffer costante \_ XML**  
ID risorsa per la stringa "buffer costante" utilizzato nei dati XML.

<span id="XML_TYPE_ID"></span><span id="xml_type_id"></span>**\_ID tipo \_ XML**  
ID di risorsa per la stringa "ID tipo" usato nei dati XML.

<span id="XML_INSTANCE_ID"></span><span id="xml_instance_id"></span>**\_ID istanza \_ XML**  
ID di risorsa per la stringa "ID istanza" utilizzato nei dati XML.

<span id="XML_TYPE_NAME"></span><span id="xml_type_name"></span>**\_nome del tipo XML \_**  
ID risorsa per la stringa "nome tipo" usato nei dati XML.

<span id="XML_CREATED_BY"></span><span id="xml_created_by"></span>**XML \_ creato \_ da**  
ID di risorsa per la stringa "created by" usato nei dati XML.

<span id="XML_CLASS_LINKAGE"></span><span id="xml_class_linkage"></span>**\_collegamento alla classe XML \_**  
ID risorsa per la stringa "collegamento di classe" utilizzato nei dati XML.

<span id="XML_DESCRIPTION"></span><span id="xml_description"></span>**\_Descrizione XML**  
ID risorsa per la stringa "Description" usata nei dati XML.

<span id="XML_SEMANTIC_NAME"></span><span id="xml_semantic_name"></span>**\_nome semantica XML \_**  
ID risorsa per la stringa "nome semantico" utilizzato nei dati XML.

<span id="XML_SEMANTIC_INDEX"></span><span id="xml_semantic_index"></span>**\_indice semantico XML \_**  
ID risorsa per la stringa "indice semantico" utilizzato nei dati XML.

<span id="XML_FORMAT"></span><span id="xml_format"></span>**\_formato XML**  
ID risorsa per la stringa "Format" usata nei dati XML.

<span id="XML_INPUT_SLOT"></span><span id="xml_input_slot"></span>**\_slot di input XML \_**  
ID risorsa per la stringa "slot di input" usato nei dati XML.

<span id="XML_ALIGNED_BYTE_OFFSET"></span><span id="xml_aligned_byte_offset"></span>**\_offset byte allineato XML \_ \_**  
ID risorsa per la stringa "offset dei byte allineati" utilizzato nei dati XML.

<span id="XML_INPUT_SLOT_CLASS"></span><span id="xml_input_slot_class"></span>**\_ \_ classe slot di input XML \_**  
ID risorsa per la stringa "classe slot di input" utilizzata nei dati XML.

<span id="XML_INSTANCE_DATA_STEP_RATE"></span><span id="xml_instance_data_step_rate"></span>**\_ \_ \_ frequenza passaggio dati istanza \_ XML**  
ID risorsa per la stringa "frequenza passaggio dati istanza" utilizzata nei dati XML.

<span id="XML_ADDRESS_U"></span><span id="xml_address_u"></span>**\_Indirizzo XML \_ U**  
ID di risorsa per la stringa "Address u" utilizzato nei dati XML.

<span id="XML_ADDRESS_V"></span><span id="xml_address_v"></span>**\_Indirizzo XML \_ V**  
ID di risorsa per la stringa "Address v" utilizzato nei dati XML.

<span id="XML_ADDRESS_W"></span><span id="xml_address_w"></span>**\_Indirizzo XML \_ W**  
ID di risorsa per la stringa "Address w" utilizzato nei dati XML.

<span id="XML_MIN_LOD"></span><span id="xml_min_lod"></span>**\_LOD min \_ XML**  
ID risorsa per la stringa "min LOD" usato nei dati XML.

<span id="XML_MAX_LOD"></span><span id="xml_max_lod"></span>**\_numero massimo di \_ LOD XML**  
ID risorsa per la stringa "Max LOD" usato nei dati XML.

<span id="XML_FILTER"></span><span id="xml_filter"></span>**\_filtro XML**  
ID risorsa per la stringa "Filter" usata nei dati XML.

<span id="XML_MIP_SLICE"></span><span id="xml_mip_slice"></span>**\_sezione XML MIP \_**  
ID di risorsa per la stringa "MIP Slice" utilizzato nei dati XML.

<span id="XML_MOST_DETAILED_MIP"></span><span id="xml_most_detailed_mip"></span>**\_MIP più \_ dettagliato \_ XML**  
ID di risorsa per la stringa "MIP più dettagliato" usato nei dati XML.

<span id="XML_MIP_LEVELS"></span><span id="xml_mip_levels"></span>**\_livelli MIP \_ XML**  
ID risorsa per la stringa "livelli MIP" usato nei dati XML.

<span id="XML_FIRST_ARRAY_SLICE"></span><span id="xml_first_array_slice"></span>**\_prima \_ sezione della matrice \_ XML**  
ID risorsa per la stringa "prima sezione della matrice" usata nei dati XML.

<span id="XML_ARRAY_SIZE"></span><span id="xml_array_size"></span>**\_dimensioni della matrice XML \_**  
ID di risorsa per la stringa "dimensione della matrice" usata nei dati XML.

<span id="XML_RESOURCE"></span><span id="xml_resource"></span>**\_risorsa XML**  
ID risorsa per la stringa "Resource" usata nei dati XML.

<span id="XML_RESOURCE_TYPE"></span><span id="xml_resource_type"></span>**\_tipo di risorsa XML \_**  
ID di risorsa per la stringa "tipo di risorsa" usato nei dati XML.

<span id="XML_ELEMENT_OFFSET"></span><span id="xml_element_offset"></span>**\_offset elemento \_ XML**  
ID risorsa per la stringa "offset elemento" utilizzato nei dati XML.

<span id="XML_ELEMENT_WIDTH"></span><span id="xml_element_width"></span>**\_larghezza elemento \_ XML**  
ID di risorsa per la stringa "spessore elemento" utilizzato nei dati XML.

<span id="XML_FIRST_WITH_SLICE"></span><span id="xml_first_with_slice"></span>**\_prima XML \_ con \_ sezione**  
ID risorsa per la stringa "prima con sezione" usata nei dati XML.

<span id="XML_BORDER_COLOR"></span><span id="xml_border_color"></span>**\_colore bordo \_ XML**  
ID risorsa per la stringa "colore bordo" utilizzata nei dati XML.

<span id="XML_SLOT"></span><span id="xml_slot"></span>**\_slot XML**  
ID di risorsa per la stringa "slot" utilizzato nei dati XML.

<span id="XML_COMPARISON_FUNCTION"></span><span id="xml_comparison_function"></span>**\_funzione di confronto XML \_**  
ID risorsa per la stringa "funzione di confronto" usata nei dati XML.

<span id="XML_MAX_ANISOTROPY"></span><span id="xml_max_anisotropy"></span>**\_anisotropia Max \_ XML**  
ID di risorsa per la stringa "Max anisotropia" usato nei dati XML.

<span id="XML_MIP_LOD_BIAS"></span><span id="xml_mip_lod_bias"></span>**\_ \_ distorsione LOD XML MIP \_**  
ID risorsa per la stringa "MIP LOD bias" usato nei dati XML.

<span id="XML_FILL_MODE"></span><span id="xml_fill_mode"></span>**\_modalità di riempimento XML \_**  
ID risorsa per la stringa "modalità di riempimento" usata nei dati XML.

<span id="XML_CULL_MODE"></span><span id="xml_cull_mode"></span>**\_modalità di eliminazione XML \_**  
ID risorsa per la stringa "modalità di abbattimento" usata nei dati XML.

<span id="XML_FRONT_COUNTER_CLOCKWISE"></span><span id="xml_front_counter_clockwise"></span>**\_contatore XML anteriore in \_ \_ senso orario**  
ID di risorsa per la stringa "front counter in senso orario" utilizzato nei dati XML.

<span id="XML_DEPTH_BIAS"></span><span id="xml_depth_bias"></span>**\_distorsione della profondità XML \_**  
ID risorsa per la stringa "bias di profondità" usato nei dati XML.

<span id="XML_DEPTH_BIAS_CLAMP"></span><span id="xml_depth_bias_clamp"></span>**\_clamp di \_ distorsione della profondità XML \_**  
ID risorsa per la stringa "morsetto di distorsione di profondità" usato nei dati XML.

<span id="XML_SLOPE_SCALE_DEPTH_BIAS"></span><span id="xml_slope_scale_depth_bias"></span>**\_ \_ \_ distorsione profondità scala scala XML \_**  
ID risorsa per la stringa "distorsione della profondità della scala di inclinazione" usata nei dati XML.

<span id="XML_DEPTH_CLIP_ENABLE"></span><span id="xml_depth_clip_enable"></span>**\_ \_ Abilita clip di profondità XML \_**  
ID di risorsa per la stringa "Enable clip di profondità" usato nei dati XML.

<span id="XML_SCISSOR_ENABLE"></span><span id="xml_scissor_enable"></span>**\_Abilitazione della forbice XML \_**  
ID risorsa per la stringa "Scissor Enable" usata nei dati XML.

<span id="XML_MULTISAMPLE_ENABLE"></span><span id="xml_multisample_enable"></span>**\_Abilitazione di MULTISAMPLE XML \_**  
ID di risorsa per la stringa "multisample Enable" usato nei dati XML.

<span id="XML_ANTIALIASED_LINE_ENABLE"></span><span id="xml_antialiased_line_enable"></span>**\_Abilitazione della riga con antialias XML \_ \_**  
ID risorsa per la stringa "abilitata per la riga con antialias" usata nei dati XML.

<span id="XML_FUNCTION"></span><span id="xml_function"></span>**\_funzione XML**  
ID risorsa per la stringa "Function" usata nei dati XML.

<span id="XML_PASS_OP"></span><span id="xml_pass_op"></span>**\_operazione pass \_ XML**  
ID risorsa per la stringa "Pass op" utilizzato nei dati XML.

<span id="XML_FAIL_OP"></span><span id="xml_fail_op"></span>**\_operazione di errore XML \_**  
ID risorsa per la stringa "fail op" usato nei dati XML.

<span id="XML_DEPTH_FAIL_OP"></span><span id="xml_depth_fail_op"></span>**\_operazione di \_ errore \_ profondità XML**  
ID di risorsa per la stringa "Depth Fail op" usato nei dati XML.

<span id="XML_DEPTH_ENABLE"></span><span id="xml_depth_enable"></span>**\_Abilita profondità \_ XML**  
ID risorsa per la stringa "Depth Enable" usata nei dati XML.

<span id="XML_DEPTH_FUNCTION"></span><span id="xml_depth_function"></span>**\_funzione Depth \_ XML**  
ID risorsa per la stringa "Depth Function" usata nei dati XML.

<span id="XML_DEPTH_WRITE_MASK"></span><span id="xml_depth_write_mask"></span>**\_maschera di \_ scrittura \_ profondità XML**  
ID risorsa per la stringa "maschera di scrittura profondità" usata nei dati XML.

<span id="XML_STENCIL_ENABLE"></span><span id="xml_stencil_enable"></span>**\_ \_ Abilitazione stencil XML**  
ID di risorsa per la stringa "stencil Enable" usato nei dati XML.

<span id="XML_STENCIL_READ_MASK"></span><span id="xml_stencil_read_mask"></span>**\_maschera di \_ lettura \_ stencil XML**  
ID risorsa per la stringa "maschera di lettura stencil" utilizzata nei dati XML.

<span id="XML_STENCIL_WRITE_MASK"></span><span id="xml_stencil_write_mask"></span>**\_maschera di \_ scrittura \_ stencil XML**  
ID risorsa per la stringa "maschera di scrittura stencil" utilizzata nei dati XML.

<span id="XML_FIRST_ELEMENT"></span><span id="xml_first_element"></span>**\_primo \_ elemento XML**  
ID risorsa per la stringa "primo elemento" utilizzato nei dati XML.

<span id="XML_NUM_ELEMENTS"></span><span id="xml_num_elements"></span>**\_elementi num \_ XML**  
ID risorsa per la stringa "num elements" usato nei dati XML.

<span id="XML_FLAGS"></span><span id="xml_flags"></span>**\_flag XML**  
ID di risorsa per la stringa "Flags" utilizzato nei dati XML.

<span id="XML_W_SIZE"></span><span id="xml_w_size"></span>**\_dimensioni XML W \_**  
ID di risorsa per la stringa "w size" utilizzato nei dati XML.

<span id="XML_FIRST_2D_ARRAY_FACE"></span><span id="xml_first_2d_array_face"></span>**\_Primo \_ quadrante della \_ matrice \_ 2D XML**  
ID di risorsa per la stringa "primo volto di matrice 2D" usato nei dati XML.

<span id="XML_NUM_CUBES"></span><span id="xml_num_cubes"></span>**\_numeri di \_ cubo XML**  
ID risorsa per la stringa "num cubes" usata nei dati XML.

<span id="XML_FACTORY"></span><span id="xml_factory"></span>**\_Factory XML**  
ID risorsa per la stringa "Factory" utilizzata nei dati XML.

<span id="XML_ADAPTER"></span><span id="xml_adapter"></span>**\_Adapter XML**  
ID di risorsa per la stringa "adapter" utilizzato nei dati XML.

<span id="XML_HEIGHT"></span><span id="xml_height"></span>**\_altezza XML**  
ID risorsa per la stringa "height" usata nei dati XML.

<span id="XML_WIDTH"></span><span id="xml_width"></span>**\_larghezza XML**  
ID risorsa per la stringa "width" usata nei dati XML.

<span id="XML_CREATION_FLAGS"></span><span id="xml_creation_flags"></span>**\_flag di creazione XML \_**  
ID risorsa per la stringa "flag di creazione" usati nei dati XML.

<span id="XML_SDK_VERSION"></span><span id="xml_sdk_version"></span>**versione di XML \_ SDK \_**  
ID risorsa per la stringa "versione SDK" usata nei dati XML.

<span id="XML_DRIVER_TYPE"></span><span id="xml_driver_type"></span>**\_tipo di driver XML \_**  
ID risorsa per la stringa "tipo di driver" utilizzato nei dati XML.

<span id="XML_FEATURE_LEVEL"></span><span id="xml_feature_level"></span>**\_livello di funzionalità XML \_**  
ID risorsa per la stringa "livello di funzionalità" usato nei dati XML.

<span id="XML_IMMEDIATE_CONTEXT"></span><span id="xml_immediate_context"></span>**\_contesto XML immediato \_**  
ID risorsa per la stringa "contesto immediato" usato nei dati XML.

<span id="XML_EXCEPTION_MODE"></span><span id="xml_exception_mode"></span>**\_modalità di eccezione XML \_**  
ID risorsa per la stringa "Exception Mode" usata nei dati XML.

<span id="XML_REFRESH_RATE"></span><span id="xml_refresh_rate"></span>**\_frequenza di aggiornamento XML \_**  
ID risorsa per la stringa "frequenza di aggiornamento" usata nei dati XML.

<span id="XML_SCANLINE_ORDERING"></span><span id="xml_scanline_ordering"></span>**\_ordinamento SCANLINE \_ XML**  
ID di risorsa per la stringa "scanline ordering" usato nei dati XML.

<span id="XML_SCALING"></span><span id="xml_scaling"></span>**\_ridimensionamento XML**  
ID risorsa per il "ridimensionamento" della stringa utilizzato nei dati XML.

<span id="XML_USAGE"></span><span id="xml_usage"></span>**\_utilizzo XML**  
ID risorsa per la stringa "Usage" usata nei dati XML.

<span id="XML_BUFFER_COUNT"></span><span id="xml_buffer_count"></span>**\_conteggio buffer \_ XML**  
ID risorsa per la stringa "conteggio buffer" utilizzato nei dati XML.

<span id="XML_WINDOWED"></span><span id="xml_windowed"></span>**\_finestra XML**  
ID risorsa per la stringa "finestra" utilizzata nei dati XML.

<span id="XML_SWAP_EFFECT"></span><span id="xml_swap_effect"></span>**\_effetto swapping \_ XML**  
ID risorsa per la stringa "effetto di scambio" utilizzato nei dati XML.

<span id="XML_STREAM"></span><span id="xml_stream"></span>**\_flusso XML**  
ID di risorsa per la stringa "Stream" utilizzato nei dati XML.

<span id="XML_OFFSET"></span><span id="xml_offset"></span>**\_offset XML**  
ID risorsa per la stringa "offset" utilizzato nei dati XML.

<span id="XML_TYPE"></span><span id="xml_type"></span>**\_tipo XML**  
ID di risorsa per la stringa "Type" utilizzato nei dati XML.

<span id="XML_METHOD"></span><span id="xml_method"></span>**\_metodo XML**  
ID di risorsa per la stringa "Method" utilizzato nei dati XML.

<span id="XML_USAGE_INDEX"></span><span id="xml_usage_index"></span>**\_indice di utilizzo XML \_**  
ID risorsa per la stringa "Usage index" usata nei dati XML.

<span id="XML_BLEND_SOURCE"></span><span id="xml_blend_source"></span>**\_origine XML Blend \_**  
ID risorsa per la stringa "origine di Blend" usata nei dati XML.

<span id="XML_BLEND_DESTINATION"></span><span id="xml_blend_destination"></span>**\_destinazione di Blend XML \_**  
ID risorsa per la stringa "destinazione Blend" usata nei dati XML.

<span id="XML_BLEND_OP"></span><span id="xml_blend_op"></span>**\_operazione di Blend XML \_**  
ID di risorsa per la stringa "Blend op" usato nei dati XML.

<span id="XML_ALPHA_BLEND_SOURCE"></span><span id="xml_alpha_blend_source"></span>**\_origine XML Alpha \_ Blend \_**  
ID di risorsa per la stringa "Alpha Blend source" usato nei dati XML.

<span id="XML_ALPHA_BLEND_DEST"></span><span id="xml_alpha_blend_dest"></span>**\_ \_ dest Blend XML \_ Alpha**  
ID di risorsa per la stringa "dest Blend Alpha" usato nei dati XML.

<span id="XML_ALPHA_BLEND_OP"></span><span id="xml_alpha_blend_op"></span>**\_operazione Alpha \_ Blend \_ XML**  
ID risorsa per la stringa "Alpha Blend op" usato nei dati XML.

<span id="XML_RENDER_TARGET_WRITE_MASK"></span><span id="xml_render_target_write_mask"></span>**\_maschera di \_ scrittura della destinazione di rendering XML \_ \_**  
ID risorsa per la stringa "maschera di scrittura della destinazione di rendering" utilizzata nei dati XML.

<span id="XML_BLEND_ENABLE"></span><span id="xml_blend_enable"></span>**\_Abilitazione di Blend XML \_**  
ID risorsa per la stringa "Blend Enable" usata nei dati XML.

<span id="XML_ALPHA_TO_COVERAGE_ENABLE"></span><span id="xml_alpha_to_coverage_enable"></span>**\_ \_ Abilitazione di XML Alpha to \_ coverage \_**  
ID di risorsa per la stringa "Alpha to coverage Enable" usato nei dati XML.

<span id="XML_BLEND_STATE_1"></span><span id="xml_blend_state_1"></span>**\_Stato di Blend XML \_ \_ 1**  
ID di risorsa per la stringa "Blend state 1" usato nei dati XML.

<span id="XML_INDEPENDENT_BLEND_ENABLE"></span><span id="xml_independent_blend_enable"></span>**\_ \_ Abilitazione di Blend indipendente da XML \_**  
ID risorsa per la stringa "Abilita Blend indipendente" utilizzata nei dati XML.

<span id="XML_RENDER_TARGET_NUMBER"></span><span id="xml_render_target_number"></span>**\_ \_ numero destinazione rendering \_ XML**  
ID risorsa per la stringa "numero destinazione di rendering" utilizzato nei dati XML.

<span id="XML_BUFFER"></span><span id="xml_buffer"></span>**\_buffer XML**  
ID di risorsa per la stringa "buffer" utilizzato nei dati XML.

<span id="XML_PRIMITIVE_TOPOLOGY"></span><span id="xml_primitive_topology"></span>**\_topologia primitiva XML \_**  
ID risorsa per la stringa "topologia primitiva" usata nei dati XML.

<span id="XML_STRIDE"></span><span id="xml_stride"></span>**\_stride XML**  
ID risorsa per la stringa "stride" usato nei dati XML.

<span id="XML_TOP"></span><span id="xml_top"></span>**\_inizio XML**  
ID di risorsa per la stringa "Top" utilizzato nei dati XML.

<span id="XML_RIGHT"></span><span id="xml_right"></span>**\_diritto XML**  
ID di risorsa per la stringa "Right" utilizzato nei dati XML.

<span id="XML_BOTTOM"></span><span id="xml_bottom"></span>**in \_ basso XML**  
ID risorsa per la stringa "bottom" usata nei dati XML.

<span id="XML_LEFT"></span><span id="xml_left"></span>**XML a \_ sinistra**  
ID di risorsa per la stringa "left" usato nei dati XML.

<span id="XML_TOP_LEFT_X"></span><span id="xml_top_left_x"></span>**XML in \_ alto a \_ sinistra \_ X**  
ID di risorsa per la stringa "top left x" usato nei dati XML.

<span id="XML_TOP_LEFT_Y"></span><span id="xml_top_left_y"></span>**XML in \_ alto a \_ sinistra \_ Y**  
ID di risorsa per la stringa "top left y" usato nei dati XML.

<span id="XML_MIN_DEPTH"></span><span id="xml_min_depth"></span>**\_profondità minima \_ XML**  
ID risorsa per la stringa "profondità minima" usata nei dati XML.

<span id="XML_MAX_DEPTH"></span><span id="xml_max_depth"></span>**\_profondità massima \_ XML**  
ID risorsa per la stringa "profondità massima" utilizzata nei dati XML.

<span id="XML_STENCIL_REFERENCE_VALUE"></span><span id="xml_stencil_reference_value"></span>**\_valore di \_ riferimento per stencil XML \_**  
ID risorsa per la stringa "valore di riferimento stencil" utilizzato nei dati XML.

<span id="XML_DEPTH_BUFFER"></span><span id="xml_depth_buffer"></span>**\_buffer di profondità XML \_**  
ID risorsa per la stringa "buffer di profondità" usato nei dati XML.

<span id="XML_BLEND_FACTOR"></span><span id="xml_blend_factor"></span>**\_fattore di Blend XML \_**  
ID risorsa per la stringa "fattore di Blend" usato nei dati XML.

<span id="XML_SAMPLE_MASK"></span><span id="xml_sample_mask"></span>**\_maschera di esempio XML \_**  
ID di risorsa per la stringa "Sample mask" usato nei dati XML.

<span id="XML_DEPTH_STENCIL_STATE"></span><span id="xml_depth_stencil_state"></span>**\_ \_ stato stencil profondità \_ XML**  
ID risorsa per la stringa "depth stencil stato" utilizzato nei dati XML.

<span id="XML_SCISSOR_RECT"></span><span id="xml_scissor_rect"></span>**\_rettangolo a forbice XML \_**  
ID risorsa per la stringa "Scissor Rect" usato nei dati XML.

<span id="XML_SAMPLER"></span><span id="xml_sampler"></span>**\_campionatore XML**  
ID di risorsa per la stringa "Sampler" usato nei dati XML.

<span id="XML_SHADER"></span><span id="xml_shader"></span>**\_shader XML**  
ID risorsa per la stringa "shader" utilizzato nei dati XML.

<span id="XML_DEPTH_STENCIL_VIEW"></span><span id="xml_depth_stencil_view"></span>**\_ \_ visualizzazione stencil profondità \_ XML**  
ID di risorsa per la stringa "depth stencil View" usato nei dati XML.

<span id="XML_SHADER_RESOURCE_VIEW"></span><span id="xml_shader_resource_view"></span>**\_ \_ visualizzazione risorse shader XML \_**  
ID risorsa per la stringa "Visualizzazione risorse shader" utilizzato nei dati XML.

<span id="XML_INPUT_LAYOUT"></span><span id="xml_input_layout"></span>**\_layout di input XML \_**  
ID di risorsa per la stringa "layout di input" usato nei dati XML.

<span id="XML_CLASS_INSTANCE"></span><span id="xml_class_instance"></span>**\_istanza di classe XML \_**  
ID risorsa per la stringa "istanza di classe" utilizzata nei dati XML.

<span id="XML_STREAM_OUTPUT_BUFFER"></span><span id="xml_stream_output_buffer"></span>**\_buffer di \_ output del flusso XML \_**  
ID risorsa per la stringa "buffer output flusso" utilizzato nei dati XML.

<span id="XML_SHADER_RESOURCE"></span><span id="xml_shader_resource"></span>**\_risorsa shader XML \_**  
ID risorsa per la stringa "Resource shader" usato nei dati XML.

<span id="XML_RENDER_TARGET_INSTANCE"></span><span id="xml_render_target_instance"></span>**\_istanza di \_ destinazione di rendering XML \_**  
ID risorsa per la stringa "istanza di destinazione di rendering" utilizzata nei dati XML.

<span id="XML_VERTEX"></span><span id="xml_vertex"></span>**\_vertice XML**  
ID di risorsa per la stringa "Vertex" usato nei dati XML.

<span id="XML_VERTEX_BUFFER"></span><span id="xml_vertex_buffer"></span>**\_buffer vertex \_ XML**  
ID di risorsa per la stringa "vertex buffer" utilizzato nei dati XML.

<span id="XML_INSTANCE"></span><span id="xml_instance"></span>**\_istanza XML**  
ID risorsa per la stringa "instance" usata nei dati XML.

<span id="XML_VIEWPORT"></span><span id="xml_viewport"></span>**\_viewport XML**  
ID di risorsa per la stringa "viewport" utilizzato nei dati XML.

<span id="XML_FIRSTCONSTANT"></span><span id="xml_firstconstant"></span>**\_FIRSTCONSTANT XML**  
ID di risorsa per la stringa "firstconstant" utilizzato nei dati XML.

<span id="XML_NUMCONSTANTS"></span><span id="xml_numconstants"></span>**\_NUMCONSTANTS XML**  
ID di risorsa per la stringa "numconstants" utilizzato nei dati XML.

<span id="XML_LOGIC_OP"></span><span id="xml_logic_op"></span>**\_operazione logica \_ XML**  
ID risorsa per la stringa "logica op" usata nei dati XML.

<span id="XML_LOGICOPS_ENABLE"></span><span id="xml_logicops_enable"></span>**\_Abilitazione di LOGICOPS XML \_**  
ID di risorsa per la stringa "logicops Enable" usato nei dati XML.

<span id="XML_SHADER_FILE"></span><span id="xml_shader_file"></span>**\_file shader XML \_**  
ID risorsa per la stringa "file shader" utilizzato nei dati XML.

<span id="XML_FILE_NAME"></span><span id="xml_file_name"></span>**\_nome file \_ XML**  
ID risorsa per la stringa "nome file" utilizzato nei dati XML.

<span id="XML_10TEXTURE1D_DESC"></span><span id="xml_10texture1d_desc"></span>**\_10TEXTURE1D XML \_ DESC**  
ID di risorsa per la stringa "10texture1d DESC" usato nei dati XML.

<span id="XML_10TEXTURE2D_DESC"></span><span id="xml_10texture2d_desc"></span>**\_10TEXTURE2D XML \_ DESC**  
ID di risorsa per la stringa "10texture2d DESC" usato nei dati XML.

<span id="XML_10TEXTURE3D_DESC"></span><span id="xml_10texture3d_desc"></span>**\_10TEXTURE3D XML \_ DESC**  
ID di risorsa per la stringa "10texture3d DESC" usato nei dati XML.

<span id="XML_TEXTURE_ARRAYSIZE"></span><span id="xml_texture_arraysize"></span>**\_trama XML \_ ARRAYSIZE**  
ID risorsa per la stringa "texture arraySize" usata nei dati XML.

<span id="XML_TEXTURE_MIPLEVEL"></span><span id="xml_texture_miplevel"></span>**\_trama XML \_ MIPLEVEL**  
ID risorsa per la stringa "texture miplevel" usata nei dati XML.

<span id="XML_CPUFLAGS"></span><span id="xml_cpuflags"></span>**\_CPUFLAGS XML**  
ID di risorsa per la stringa "cpuflags" utilizzato nei dati XML.

<span id="XML_MISCFLAGS"></span><span id="xml_miscflags"></span>**\_MISCFLAGS XML**  
ID di risorsa per la stringa "MiscFlags" utilizzato nei dati XML.

<span id="XML_BINDFLAGS"></span><span id="xml_bindflags"></span>**\_BINDFLAGS XML**  
ID di risorsa per la stringa "bindflags" utilizzato nei dati XML.

<span id="XML_SAMPLEDESC_QUALITY"></span><span id="xml_sampledesc_quality"></span>**\_qualità SAMPLEDESC \_ XML**  
ID risorsa per la stringa "SampleDesc Quality" usata nei dati XML.

<span id="XML_SAMPLEDESC_COUNT"></span><span id="xml_sampledesc_count"></span>**\_conteggio SAMPLEDESC \_ XML**  
ID risorsa per la stringa "SampleDesc count" usata nei dati XML.

<span id="XML_DEPTH"></span><span id="xml_depth"></span>**\_profondità XML**  
ID risorsa per la stringa "Depth" usata nei dati XML.

<span id="XML_11TEXTURE1D_DESC"></span><span id="xml_11texture1d_desc"></span>**\_11TEXTURE1D XML \_ DESC**  
ID di risorsa per la stringa "11texture1d DESC" usato nei dati XML.

<span id="XML_11TEXTURE2D_DESC"></span><span id="xml_11texture2d_desc"></span>**\_11TEXTURE2D XML \_ DESC**  
ID di risorsa per la stringa "11texture2d DESC" usato nei dati XML.

<span id="XML_11TEXTURE3D_DESC"></span><span id="xml_11texture3d_desc"></span>**\_11TEXTURE3D XML \_ DESC**  
ID di risorsa per la stringa "11texture3d DESC" usato nei dati XML.

<span id="XML_BYTEWIDTH"></span><span id="xml_bytewidth"></span>**\_BYTEWIDTH XML**  
ID di risorsa per la stringa "bytewidth" utilizzato nei dati XML.

<span id="XML_10BUFFER_DESC"></span><span id="xml_10buffer_desc"></span>**\_10BUFFER XML \_ DESC**  
ID di risorsa per la stringa "10buffer DESC" usato nei dati XML.

<span id="XML_11BUFFER_DESC"></span><span id="xml_11buffer_desc"></span>**\_11BUFFER XML \_ DESC**  
ID di risorsa per la stringa "11Buffer DESC" usato nei dati XML.

<span id="XML_BYTESTRIDE"></span><span id="xml_bytestride"></span>**\_BYTESTRIDE XML**  
ID di risorsa per la stringa "bytestride" utilizzato nei dati XML.

<span id="XML_STEREO"></span><span id="xml_stereo"></span>**XML \_ stereo**  
ID di risorsa per la stringa "stereo" utilizzato nei dati XML.

<span id="XML_BACKGROUNDCOLOR"></span><span id="xml_backgroundcolor"></span>**\_BACKGROUNDCOLOR XML**  
ID risorsa per la stringa "BackgroundColor" utilizzato nei dati XML.

<span id="XML_ALPHA_MODE"></span><span id="xml_alpha_mode"></span>**\_modalità XML Alpha \_**  
ID risorsa per la stringa "Alpha Mode" usata nei dati XML.

<span id="XML_RESTRICTTOOUTPUT"></span><span id="xml_restricttooutput"></span>**\_RESTRICTTOOUTPUT XML**  
ID di risorsa per la stringa "restricttooutput" utilizzato nei dati XML.

<span id="XML_MODEROTATION"></span><span id="xml_moderotation"></span>**\_MODEROTATION XML**  
ID di risorsa per la stringa "moderotation" utilizzato nei dati XML.

<span id="XML_ISTEMPORARYMONOSUPPORTED"></span><span id="xml_istemporarymonosupported"></span>**\_ISTEMPORARYMONOSUPPORTED XML**  
ID di risorsa per la stringa "istemporarymonosupported" utilizzato nei dati XML.

<span id="XML_FORCED_SAMPLE_COUNT"></span><span id="xml_forced_sample_count"></span>**\_conteggio campione forzato XML \_ \_**  
ID risorsa per la stringa "numero di campioni forzato" usato nei dati XML.

<span id="XML_QUERY"></span><span id="xml_query"></span>**\_query XML**  
ID risorsa per la stringa "query" utilizzata nei dati XML.

<span id="XML_10QUERY_DESC"></span><span id="xml_10query_desc"></span>**\_10QUERY XML \_ DESC**  
ID di risorsa per la stringa "10query DESC" usato nei dati XML.

<span id="XML_11QUERY_DESC"></span><span id="xml_11query_desc"></span>**\_11QUERY XML \_ DESC**  
ID di risorsa per la stringa "11query DESC" usato nei dati XML.

<span id="XML_FLAGS_NONE"></span><span id="xml_flags_none"></span>**\_flag XML \_ None**  
ID risorsa per la stringa "flag None" utilizzato nei dati XML.

<span id="XML_BUFFERUSAGE"></span><span id="xml_bufferusage"></span>**\_BUFFERUSAGE XML**  
ID di risorsa per la stringa "BufferUsage" utilizzato nei dati XML.

<span id="XML_UNKNOWN"></span><span id="xml_unknown"></span>**XML \_ sconosciuto**  
ID risorsa per la stringa "Unknown" utilizzata nei dati XML.

<span id="XML_MAXFRAMELATENCY"></span><span id="xml_maxframelatency"></span>**\_MAXFRAMELATENCY XML**  
ID di risorsa per la stringa "maxframelatency" utilizzato nei dati XML.

<span id="XML_SOURCESIZEWIDTH"></span><span id="xml_sourcesizewidth"></span>**\_SOURCESIZEWIDTH XML**  
ID di risorsa per la stringa "sourcesizewidth" utilizzato nei dati XML.

<span id="XML_SOURCESIZEHEIGHT"></span><span id="xml_sourcesizeheight"></span>**\_SOURCESIZEHEIGHT XML**  
ID di risorsa per la stringa "sourcesizeheight" utilizzato nei dati XML.

<span id="XML_MATRIXTRANSFORM"></span><span id="xml_matrixtransform"></span>**\_MATRIXTRANSFORM XML**  
ID di risorsa per la stringa "MatrixTransform" utilizzato nei dati XML.

<span id="XML_TILED_RESOURCE_INFO"></span><span id="xml_tiled_resource_info"></span>**\_ \_ informazioni sulle risorse AFFIANCAte XML \_**  
ID risorsa per la stringa "informazioni sulle risorse affiancate" usato nei dati XML.

<span id="XML_TILES_RESOURCE_SUBRESOURCE_TILINGS"></span><span id="xml_tiles_resource_subresource_tilings"></span>**\_ \_ affiancamenti delle risorse delle risorse della risorsa TILEs XML \_ \_**  
ID di risorsa per la stringa "riquadri affiancamenti risorse risorse" usati nei dati XML.

<span id="XML_DEVICE_CHILD_INFORMATION"></span><span id="xml_device_child_information"></span>**\_ \_ informazioni figlio dispositivo \_ XML**  
ID risorsa per la stringa "informazioni figlio dispositivo" utilizzato nei dati XML.

<span id="XML_DEVICE"></span><span id="xml_device"></span>**\_dispositivo XML**  
ID risorsa per la stringa "Device" usata nei dati XML.

<span id="XML_OTHER"></span><span id="xml_other"></span>**\_altro XML**  
ID di risorsa per la stringa "other" utilizzato nei dati XML.

<span id="XML_PREDICATE"></span><span id="xml_predicate"></span>**\_predicato XML**  
ID di risorsa per la stringa "Predicate" usato nei dati XML.

<span id="XML_PREDICATE_VALUE"></span><span id="xml_predicate_value"></span>**\_valore del predicato XML \_**  
ID risorsa per la stringa "valore predicato" usato nei dati XML.

<span id="XML_DEVICE_CONTEXT_TYPE"></span><span id="xml_device_context_type"></span>**\_tipo di \_ contesto di dispositivo XML \_**  
ID risorsa per la stringa "tipo di contesto di dispositivo" usato nei dati XML.

<span id="XML_BUFFER_HANDLE"></span><span id="xml_buffer_handle"></span>**\_handle del buffer XML \_**  
ID risorsa per la stringa "handle del buffer" utilizzato nei dati XML.

<span id="XML_DEVICE_12"></span><span id="xml_device_12"></span>**\_Dispositivo XML \_ 12**  
ID risorsa per la stringa "Device 12" usato nei dati XML.

<span id="XML_MAX_RECORDING_COMMAND_LISTS"></span><span id="xml_max_recording_command_lists"></span>**\_elenchi di \_ comandi di registrazione max \_ XML \_**  
ID di risorsa per la stringa "Max Registration Command lists" usato nei dati XML.

<span id="XML_DEFAULT_COMMAND_QUEUE"></span><span id="xml_default_command_queue"></span>**\_ \_ coda comandi predefinita \_ XML**  
ID risorsa per la stringa "default Command Queue" usato nei dati XML.

<span id="XML_D3D11_DEVICE_INFORMATION"></span><span id="xml_d3d11_device_information"></span>**\_ \_ Informazioni sul dispositivo D3D11 XML \_**  
ID risorsa per la stringa "informazioni sul dispositivo D3D11" usato nei dati XML.

<span id="XML_D3D12_DEVICE_INFORMATION"></span><span id="xml_d3d12_device_information"></span>**\_ \_ Informazioni sul dispositivo D3D12 XML \_**  
ID risorsa per la stringa "informazioni sul dispositivo d3d12" usato nei dati XML.

<span id="XML_CONSERVATIVE_RASTER"></span><span id="xml_conservative_raster"></span>**\_raster conservativo XML \_**  
ID di risorsa per la stringa "raster conservabile" usato nei dati XML.

<span id="XML_INPUT_LAYOUT_12"></span><span id="xml_input_layout_12"></span>**\_Layout di input XML \_ \_ 12**  
ID di risorsa per la stringa "input layout 12" utilizzato nei dati XML.

<span id="XML_GRAPHICS_PIPELINE_STATE_12"></span><span id="xml_graphics_pipeline_state_12"></span>**\_Stato della pipeline di grafica XML \_ \_ \_ 12**  
ID risorsa per la stringa "stato della pipeline grafica 12" usato nei dati XML.

<span id="XML_BLEND_STATE_12"></span><span id="xml_blend_state_12"></span>**\_Stato di Blend XML \_ \_ 12**  
ID risorsa per la stringa "Blend state 12" usata nei dati XML.

<span id="XML_ROOT_SIGNATURE_12"></span><span id="xml_root_signature_12"></span>**\_Firma radice \_ XML \_ 12**  
ID risorsa per la stringa "firma radice 12" usata nei dati XML.

<span id="XML_STREAM_OUTPUT_12"></span><span id="xml_stream_output_12"></span>**\_Output flusso \_ XML \_ 12**  
ID di risorsa per la stringa "Stream output 12" usato nei dati XML.

<span id="XML_RASTERIZED_STREAM"></span><span id="xml_rasterized_stream"></span>**\_flusso rasterizzato \_ XML**  
ID risorsa per la stringa "flusso rasterizzato" usato nei dati XML.

<span id="XML_SO_DECLARATION_ENTRY_12"></span><span id="xml_so_declaration_entry_12"></span>**XML \_ , \_ voce di Dichiarazione \_ \_ 12**  
ID di risorsa per la stringa "so entry della dichiarazione 12" usato nei dati XML.

<span id="XML_START_COMPONENT"></span><span id="xml_start_component"></span>**\_componente di avvio XML \_**  
ID risorsa per la stringa "componente iniziale" utilizzato nei dati XML.

<span id="XML_COMPONENT_COUNT"></span><span id="xml_component_count"></span>**\_conteggio componenti \_ XML**  
ID risorsa per la stringa "numero di componenti" usato nei dati XML.

<span id="XML_OUTPUT_SLOT"></span><span id="xml_output_slot"></span>**\_slot di output XML \_**  
ID risorsa per la stringa "slot di output" usato nei dati XML.

<span id="XML_COMPUTE_PIPELINE_STATE_12"></span><span id="xml_compute_pipeline_state_12"></span>**\_Stato della pipeline di calcolo XML \_ \_ \_ 12**  
ID risorsa per la stringa "Compute pipeline state 12" usato nei dati XML.

<span id="XML_D3D12_SHADER_BYTECODE"></span><span id="xml_d3d12_shader_bytecode"></span>**\_BYTECODE dello \_ shader D3D12 \_ XML**  
ID risorsa per la stringa "bytecode shader d3d12" usato nei dati XML.

<span id="XML_D3D12_SHADER_BYTECODE_PTR"></span><span id="xml_d3d12_shader_bytecode_ptr"></span>**\_PTR del \_ BYTECODE dello shader XML D3D12 \_ \_**  
ID risorsa per la stringa "d3d12 shader bytecode PTR" usato nei dati XML.

<span id="XML_D3D12_SHADER_BYTECODE_LENGTH"></span><span id="xml_d3d12_shader_bytecode_length"></span>**\_ \_ \_ Lunghezza BYTECODE XML D3D12 shader \_**  
ID risorsa per la stringa "lunghezza bytecode d3d12 shader" usata nei dati XML.

<span id="XML_D3D12_VIEW_DIMENSION"></span><span id="xml_d3d12_view_dimension"></span>**\_Dimensione di \_ visualizzazione \_ D3D12 XML**  
ID di risorsa per la stringa "d3d12 View Dimension" usato nei dati XML.

<span id="XML_D3D12_NOTHING_TO_DEFINE"></span><span id="xml_d3d12_nothing_to_define"></span>**XML \_ D3D12 \_ nulla \_ da \_ definire**  
ID risorsa per la stringa "d3d12 Nothing to define" usato nei dati XML.

<span id="XML_OFFSET_IN_BYTES"></span><span id="xml_offset_in_bytes"></span>**\_offset XML \_ in \_ byte**  
ID risorsa per la stringa "offset in byte" utilizzato nei dati XML.

<span id="XML_SIZE_IN_BYTES"></span><span id="xml_size_in_bytes"></span>**\_dimensioni XML \_ in \_ byte**  
ID di risorsa per la stringa "size in bytes" usata nei dati XML.

<span id="XML_SO_CUR_OFFSET"></span><span id="xml_so_cur_offset"></span>**OFFSET XML in \_ cui \_ cur \_**  
ID di risorsa per la stringa "quindi offset cur" usato nei dati XML.

<span id="XML_ROOT_SIGNATURE"></span><span id="xml_root_signature"></span>**\_firma radice \_ XML**  
ID risorsa per la stringa "firma radice" usata nei dati XML.

<span id="XML_NUM_PARAMETERS"></span><span id="xml_num_parameters"></span>**\_parametri num \_ XML**  
ID di risorsa per la stringa "num Parameters" usato nei dati XML.

<span id="XML_ROOT_PARAMETERS"></span><span id="xml_root_parameters"></span>**\_parametri radice \_ XML**  
ID di risorsa per la stringa "Parameters radice" usato nei dati XML.

<span id="XML_ROOT_PARAMETER"></span><span id="xml_root_parameter"></span>**\_parametro radice \_ XML**  
ID risorsa per la stringa "parametro radice" usato nei dati XML.

<span id="XML_ROOT_PARAMETER_TYPE"></span><span id="xml_root_parameter_type"></span>**\_tipo di \_ parametro \_ radice XML**  
ID risorsa per la stringa "tipo di parametro radice" usato nei dati XML.

<span id="XML_ROOT_PARAMETER_SHADER_VISIBILITY"></span><span id="xml_root_parameter_shader_visibility"></span>**\_ \_ \_ visibilità shader parametri radice \_ XML**  
ID di risorsa per la stringa "root Parameter shader Visibility" usato nei dati XML.

<span id="XML_DESCRIPTOR_HANDLE"></span><span id="xml_descriptor_handle"></span>**\_handle descrittore XML \_**  
ID risorsa per la stringa "handle descrittore" usato nei dati XML.

<span id="XML_DESCRIPTOR_HANDLE_PTR"></span><span id="xml_descriptor_handle_ptr"></span>**\_handle descrittore XML \_ \_ ptr**  
ID risorsa per la stringa "descrittore handle PTR" utilizzato nei dati XML.

<span id="XML_ROOT_CONSTANTS"></span><span id="xml_root_constants"></span>**\_costanti radice \_ XML**  
ID di risorsa per la stringa "costanti radice" utilizzato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR"></span><span id="xml_root_descriptor"></span>**\_descrittore radice XML \_**  
ID risorsa per la stringa "descrittore radice" utilizzato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_SHADER_REGISTER"></span><span id="xml_root_descriptor_shader_register"></span>**\_ \_ Registro shader del descrittore radice XML \_ \_**  
ID risorsa per la stringa "registro shader del descrittore radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_REGISTER_SPACE"></span><span id="xml_root_descriptor_register_space"></span>**\_ \_ spazio registro del descrittore radice XML \_ \_**  
ID risorsa per la stringa "spazio del registro del descrittore radice" utilizzato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_TABLE"></span><span id="xml_root_descriptor_table"></span>**\_ \_ tabella descrittore radice XML \_**  
ID risorsa per la stringa "tabella descrittore radice" utilizzata nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_NUM_RANGES"></span><span id="xml_root_descriptor_num_ranges"></span>**\_ \_ intervalli numeri descrittore radice XML \_ \_**  
ID risorsa per la stringa "numero di descrittori radice numero di intervalli" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGES"></span><span id="xml_root_descriptor_ranges"></span>**\_ \_ intervalli descrittore radice XML \_**  
ID risorsa per la stringa "intervalli descrittori radice" usati nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE"></span><span id="xml_root_descriptor_range"></span>**\_ \_ intervallo descrittore radice XML \_**  
ID risorsa per la stringa "intervallo descrittore radice" utilizzato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_TYPE"></span><span id="xml_root_descriptor_range_type"></span>**\_tipo di \_ intervallo descrittore radice XML \_ \_**  
ID risorsa per la stringa "tipo di intervallo descrittore radice" utilizzato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_COUNT"></span><span id="xml_root_descriptor_range_count"></span>**\_ \_ conteggio intervalli descrittori radice XML \_ \_**  
ID risorsa per la stringa "conteggio intervalli descrittori radice" usato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_BASE_SHADER_REGISTER"></span><span id="xml_root_descriptor_range_base_shader_register"></span>**\_ \_ \_ \_ Registro shader di base intervallo \_ descrittore radice XML \_**  
ID risorsa per la stringa "registro radice dello shader di base dell'intervallo di descrittore" utilizzato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_REGISTER_SPACE"></span><span id="xml_root_descriptor_range_register_space"></span>**\_spazio del \_ \_ registro intervalli \_ DEscrittori radice XML \_**  
ID di risorsa per la stringa "spazio del registro dell'intervallo descrittore radice" utilizzato nei dati XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_OFFSET"></span><span id="xml_root_descriptor_range_offset"></span>**\_ \_ offset intervallo descrittore radice XML \_ \_**  
ID risorsa per la stringa "offset intervallo descrittore radice" utilizzato nei dati XML.

<span id="XML_ROOT_CONSTANTS_SHADER_REGISTER"></span><span id="xml_root_constants_shader_register"></span>**\_ \_ Registro shader delle costanti radice \_ \_ XML**  
ID risorsa per la stringa "root Constants shader Register" usato nei dati XML.

<span id="XML_ROOT_CONSTANTS_REGISTER_SPACE"></span><span id="xml_root_constants_register_space"></span>**\_ \_ spazio registro delle costanti \_ radice \_ XML**  
ID risorsa per la stringa "lo spazio del registro delle costanti radice" usato nei dati XML.

<span id="XML_ROOT_CONSTANTS_INT32_COUNT"></span><span id="xml_root_constants_int32_count"></span>**\_ \_ Conteggio Int32 delle costanti \_ radice \_ XML**  
ID risorsa per la stringa "numero Int32 costanti radice" utilizzato nei dati XML.

<span id="XML_NOT_SET"></span><span id="xml_not_set"></span>**XML \_ non \_ impostato**  
ID risorsa per la stringa "not set" utilizzato nei dati XML.

<span id="XML_ERROR_RETRIEVING_VALUE"></span><span id="xml_error_retrieving_value"></span>**\_errore XML durante il recupero del \_ \_ valore**  
ID risorsa per la stringa "errore durante il recupero del valore" utilizzato nei dati XML.

<span id="XML_VERTEX_SHADER"></span><span id="xml_vertex_shader"></span>**\_Vertex \_ shader XML**  
ID risorsa per la stringa "vertex shader" usato nei dati XML.

<span id="XML_PIXEL_SHADER"></span><span id="xml_pixel_shader"></span>**\_pixel \_ shader XML**  
ID di risorsa per la stringa "pixel shader" utilizzato nei dati XML.

<span id="XML_DOMAIN_SHADER"></span><span id="xml_domain_shader"></span>**\_shader del dominio XML \_**  
ID risorsa per la stringa "Domain shader" usato nei dati XML.

<span id="XML_HULL_SHADER"></span><span id="xml_hull_shader"></span>**\_Hull \_ shader XML**  
ID risorsa per la stringa "Hull shader" utilizzato nei dati XML.

<span id="XML_GEOMETRY_SHADER"></span><span id="xml_geometry_shader"></span>**\_shader XML Geometry \_**  
ID risorsa per la stringa "Geometry shader" usato nei dati XML.

<span id="XML_ROOT_SIGNATURE_OBJECT"></span><span id="xml_root_signature_object"></span>**\_ \_ oggetto firma radice \_ XML**  
ID risorsa per la stringa "oggetto della firma radice" utilizzato nei dati XML.

<span id="XML_ROOT_SIGNATURE_DESERIALIZED_DESC"></span><span id="xml_root_signature_deserialized_desc"></span>**\_Descrizione della \_ firma radice XML \_ deserializzata \_**  
ID risorsa per la stringa "deserializzazione della firma radice" desc utilizzata nei dati XML.

<span id="XML_COMMAND_QUEUE"></span><span id="xml_command_queue"></span>**\_coda comandi \_ XML**  
ID risorsa per la stringa "Command Queue" usata nei dati XML.

<span id="XML_D3D12_COMMAND_LIST_TYPE_TYPE"></span><span id="xml_d3d12_command_list_type_type"></span>**\_Tipo di \_ elenco di comandi D3D12 \_ \_ XML \_**  
ID risorsa per la stringa "tipo di tipo elenco di comandi d3d12" usato nei dati XML.

<span id="XML_COMMAND_QUEUE_PRIORITY"></span><span id="xml_command_queue_priority"></span>**\_priorità della \_ coda di comandi XML \_**  
ID risorsa per la stringa "priorità della coda dei comandi" usata nei dati XML.

<span id="XML_COMMAND_QUEUE_IS_DEFAULT"></span><span id="xml_command_queue_is_default"></span>**\_coda comandi \_ XML \_ \_ predefinita**  
ID risorsa per la stringa "Command queue is default" usato nei dati XML.

<span id="XML_COMMAND_ALLOCATOR"></span><span id="xml_command_allocator"></span>**\_allocatore di comandi XML \_**  
ID risorsa per la stringa "Command allocator" usato nei dati XML.

<span id="XML_D3D12_RESOURCE"></span><span id="xml_d3d12_resource"></span>**\_Risorsa D3D12 \_ XML**  
ID risorsa per la stringa "d3d12 Resource" usato nei dati XML.

<span id="XML_D3D12_DESCRIPTOR_HEAP"></span><span id="xml_d3d12_descriptor_heap"></span>**\_ \_ Heap descrittore D3D12 XML \_**  
ID risorsa per la stringa "heap descrittore d3d12" usato nei dati XML.

<span id="XML_D3D12_DESCRIPTOR"></span><span id="xml_d3d12_descriptor"></span>**\_Descrittore D3D12 XML \_**  
ID risorsa per la stringa "descrittore d3d12" usato nei dati XML.

<span id="XML_D3D12_RTV"></span><span id="xml_d3d12_rtv"></span>**\_RTV D3D12 \_ XML**  
ID di risorsa per la stringa "d3d12 RTV" usato nei dati XML.

<span id="XML_D3D12_VBV"></span><span id="xml_d3d12_vbv"></span>**\_VBV D3D12 \_ XML**  
ID risorsa per la stringa "d3d12 VBV" utilizzato nei dati XML.

<span id="XML_D3D12_IBV"></span><span id="xml_d3d12_ibv"></span>**\_IBV D3D12 \_ XML**  
ID risorsa per la stringa "d3d12 IBV" utilizzato nei dati XML.

<span id="XML_D3D12_CBV"></span><span id="xml_d3d12_cbv"></span>**\_CBV D3D12 \_ XML**  
ID risorsa per la stringa "d3d12 CBV" utilizzato nei dati XML.

<span id="XML_D3D12_SRV"></span><span id="xml_d3d12_srv"></span>**\_SRV D3D12 \_ XML**  
ID risorsa per la stringa "d3d12 SRV" utilizzato nei dati XML.

<span id="XML_D3D12_UAV"></span><span id="xml_d3d12_uav"></span>**\_UAV D3D12 \_ XML**  
ID risorsa per la stringa "d3d12 UAV" usato nei dati XML.

<span id="XML_D3D12_SOV"></span><span id="xml_d3d12_sov"></span>**\_SOV D3D12 \_ XML**  
ID risorsa per la stringa "d3d12 SOV" utilizzato nei dati XML.

<span id="XML_D3D12_SAMPLER"></span><span id="xml_d3d12_sampler"></span>**\_ \_ Campionatore D3D12 XML**  
ID risorsa per la stringa "d3d12 Sampler" usata nei dati XML.

<span id="XML_D3D12_DSV"></span><span id="xml_d3d12_dsv"></span>**\_Origine dati D3D12 XML \_**  
ID di risorsa per la stringa "d3d12 DSV" usato nei dati XML.

<span id="XML_TEXTURE_1D"></span><span id="xml_texture_1d"></span>**\_Trama XML \_ 1D**  
ID risorsa per la stringa "texture 1D" usata nei dati XML.

<span id="XML_TEXTURE_1DARRAY"></span><span id="xml_texture_1darray"></span>**\_Trama XML \_ 1DARRAY**  
ID risorsa per la stringa "texture 1darray" usata nei dati XML.

<span id="XML_TEXTURE_2D"></span><span id="xml_texture_2d"></span>**\_Trama XML \_ 2D**  
ID risorsa per la stringa "trama 2D" usata nei dati XML.

<span id="XML_TEXTURE_2DARRAY"></span><span id="xml_texture_2darray"></span>**\_Trama XML \_ 2DARRAY**  
ID risorsa per la stringa "texture 2darray" usata nei dati XML.

<span id="XML_TEXTURE_2DMS"></span><span id="xml_texture_2dms"></span>**\_Trama XML \_ 2DMS**  
ID risorsa per la stringa "texture 2dms" usata nei dati XML.

<span id="XML_TEXTURE_2DMSARRAY"></span><span id="xml_texture_2dmsarray"></span>**\_Trama XML \_ 2DMSARRAY**  
ID risorsa per la stringa "texture 2dmsarray" usata nei dati XML.

<span id="XML_TEXTURE_3D"></span><span id="xml_texture_3d"></span>**\_Trama XML \_ 3D**  
ID risorsa per la stringa "trama 3D" usata nei dati XML.

<span id="XML_MISCELLANEOUS"></span><span id="xml_miscellaneous"></span>**\_varie XML**  
ID di risorsa per la stringa "varie" utilizzato nei dati XML.

<span id="XML_RENDER_TARGET_VIEW_12"></span><span id="xml_render_target_view_12"></span>**\_ \_ Visualizzazione destinazione rendering \_ XML \_ 12**  
ID risorsa per la stringa "visualizzazione destinazione rendering 12" utilizzata nei dati XML.

<span id="XML_STRIDE_COUNT"></span><span id="xml_stride_count"></span>**\_conteggio XML stride \_**  
ID risorsa per la stringa "conteggio stride" utilizzato nei dati XML.

<span id="XML_SHADER_ROOT_SIGNATURES"></span><span id="xml_shader_root_signatures"></span>**\_ \_ firme radice XML \_ shader**  
ID risorsa per la stringa "firme radice shader" usato nei dati XML.

<span id="XML_CMDLIST_ROOT_SIGNATURE_OBJECT"></span><span id="xml_cmdlist_root_signature_object"></span>**\_oggetto della \_ \_ firma radice CMDLIST XML \_**  
ID risorsa per la stringa "cmdlist root Signature object" usato nei dati XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_COUNT"></span><span id="xml_input_layout_element_count"></span>**\_ \_ \_ conteggio elementi layout input \_ XML**  
ID risorsa per la stringa "numero di elementi del layout di input" usato nei dati XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_DESCRIPTIONS"></span><span id="xml_input_layout_element_descriptions"></span>**\_descrizioni degli \_ elementi di layout di input XML \_ \_**  
ID risorsa per la stringa "descrizioni degli elementi di layout di input" usato nei dati XML.

<span id="XML_SHADER_DESCRIPTION"></span><span id="xml_shader_description"></span>**\_Descrizione shader XML \_**  
ID di risorsa per la stringa "shader Description" usato nei dati XML.

<span id="XML_PSO_ROOT_SIGNATURE_OBJECT"></span><span id="xml_pso_root_signature_object"></span>**\_ \_ \_ oggetto firma radice PSO \_ XML**  
ID risorsa per la stringa "oggetto firma radice PSO" utilizzato nei dati XML.

<span id="XML_BUFFER_LOCATION"></span><span id="xml_buffer_location"></span>**\_posizione del buffer XML \_**  
ID risorsa per la stringa "percorso buffer" utilizzato nei dati XML.

<span id="XML_BUFFER_FILLED_LOCATION"></span><span id="xml_buffer_filled_location"></span>**\_percorso di \_ riempimento del buffer XML \_**  
ID risorsa per la stringa "percorso di riempimento del buffer" utilizzato nei dati XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS"></span><span id="xml_d3d12_virtual_address"></span>**\_ \_ Indirizzo virtuale D3D12 \_ XML**  
ID risorsa per la stringa "d3d12 Virtual Address" usato nei dati XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS_OFFSET"></span><span id="xml_d3d12_virtual_address_offset"></span>**\_ \_ \_ Offset indirizzo virtuale D3D12 \_ XML**  
ID risorsa per la stringa "offset indirizzo virtuale d3d12" utilizzato nei dati XML.

<span id="XML_D3D12_OFFSET_IN_BYTES"></span><span id="xml_d3d12_offset_in_bytes"></span>**\_Offset D3D12 \_ XML \_ in \_ byte**  
ID risorsa per la stringa "offset d3d12 in byte" usato nei dati XML.

<span id="XML_MAPPED_TILES"></span><span id="xml_mapped_tiles"></span>**riquadri con \_ mapping XML \_**  
ID di risorsa per la stringa "tessere mappate" utilizzato nei dati XML.

<span id="XML_POOL_TILES"></span><span id="xml_pool_tiles"></span>**riquadri del \_ pool XML \_**  
ID risorsa per la stringa "riquadri del pool" usata nei dati XML.

<span id="XML_TILE"></span><span id="xml_tile"></span>**\_riquadro XML**  
ID di risorsa per la stringa "Tile" utilizzato nei dati XML.

<span id="XML_OPTIONS"></span><span id="xml_options"></span>**\_Opzioni XML**  
ID risorsa per la stringa "Options" usata nei dati XML.

<span id="XML_TILE_LIST_MODE"></span><span id="xml_tile_list_mode"></span>**\_ \_ modalità elenco dei riquadri XML \_**  
ID risorsa per la stringa "modalità elenco sezioni" usata nei dati XML.

<span id="XML_TILE_POOL_SLOT"></span><span id="xml_tile_pool_slot"></span>**\_slot del \_ pool di sezioni XML \_**  
ID risorsa per la stringa "slot del pool di sezioni" usato nei dati XML.

<span id="XML_TILE_COORDINATE"></span><span id="xml_tile_coordinate"></span>**\_coordinata del riquadro XML \_**  
ID risorsa per la stringa "coordinata affiancata" usata nei dati XML.

<span id="XML_HEAP_TYPE"></span><span id="xml_heap_type"></span>**\_tipo di heap XML \_**  
ID risorsa per la stringa "tipo di heap" utilizzato nei dati XML.

<span id="XML_RESOURCE_USAGE"></span><span id="xml_resource_usage"></span>**\_utilizzo delle risorse XML \_**  
ID di risorsa per la stringa "Usage risorse" usato nei dati XML.

<span id="XML_RESOURCE_USAGES"></span><span id="xml_resource_usages"></span>**\_utilizzi delle risorse XML \_**  
ID risorsa per la stringa "utilizzi risorse" utilizzato nei dati XML.

<span id="XML_SUBRESOURCE"></span><span id="xml_subresource"></span>**sottorisorsa XML \_**  
ID risorsa per la stringa "subresource" utilizzato nei dati XML.

<span id="XML_SUBRESOURCE_USAGE"></span><span id="xml_subresource_usage"></span>**\_utilizzo delle sottorisorse XML \_**  
ID risorsa per la stringa "utilizzo risorse risorse" utilizzato nei dati XML.

<span id="XML_COMPUTE_SHADER"></span><span id="xml_compute_shader"></span>**\_shader di calcolo \_ XML**  
ID di risorsa per la stringa "compute shader" usato nei dati XML.

<span id="XML_COMMAND_LIST_12"></span><span id="xml_command_list_12"></span>**\_Elenco di comandi XML \_ \_ 12**  
ID di risorsa per la stringa "command list 12" usato nei dati XML.

<span id="XML_PSO_RENDER_TARGET_FORMATS"></span><span id="xml_pso_render_target_formats"></span>**\_formati di \_ destinazione di rendering XML PSO \_ \_**  
ID risorsa per la stringa "formati di destinazione di rendering PSO" usato nei dati XML.

<span id="XML_RENDER_TARGET_VIEWS"></span><span id="xml_render_target_views"></span>**\_visualizzazioni di \_ destinazione di rendering XML \_**  
ID risorsa per la stringa "visualizzazioni destinazione rendering" utilizzata nei dati XML.

<span id="XML_SHADER_RESOURCE_VIEWS"></span><span id="xml_shader_resource_views"></span>**\_ \_ viste delle risorse dello shader XML \_**  
ID risorsa per la stringa "viste risorse shader" utilizzato nei dati XML.

## <a name="requirements"></a>Requisiti

<table><colgroup><col style="width: 50%" /><col style="width: 50%" /></colgroup><tbody><tr class="odd"><td><p>Intestazione</p></td><td>Vspixengine. h</td></tr></tbody></table>

 

 



